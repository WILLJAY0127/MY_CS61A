字典是Python内置的一种数据类型 用来存放键值对键 就是用来查找数据的 用键来查找对应的数值 字典的写法 无论是代码还是显示值 都用花括号和冒号来分隔键和值 我们来看个例子 这个复制语句把numerals设为一个字典 用三组键值对来构建 用逗号分隔开 逗号用来分隔不同的键值对 在这个例子里 所有的键都是字符串类型 所有的值都是数字 但并不是 非得这样 键和值都可以用数字或字符串 创建好numerals 字典后 最常用的操作 就是根据键来查找对应的值 字典的查找语法和列表很像 但不能直接放索引 比如想获取索引0的元素 这样就会抱见错误 字典的方括号 只能放键来查找 我可以查找v这个键 这样就得到了数字5 这个值对应的是v键 我也可以查找x键 但查不了xray 因为它不在字典中 我也不能通过值来反向查找对应的键 比如你想查找数字5对应的键 但这也是不行的 字典查找只能是单项的 字典也是序列结构 具体来说是单项的字典 所以如果我把numerals转换成列表 就能看到所有的键 这样就能在4循环里便利所有键 那如果想便利所有的值呢 其实也可以做到用这个语法就行 用numerals values 记得加上括号 返回的结果看起来有点特别 显示了这个字典的数字 所以我可以把这个字典的数字 转换成列表 这样就能看到所有的键 这样就能在4循环里 便利所有键 这点特别 显示的是dict values 1,50 这是一种特殊的序列类型 不过它本身并不是列表 所以它不能像列表那样做所有操作 但很多功能还是有的 比如说 比如可以对它们求和 或者用4循环来便利每个值 要是你确实需要一个真正的列表 可以在numerals values上调用list函数 这样得到的就是普通列表 而不是字典直视图 这样就能看到所有的数字 这是字典特有的一种特殊序列 就像ranges专门处理整数的特殊序列一样 我们可以创建空字典 包含单个建制队的字典 或者多个建制队的字典 就像我们之前看到的那样 另外还有一个特点是 字典中的值 本身可以是任意复杂的数据结构 就像列表的元素 可以是另一个列表一样 字典的值也可以是列表 

我们可以这样写 哦对了 这里还需要加上引号 我们可以让某个值是一个列表 而另一个值是字符串 这样完全没问题 如果我们把这个字典叫d 我可以取到建一对应的元素 也可以取到建3对应的元素 我还能数数有几个元素 结果是两个 这说明有两个建 对应这些建的值的长度 可能不同 这个值有两个元素 因为它是个列表 这个值有多少个元素呢 这是个五个字母的字符串 关于字典的建呢 有两个重要的限制需要注意 首先建是不能重复的 比如我把建1设为first 然后又想把建2设为first 然后又想把它设为second 最后得到的字典里1这个建只会出现一次 所以如果想让一个建对应两个不同的值 就应该让它对应一个包含这些值的列表 第二个限制是建不能是列表或者字典 

这样就会报错 unhashable type list 不可hash 是计算机科学里的 专业术语 其实我很想给大家详细讲讲 但我得给61b课程留下内容 大家在那里会学到 或者你也可以在网上查查相关资料 之所以会出现这个错误 是因为 我试图用列表来当建 这在python中是不允许的 同样的 我也不能把字典当作建来用 就会出现同样的错误 但把字典作为值是完全可以的 什么都可以定义的 什么都可以当值 这些限制只针对建 我们再来总结一下字典的限制 字典就是建值对的集合 但字典的建不能是列表或字典本身 而且建必须是唯一的 不能有重复 每个建最多只能对应一个值 第一个限制跟python字典的底层实现机制有关 第二个限制则是字典抽象概念的一部分 也就是说字典的设计就是要通过建来找到对应的值 如果同一个建出现了多次 那就没法确定到底返回哪个值了 因为会有两个候选 所以字典的核心功能就是通过特定建来查找对应的值 如果你想让一个建对应多个值 可以把它们存储在序列中 比如列表里 你也可以用字典推导式来创建字典 这是一种复合表达式 你要在这里写明建的样子值的样子 然后指定数据的样子 写一个连接表达式 它会给你生成一个序列 用来构建建值对的 你可以筛选掉其中某些元素 并且为了说明如何构造建和值 你可以给这个连接表达式中的每个元素起个名字 过滤表达式最后这个部分是可选的 所以有个简化版本会省略这部分 这是一个求知后生成新字典的表达式 它的求知过程会添加一个以当前真为副本的表达式 以当前的新真为副本的新真 先创建一个空字典 作为表达式的结果值 然后我们在表达式完全求知前把这个字典填满 对于通过这个迭代表达式得到的每个元素 你会把这个名称绑定到第一步中创建的元素 这样名称就有了一个值 就是序列里的某个元素 然后如果过滤条件判断为真 就在结果字典里添加一个见值对 见来自见表达式 值来自值表达式 我们来看一个字典推导式的例子 这个推导式表示对于1 2 3 4中的每个x x的平方对应x 当x大于2时还包括5 首先x会被复制为1 x不大于2 所以这个就被跳过了 这个也一样 但这个值大于2 所以我们就添加一个 3的平方到3这个见值对 结果就显示在输出里了 对4和5也是同样处理 最终通过计算这个字典推导式 我们得到一个把数字映射到其平方根的字典 我觉得可以举个例子来试试这种推导式 好 我们来看一下 我们来实现index函数 这个函数接受三个参数 键序列 值序列 以及一个双参数匹配函数 它会返回一个字典 这个字典将键映射到列表 其中每个键k对应的列表包含了所有满足条件的值v 这些v要满足匹配函数match 返回真值 当我们说从键到列表的字典时 意思是字典中的每个条目都会对应一个键 而对应的值会是一个列表 所以这里的意思是用所有这些键 7 9和11来构建一个字典 这就是我们在示例中看到的情况 这些值来自我们提供的列表 也就是30到50这个范围 但是每个键所对应的列表 其实就是这个范围内的某些元素 就是那些能让这个函数返回真值的元素 这个函数的意思是 对于给定的键和值之间的函数 只有当键能整出值时才返回真值 也就是说如果值是键的倍数 所以这个函数最终会返回一个字典 比如键7就对应着这个范围内 所有7的倍数组成的列表 同理适用于9和11 这其实就是字典推导式的一个很自然的应用场景 因为我们要用的键其实已经都在keys列表里 准备好了 所以实际上确定键表达式并不需要太多工作 就是K 这里的K要和这个K匹配 不过我也可以用其他字母根据描述 用K感觉挺合适的 那么如何构建一个列表 包含所有与键K匹配的值呢 这就是列表推导式的一个例子 获取所有的V 其中V只要match 返回真值 注意这个列表推导式是嵌套在字典推导式内部的 这个表达式既可以指向值V 也能指向键K 完成匹配操作 这样我们就实现好这个函数了 