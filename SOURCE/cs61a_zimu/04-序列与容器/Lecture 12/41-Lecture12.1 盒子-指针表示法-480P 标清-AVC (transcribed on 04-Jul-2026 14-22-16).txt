盒子指针标记法 就是我们在环境图中 用来表示列表的方法 所以我们需要这种标记法的原因呢 是因为序列数据 实际上可能会变得相当复杂 而这都是因为闭合属性的存在 一个组合数据值的方法 要满足闭合属性 就是说组合后的结果 本身还能用同样的方法 继续组合 如果我能把元素放进列表里 那我就能把这个列表 再放进另一个列表里 B包之所以强大 是因为它让我们能够创建层次结构 层次结构由各个部分组成 而这些部分 本身又由更小的部分组成 以此类推 这种表示方法非常实用 能用来描述世间万物 所以这里的关键点在于 列表不仅可以包含 各种元素 还能把其他列表 也作为元素包含进来 这意味着我们需要一种方法 来理清各个元素之间的包含关系 这时我们就需要用到 盒子指针标记法 在环境图中 列表用一排带索引的相邻盒子表示 每个元素对应一个盒子 每个盒子要么存放一个原始值 要么指向一个复合值 所以如果我说 pair等于列表1 我在环境图里就这样表示 把名称pair绑定到包含1和2的列表上 角落里的这些数字就是列表的索引 因为这个列表有两个元素 所以大家会看到两个相邻的盒子 现在来看个更复杂的例子 nested list绑定到了一个列表上 这个列表包含了一个列表 另一个列表 以及 第三个列表 而第三个列表本身又包含了其他列表 这里面包含了各种各样的元素 甚至还有个函数 好 我们来看一下屏幕上显示的所有内容 嵌套列表是个三元列表 所以大家看到它对应着三个相邻的矩形框 而嵌套列表的每个元素本身也是个列表 这就是为什么我们会看到指向复合值的箭头 列表都是复合值 函数 也是复合值 任何有多个部分的对象都会用箭头指向它的图示 好 这里我们看到索引0那个元素就是个包含1和2的列表 索引1处的元素就是空列表 这里就能看到这个空列表 索引3处的元素本身也是个列表 里面又包含两个列表 所以才会形成一个列表 像这种层次结构 它由不同的部分构成 这些部分又由更小的部分组成 像sinfos这些原始值 还有none都可以直接放进盒子里 函数也一样 我们按之前的方式写函数 然后用小箭头来表示 它是这个内层列表中索引为1的元素 而这个内层列表又位于外层列表之中 