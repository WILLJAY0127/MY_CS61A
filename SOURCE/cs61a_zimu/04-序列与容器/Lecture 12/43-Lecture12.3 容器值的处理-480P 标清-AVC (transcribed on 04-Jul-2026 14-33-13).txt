处理容器数据时 通常需要遍历列表中的所有数值 或者字典中 所有相关的数值 但Python内置了一些函数 能帮我们更高效地完成这个操作 特别是 有几个专门做序列聚合的函数 这些函数都接受可迭代参数 然后把它们聚合成一个单一的结果 下面我来给大家看看 Python 3的实际文档 帮大家理解它的工作原理 比如 有个内置函数叫sum 它能接受可迭代参数 比如列表 那么这种写法并不是说调用时要加方括号 而是Python文档中 用来说明第二个参数是可选的 所以你可以只传一个列表参数 来调用sum函数 它就会对这些值求和 或者你也可以 传两个参数 一个值列表和一个起始值 它会遍历可迭代对象中的所有值 然后把它们累加到起始值上 好 这里说的是返回数字可迭代对象的总和 不是自负串 加上参数start的值 这个参数的默认值是0 如果可迭代对象为空 就直接返回起始值 2 3 4 这三个数加起来是9 我们来看看 求2 3 4的合选值是0 这个函数不能直接处理自负串 那么这个起始值是干什么用的呢 比如说如果我一开始用5作为起点 那么它就会先加上5 再加上2 3和4 这有什么用呢 比如假设你要相加的不仅仅是数字数据 你需要先提供一个该类型的值 作为起始值 还记得我可以把2和3加到列表4上 得到一个更长的值值 同样地我可以对包含2 3和4的列表进行求和 只要满足 我最初是从一个列表开始的 如果我一开始没用列表 如果我把这个列表加到数字0上 这是行不通的 所以如果不用出始值直接求和 就会出现这种情况 但因为我一开始给了一个空列表上 然后加了2和3 接着又加了4 最后就得到了列表234 max 函数有两种用法 你可以传给它一个可迭代对象 比如列表 还有一个可选的键函数 它就会返回最大值 或者你也能传入多个参数 比如一串数字 再加上一个键函数 然后它会找出这些参数的最大值 所以文档说明 如果只有一个参数 就返回其中的最大值 也就是 传入列表里的元素 如果有两个或更多参数 就返回最大的那个 所以从0到5这个范围里 最大值是4 或者我也可以写成max1234 最前面加个0 这样效果是一样的 那么这个键函数 又是怎么回事 其实 文档里没有明确说明 但实际上 它会对每个元素 应用一个函数 然后根据函数返回值 来计算最大值 所以 我们可以问 这样的问题 比如 如果我们关心的代数表达式是7x4 那么在0到9的范围内 最大值是多少 

如果我们把这个函数设为函数 那么它就会告诉我 调用这个函数 得到的最大值是3 好 如果我拿这个函数 把它应用到数字3上 就会得到8 但如果用在2上 得到的值 就会小一些 如果应用到4 或者5 6 7 得到的值 就会是3 如果我把这个函数 设为函数 就会是3 那么这个函数 就会是3 那么这个函数 就会是3 那么这个函数 得到的值 也会更小 所以它才会返回3这个值 接下来看第三个函数 凹凹函数 接受一个可迭代对象 当所有元素经过步伍转换后 都为真实 就返回出 就是 对每个元素进行布尔值判断 如果可迭代对象为空 也会返回出 这个 步 函数是什么呢 步函数 就是判断一个值的 真假 所以 比较容易 跟 Template cake 有返回 Albanv construiti 和 oily 原理 是 etc 输入 函数 大家看一下 大家 我想问range 5里的每个x是不是都小于5 我会得到true true true true true 凹函数的作用就是把它们汇总起来 然后告诉我这些值是否都是true 我不一定要传波尔值列表给它 我可以传数字列表01234给它 这样它就会告诉我因为0是假值 所以这些不全是真值 以上就是三种处理序列的内置函数 另外还有命函数和any函数 它们分别对应max和all的互补功能 