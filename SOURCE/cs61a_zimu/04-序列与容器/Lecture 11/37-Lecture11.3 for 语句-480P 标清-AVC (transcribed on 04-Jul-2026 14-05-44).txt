序列在计算中非常重要 所以人们就开发了一些新的语句 来帮助我们处理序列 其中就有for语句 for语句就是专门用来便利序列的 我来给大家演示个例子 然后再详细说说它是怎么工作的 

我要写个函数来计算某个值 在序列S中出现的次数 就是统计这个值 在序列S里出现了多少次 如果用while语句来实现 我可能会写成total和index都等于0 在序列S中出现的次数 在索引小于序列长度的情况下 我会访问序列S中对应索引的元素 这样我就获取到了该索引对应的特定元素 如果我一开始将索引设为0 就能获取第0个元素 之后如果我更新索引值 就能获取序列中的下一个元素 在程序执行过程中的某个时刻 我会将索引重新绑定为索引加1 通过这样的方式 我会便利列表里的每个元素 那对于每个元素要做什么呢 我需要判断这个元素是不是我要找的值 如果是的话 就让总数加 最后返回总数即可 那么到目前为止 我们做了什么 我们定义了一个count函数 它接受一个序列 用来统计某个值出现的次数 比如数字1在这个序列里出现了 噢找到了出现了三次 123 好这个文档测试很有用 我们直接粘贴到这里 然后我们来讨论这个实现 我们试着让它更简洁些 这里我们看到total被重新复制为total加1 Python其实有更简洁的写法 我直接用total加等于1就行 这是一种不同的写法 我们可以看到这个是一个重新的复制运算符 它会把total更新为原来的值加1 那我这里也可以做同样的简化 好我们已经取得了一些进展 但我觉得我们还能做得更好 这个实现的核心部分就在这里 而剩下的代码只是便利序列中的所有元素 由于这个操作非常常见 所以Python专门提供了for-reg for-reg 让我们完全不用操心索引了 而不需要再写val-index小于lan-s 我直接写成forelement in-s就行了 这样就能取代那两行代码 还能省掉底部的索引 自增步骤 现在我们来分析一下这个实现的逻辑 它的意思是 我要统计在s中value出现的总次数 对于s里的每个元素 就是这个值 那么总数就加1 要是我运行文档测试 就会看到它在这个序列里统计1的个数 预期结果应该是三次返回的结果 完全正确测试通过了 好刚才我们演示了一个序列迭代的例子 在这个例子中我们使用了for-reg 那么它是怎么运作呢? 具体来说element这个名称 会被绑定到当前环境的第一个框架里 注意不是新的框架for语句 不会创建新的框架 当element名称被绑定后 接着就会执行for语句的代码块 然后变量element就会指向s中的下一个元素 然后我们再次执行循环体 所以当我们使用for循环时 它会多次执行循环体里面的代码 执行的次数等于s中元素的数量 每次循环element都会对应s中不同的元素 这就是完整的执行流程 for语句的语法结构是这样的 你需要一个变量名 还需要一个表达式 还需要一个代码块 第一步是计算这个头部表达式 它必须返回一个可迭代值 我说我们现在讲的是迭代 可迭代值就跟这个相关 但这个术语我后面几周才会回来 我会详细定义 现在大家先理解为 这个表达式要能算出一个序列 稍后我们会看到除了序列 还有很多其他类型也可以用来迭代 现在大家先这么理解就好 我们需要一个能够生成序列的表达式 我们需要为序列中的每个元素命名 接着对于序列里的每个元素 我们会把这个名字绑定到当前真的元素上 然后执行对应的代码块 比如说 for语句还有个很酷的功能是 我们可以在for语句头部直接做序列解包 比如说 我有一个包含多个列表的列表 我这里把它叫做pairs 我想统计其中元素两次相同的配对数量 那么我来逐个检查这些配对元素 我会逐一判断1和2相同吗 2和2相同吗 相同 那么相同计数器就加1 3和2不同 但4和4相同 所以计数器需要再次加1 那么实现这个功能的for语句怎么写呢 这种方法只适用于固定长度的序列组合 比如成对序列 这时候你就可以把每个键值对拆分到不同的变量名 所以我们可以写4x y in pairs 这样的语法 这里的pairs就是一个包含多个键值对的序列 x和y现在会分别对应每个键值对中的元素 而不是整个键值对本身 这样就能判断x和y是否相等了 如果相等 我们就让same count加1 这样我们就为固定序列中的每个元素都起了名字 这就是我们所说的序列解包 这就像夺重复值 就像我们写xy等于某个配对时一样 这里的每个变量名x和y都会绑定到配对中的特定值 