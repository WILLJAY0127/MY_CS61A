Range是另一种序列类型 也就是说它属于序列 但它不是列表 Range的作用是用来表示连续整数序列 其实Range可以表示各种整数序列 但我们主要关注连续整数的情况 就是按顺序递增的那种 想象一条无限的竖轴 上面布满了整数 我们来看其中的一小段 Range的作用 就是在这条长竖轴中 通过指定起始值 来选取一段有限长度 以及一个结束值 选取这两个值之间 所有递增排列的整数 好 我这样画图的方式很重要 这两个点都正好指向竖轴上 数字前面的位置 这意味着2确实包含在从2到2的这个范围内 但数字2不在这个范围内 所以是包含起始值 但不包含结束值 这个设计确实挺特别的 但恰恰是这种做法 让很多事情变得更简单 计算Range的长度很简单 直接用结束值减去起始值就行 所以2减去-2等于4 这里就有4个元素 大家可以看到 选取元素也很简单 只需要用起始值加上索引就行了 索引是从0开始记的 所以索引0对应的元素是-2 索引3对应的元素就是-2加3等于1 好 这样一来Range就具备了长度和元素选取的选项 所以它确实是一种序列 尽管它不是列表 如果想看具体元素 要怎么转成列表呢 我们可以用列表构造函数 就是个内置函数 叫list 对 任何序列调用它时 就会返回包含该序列所有元素的列表 所以Range本身不是列表 但调用list的函数 就能得到包含所有元素的列表 这就是列表构造函数的选项 列表构造函数的作用 Range序列还有个特点就是 如果你省略掉其中一个数字 只给出了一个数字 系统会把它当作结束值 起始值 默认设为0 所以Range列出来就是 0 1 2 3包含0 但不包含4 好 比如说 我创建一个从5到8的Range 它不是列表 而是一个Range序列表 它不是列表 如果我想得到列表 我需要调用list函数来处理这个值 这样就能看到里面具体有哪些元素了 好 既然我们了解了Range是什么 我知道这个Range包含5 6 7 但如果我想确认一下 确保它包含了起始值 同时不包含结束值 我可以调用list函数来确认 一个到4的Range是从0开始 不包括4的序列 如果我把这些值列出来 会看到结果是0 1 2 3 那么我们可以用Range做什么呢 嗯 有很多时候 我们需要的序列 正好就是一组递增的整数序列 比如我想对某个数字n以下的所有整数求和 我可以记录一个总和变量 逐个取出n的整数求和 从0到n这个区间内的每个元素i 然后把总和更新为总和加上i 最后返回这个总和 这样的话 如果我对5以下的数求和 我会累加1 然后2 3 4还有0 这样就能得到10 有些情况下 我们其实并不需要关心具体的整数值 就只是想重复执行特定次数而已 比如说 我想打印三次 go bears 典型的写法就是使用这样的4循环语句 现在如果我运行这个代码 就会输出go bears go bears go bears ranger 就表示这个范围里有三个元素 我们给每个元素都起了个名字 但其实我们并不在乎具体叫什么名字 因为我们根本不会用到这个变量 所以有个约定俗成的做法 就是用单个下滑线 或者直接留空 就是告诉其他程序员 这个变量名实际上不会在别处使用 就算我写个x在那里也没问题 但我根本就不会用这个变量名 所以这里写什么其实都无所谓 这其实就是表明你不在意这个变量名 