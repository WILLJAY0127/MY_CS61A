列表是Python中非常常用的内置数据类型 创建一个列表的话 我们可以使用列表字面量 要把这个列表附给一个变量 直接用复制语句就可以了 比如这里有一些奇数 我可以取索引为0的元素 或者索引1 2 3的元素 还可以获取列表的长度 注意最后一个元素的索引 比列表长度要小1 我们可以把索引理解为从起始位置的偏移量 偏移量为0就是列表的开头 接着是偏移量为1的位置 再是2 3的位置 以此类推 这个其实就是调用表达式 而这个是元素选择表达式 你需要先计算一个表达式来得到列表 再计算索引值 所以我们可以在这些位置使用任意表达式 还可以按需要自由组合结果 比如我可以写odds3-odds2 这样我就得到了数字2 然后我可以把这个结果作为索引来访问odds列表 也就是说这里可以放任意表达式 它会被优先计算 那里也可以写任意表达式 然后再计算这个 当我们得到这里的值之后 这个值是个列表 还有这里的值 这是个整数 你就可以访问列表中的特定元素 那么在使用列表时 我们会用到列表字面量 我们还可以用表达式来描述列表中的每个元素 这样的话 digits变量就会指向相同的列表 不管我们是直接写下数字 还是用表达式来计算出这些数值 要获取元素的数量 我们可以用len的函数来获取列表长度 len是Python的内置函数 如果我们想通过索引来查找某个元素 我们可以使用元素选择语法 或者用operator模块里的getitum函数 这两种方法效果是一样的 要把两个列表合并 或者让列表重复 我们直接用加法和乘法就行了 比如我说列表 我们可以用加法和乘法 比如我说列表和乘法 比如我说列表和乘法 加上digits列表乘以2 把digits列表乘以2 就是把里面的元素复制两次 然后把这些加到列表2的末位 这样我就得到了一个有10个元素的长列表 我也可以用operator模块里的add和μ函数来实现相同操作 最后要说明的是 列表元素不限于整数 可以是任何东西 甚至包括其他列表 比如 这里有一个列表 这里我用了一个列表字面量 里面嵌套了两个列表字面量 这样创建的就是一个包含两个元素的列表 而每个元素本身又是包含两个整数的列表 所以当我们获取pairs的第一个元素时 它包含了30和40这两个数 大家可以看到这里用了一个组合表达式 具体来说就是元素选择表达式 得到了一个列表 然后我再从这个列表中选取元素 就能拿到30这个数值 