我们来看看几个内置的列表处理过程 append函数会把两个不同列表的元素合并成一个新列表 或者如果你传入超过两个列表的话 就会得到一个包含所有传入列表元素的单一列表 map函数会对列表s中的每个元素调用过程f 然后把结果收集成新列表 filter函数同样会对列表s的每个元素调用过程f 然后把s中所有使得f返回真值的元素收集到一个新列表里 apply函数和map有点不太一样 它是把整个列表s作为参数传给过程f来调用的 所以函数f只会在s的所有元素上一次性调用 好的我们来快速试一下这几个操作 然后我们来看个例子 如果我有列表s由cons1 cons2和neo组成 那么将s追加到自身 就会得到元素1212 我可以多次追加s来获得所有ss中的值和s中的值 这与将s列出四次的结果完全不同 那样会得到一个嵌套结构 如果我用even为词对s的所有元素调用map 就会发现一不是偶数 但二是偶数 当然我也可以临时创建一个过程进行映射 2乘以x得按scheme的编程风格来写 遍历s的所有元素就得到1乘2等于2 2乘2等于4 过滤出s中的偶数值 结果就只剩偶数了 比如我输入5678 9就只会得到6和8 如果我问哪些元素是列表数字5 列表67数字8 以及只包含9的列表 那么我们会发现符合条件的列表元素是67和9 这两个列表 如果我想在每一个这样的列表前面加上数字5 我们可以用map来映射以下这个过程 对每个列表 我想用cons操作 把5加到列表前面 这样就会得到6和8的元素了 这样就会得到6和8的元素了 如果我想在每一个这样的列表前面加上数字5我们可以用map来映射以下这个过程对每个列表我想用cons操作把5加到列表前面这样就会得到6和8的元素 如果我想在每个这样的列表前面加上数字5 5 六七和五九 接下来我们看最后提到的列表处理过程 apply 当我们用apply 将quotient过程应用到参数10和5上10 实际上就是计算10除以5 再比如用apply将plus应用到1234这几个数上 效果就相当于直接调用plus 大家注意 如果我们用apply就相当于直接调用plus 这跟用map把plus映射到1234上的效果很不一样 这就相当于创建了一个列表 里面是和 但这样并不会把数字加起来求和 而如果用apply把plus应用到列表上就能直接求和 所以在scheme里我们就不需要专门写一个sum过程了 因为plus函数本身就能实现求和功能 