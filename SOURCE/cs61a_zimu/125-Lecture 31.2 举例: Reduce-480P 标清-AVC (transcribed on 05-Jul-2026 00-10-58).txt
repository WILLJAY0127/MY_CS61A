我们来看一个需要用到异常处理的场景 这是一个重要的高阶函数 叫做Reduce Reduce的作用是把整个值序列汇总成单个值 以下是Reduce函数的描述 IterTools模块中有一个内置的Reduce版本 但我们也可以自己编写 它接受一个函数、一个序列和一个初始值 然后用这个函数把序列里的元素成对组合 这个函数接受两个参数从初始值开始计算 比如用乘法对数字2、4、8进行Reduce 初始值为1 这相当于说用1乘以2再将结果乘以4 然后将结果乘以8 最终得到64 所以F是一个双参数 函数S是一个值序列 每个值都能作为F的第二个参数 而Initial必须是能作为第一个参数 最后一个限制是 F的返回值也必须能作为F的第一个参数 因为当你调用F时 你需要把这个值直接传给F的下一次调用 如果我们对Palo1、2、3、4进行Reduce操作 从初始值2开始 过程就是让2的指数次幂逐渐增大 我们先算2的一次方 得到2 然后 把这个结果平方得到4 接着算4的三次方 得到6、14 而64的四次方一下子变得很大 这就是Reduce函数的作用 只需2的一次方 结果平方再三次方再四次方 就能得到1600万 我们来实现这个函数 Reduce函数需要三个参数 函数F、序列S和一个初始值 它的作用就是用F函数从初始值开始逐个处理S中的元素 我们之前看的例子是用μ函数 从一开始对2、4、8进行Reduce计算 这样算下来应该得到64或者比如用Add函数 从0开始对1、2、3、4做Reduce 这样就能得到10 从operator的模块导入Add和μ函数 我能想到的最简单实现就是 对于S中的每个X 让Initial等于F函数 作用在Initial和X上的结果 然后我们就反复 我们可以看到 我们可以看到 我们可以看到 我们可以看到 我们可以看到 我们可以看到 我们可以看到 这个最后一个出始值 这个最后一个出始值 这个最后一个出始值 这样就能通过文档测试了 这样就能通过文档测试了 我们也可以用递归的方式来写 比如我们会写如果序列S为空 比如我们会写如果序列S为空 也就是序列为空的情况 那就直接返回初始值 没有其他需要处理的了 否则我们就取序列的第一个元素和剩余部分 否则我们就取序列的第一个元素和剩余部分 否则我们就取序列的第一个元素和剩余部分 然后返回初始值 一定要用reduce的结果, 参数是f剩余序列, 以及更新后的初始值也就是f, 

这样也能通过我们的文档测试, 那么怎么使用reduce函数呢? 比如说我们要写一个dividial函数, 它接收一个分子和一个分母序列, 把分子逐个除以这些分母, 这不正是reduce的用武之地吗? 或者用除法函数来reduce, 从分子开始对分母序列进行规约, 

我们可以用1024作为分子调用dividial, 依次除以24和8, 得到的结果就是16, 这就是除完所有数之后剩下的值, 那要是中间有个0会怎么样呢? 就会报除0错误, 假设我们不希望dividial这样除, 我们实际想要的是计算除以所有d的结果, 

但要是遇到除0错误, 我们就改成返回无穷大, 在python里就是这么得到无穷大的, 所以新实现里这种情况下会得到inf或无穷大, 但这里我们还是会得到16, 这样处理错误的好, 除10, 这样我们实现的reduce函数, 完全不需要知道除0错误的存在, 相反, 我们写个函数, 明确知道要调用reduce并传入可能引发除0错误的函数, 所以是由这个知道传入啥参数的函数来负责处理错误, 这样我们就实现了关注点分离, dividial函数专门处理除法运算, reduce函数专门负责规约操作, 但reduce完全不值得计算, 因为它是不知道除法是干嘛的, 或会引发什么错误, 而dividial也不需要知道如何组合元素, 它只知道自己要调用reduce函数, 