我们再来看个例子 计算从树的根节点到叶子节点这条路径上所有标签值的总和 然后把结果打印出来 有些递归函数是通过处理递归调用的返回值来构建最终结果的 而另一些则是通过向递归调用传递参数信息来累积结果 这两种策略在处理树结构时都很有用 下面我们就来看看 这两种方法的具体示例 在讲树结构之前 我们先回头看看我们的老朋友 阶乘函数一种写法是 如果n等于0 我们就返回1 否则我们就返回n乘以fact的结果 比如我们拿4来运行这个例子 那么就会得到4乘3乘2乘1等于24 这就是一个 获取递归调用返回值并进行操作的例子 这里呢 我们就是在乘以n 但还有另一种方法 我可以定义一个fact times函数 它接受n和k两个参数 而不是简单的返回n乘乘乘 这个函数会返回k乘n乘乘乘一直乘到1 

具体实现是这样的 如果n等于0 我们就直接返回k 因为0的阶乘就是1 否则我们就递归调用fact times并返回结果 我觉得这个例子最有趣的是 我们完全没有处理递归调用fact times的返回值 而是直接返回k乘乘乘乘1 而是直接返回k乘乘乘乘1 而是直接返回k乘乘乘乘1 而是直接返回k乘乘乘乘1 原样返回 也就是说 递归调用的结果必须等同于当前调用的结果 按照这个思路继续推理 也就是说 基准情形的返回值必须等于原递归调用的返回值 在这个例子中 这个推断确实成立 所以如果我们要计算k乘以n的阶乘 表达式是这样的 我们发现 这其实就是k乘以n再乘以n-1的阶乘 这部分是k乘以n 而这一块是n-1的阶乘 所以这里的结果是逐步累积起来的 等到遇到基准情形时 所有的乘法运算就都完成了 而在另一种版本里 你根本还没开始做乘法运算 当你到达基准情形时 这些都是返回之后才发生的 实际上 如果我们想用facti times 来定义facti times 这并不难 我们只需要返回调用fact times的结果 4的阶乘 结果还是24 好 现在我们来看一个数结构的例子 我这里有个文件 包含了数结构的构造函数 以及选择器函数 文件名是treePi 所以如果我想用上这些函数 我可以写个导入语句 把它们都引进来 现在我们可以 来创建一棵树 比如一个数字数 3作为根节点 然后有一个分支 是值为4的叶子节点 还有一个分支 不是叶子节点 包含了5和6 我们再来看一棵 用字母的树 我们把h作为根节点 它下面有一个分支 根节点是a 这个分支 有两个叶子节点 s和t 然后 我们还有一个e 作为h 根节点下的一个叶子节点 所以haste这棵树的底层结构 其实就是多层嵌套的列表 但我可以获取它的标签 得到的结果是h 也可以获取它的分支 好 假设我们要写一个函数 输入一棵树 找到所有叶子节点 然后为每个叶子打印 从根节点 到该叶子的所有标签之核 就像之前的fact times函数那样 我会添加第二个参数 也就是当前累计的 和 这样 我就能在变力过程中 不断累积这个总和 然后当我到达叶子节点 就直接打印出来 那么不管是不是叶子节点 都会有个标签 我们要把它加到当前的累加值里 接着 我们要检查 这是否是叶子节点 如果是 我们就打印当前的累加结果 这个值包含了叶子节点标签 以及所有上层节点的叶子节点 以及所有上层节点的标签 否则 我们就继续 便利所有分支 t 是一棵树 所以我可以对它调用branches 我会得到一个树的列表 所以每个b都是一棵树 而print sums 是一个可以对数 调用的函数 如果我以0为起点 在数字数上调用print sums 我会看到7 这个7 来自根节点的3 加上4 它就在根节点的3 在根节点下面 是个叶子节点 另一个需要打印的 是从3到5 这条路径上的总和 但5 不是叶子节点 所以先不打印 继续到6 3加5加6等于14 这就是我们下面得到的 14 如果我们在haste数上调用print sums函数 结果是 我觉得我们应该从一个 空字符串开始 这样就可以不断添加字符串 这时候 我们就打印出 has 对应路径 has 到达叶子节点 或者 hat hat 也是叶子节点 还有he也就是 根节点 下面的这个叶子 这个函数的结构呢 就是我们在便利时逐步累加总和 总是把新值传给so far参数 我们可以直接写so far加上 label t 不用这个复制语句 那样写 也可以 或者我们直接更新so far 然后用新值来打印 或者传给所有的低规调用 