多态函数 多态函数就是能处理多种不同数据类型的函数 STR和REPR就是很好的例子 因为你可以把任何对象传给内置的STR和REPR函数 它们总能给出合适的结果 这是怎么做到的呢 基本思路是 REPR函数会让它的参数自己来展示自己 在Python里 这是通过特殊方法名实现的 说它特殊是因为它对应着内置函数 但这个通用思路 就是让函数直接跟参数商量该怎么做 这个思路显然不止适用于Python 

具体来说 REPR函数会调用参数的EveryPart方法 这个无参数方法会返回REPR字符串 所以我可以在half上调用REPR 也就是分数二分之一 或者直接调用它的方法 方法调用会在类里面插着 所以实际上是fraction类 知道怎么生成分数的 REPR函数 而不是REPR函数本身 同样的 Sdarr函数会调用一个无参数方法 这个方法的名字是fusdarr 这也是一个特殊方法名 所以呢 如果我想的话 可以直接调用这个方法 这样就能为分数对象生成对应的字符串表示 所以这里有个很重要的概念 我们可以写一个函数 比如说 我们想要一个函数的分数项目 比如star 或REPR 这个函数本身 其实没多少逻辑 它只是把处理权委托给传进来的参数 通过调用这个参数上的特定方法 来实现 那我们来看一下REPR和STR 具体是怎么实现的 实际上情况 比我刚才描述的 稍微复杂一点 它并不是简单的调用参数上的REPR方法 实例中的REPR属性 会被忽略掉 内置的REPR函数 只会调用类属性中的REPR 那我们 要怎么实现这个功能呢 那么下面哪个函数定义对应着这样一个REPR函数 它接收参数 查找类属性REPR 然后调用它 

这里有五个选项 只有一个符合我刚才描述的要求 大家可以思考一下 答案就是三 二一 这个函数 能够绕过实例属性 或者通过查看参数类型来忽略它们 这样就直接找到了类本身 这样一来 直接访问类的REPR属性 就保证能获取到类属性 现在这个类属性本身是个函数 但这个函数不是绑定方法 因为是在类级别查找的 所以需要显示传入X参数 才能让这个类属性在我们关注的X上被调用 稍后 我们会看个具体例子 STR函数也很复杂 比REPR还要复杂一些 实例属性里的STR会被忽略 如果类上根本没有STR属性 那么调用STR就会直接返回REPR的结果 所以默认情况下 这两个函数的结果是一样的 只有当你特意区分它们时 结果才会不同 这里我们用到了一个很重要的概念 这就是所谓的接口 所以之前我们讲面向对象编程的时候 我说过这个理念的核心就是 对象之间会相互传递消息 这就是它们相互沟通的方式 具体到语言层面 它的实现机制是通过查找属性或方法来实现 从而完成相互沟通 这就是消息传递的核心思想 消息传递其实是一种比喻 而实际操作中 我们是通过查找属性来实现消息传递的 属性的查找规则设计的很巧妙 这样一来 不同数据类型只要拥有相同的属性名 就能响应同一条消息 再来说说共享消息这个概念 如果某个属性名出现在不同类中 都能触发相同的行为 这就是一种很强大的抽象方式 这就是我们所说的接口概念 接口其实就是一组共享消息 以及相关规范 告诉我们这些消息该做什么 有什么含义 比如说那些实现了rapper和style方法的类 让这两个方法分别返回排座 可以解析和人类可读的字符串 接口不一定是语言内置的 不过我们刚说的这个确实是内置的 比如说如果你定义一组类都拥有相同的方法 而且这些方法的功能都很相似 那么你就创建了一个接口 下面我们来看看如何构建一个实现这个接口的类 我们知道python内置了一个fraction类 我们来看看 我们来创建一个类似的类 我把它叫做ratio 这样就能区分开来 创建ratio实例时 只需要传入分子和分母 

如果我们希望ratio类的实例能够自我显示 我们需要定义一个rapper方法 它会返回一个字符串 格式是ratio 后面跟着括号 这里字符串中有两个站位符 我们用format方法来填充 self num添入第一个 也就是第0个站位符 self denom添入第二个 如果我们也想要一个人能看懂的字符串 参照fraction类的做法 我们就写成0除以1的形式 其中0代表分子 1代表分母 

现在我们来创建一个比例对象 half 然后打印它 就能看到友好的显示格式 如果直接显示half 就会得到对应的python表达式 