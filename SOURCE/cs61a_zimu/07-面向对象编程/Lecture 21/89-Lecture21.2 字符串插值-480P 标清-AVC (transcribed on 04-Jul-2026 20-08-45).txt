Python中的f字符串功能 能让你在字符串字面量里直接嵌入表达式 来生成字符串 这是Python中进行字符串差值的首选方法 什么是字符串差值呢 字符串差值就是对一个字符串字面量进行求值 也就是引号里的一些文本 但同时里面还包含表达式 这些表达式会被求值 然后成为字符串的一部分 当你需要构建一个字符串 里面既有预定义的文本 又有表达式计算结果的时候 就会用到字符串差值 其实我们可以用字符串拼接来完成这个操作 比如 如果你想创建一个包含圆周率数字的字符串 你可能需要把圆周率 以这个字符串和包含圆周率数字的字符串相加 再加上 包含省略号的字符串 当然 这个结果还可以用在更大的表达式中 比如 用来打印输出 字符串差值 是生成这个字符串的另一种方式 通过Python的F字符串功能 这种方法 既更快又更简洁 我们管这个叫F字符串 因为它在引号前面有个F F字符串的特殊性 在于 它可以 包含大括号里的其他表达式 这些表达式 会被求值 你可以写想要的文本 以及 想要在字符串中求值 并包含的表达式 这是字符串字面量 但里面嵌入的是Pi的值 而不是Pi这个字符串 同样 这个表达式会求值得到一个字符串 所以我可以把它嵌入更大的表达式 比如 用来打印 F字符串 这些字符串求值的结果 包含每个字表达式值的字符串形式 我们稍后会看一些例子 这些字表达式 会在当前环境中进行求值 也就是说 凡是能用拼接方式弄出来的字符串 也都能够通过字符串差值来实现 好 来看一个F字符串的例子 这里只是一段普通的文本 这里面写的是2加2等于1 这里面写的是2加2等于1 这里面写的是2加2等于2 它会按普通字符串来处理 它会按普通字符串来处理 但如果我在其中某些地方加上大括号 但如果我在其中某些地方加上大括号 但如果我在其中某些地方加上大括号 那么这部分就会被当作Python表达式 那么这部分就会被当作Python表达式 那么这部分就会被当作Python表达式 那么这部分就会被当作Python表达式 先进行求值 然后嵌入到最终的字符串里 然后嵌入到最终的字符串里 记住一定要有F前缀 记住一定要有F前缀 记住一定要有F前缀 如果没有这个F 那你就会得到一个普通字符串 然后Mac折 那你就会得到一个普通字符串 那你就会得到一个普通字符串 里面包含大括号 1,рен 只要是合法的Python表达式都可以放进来 如果我非要较真的话 我也可以写个复杂的Lambda表达式来得到4 关于引号的使用有一些具体限制 因为我们现在处在字符串内部 以及大括号的使用方法 这些细节大家感兴趣的话可以自行了解 这些不是本课程的必修内容 因为这些是Python特有的一些底层细节 但这种通用概念包含表达式的字符串字面量 也就是字符串差值 在很多编程语言中都存在 我之前提到过 F字符串表达式求知后 得到的是子表达式值的字符串形式 所以如果我们有一个对象 F它有不同的str和repar 这边是 repr字符串 这边是str字符串 那么当我们构建F字符串时 会发生什么 比如半个的 一半等于半乘以半 你得到的是str字符串 而不是repar字符串 如果你真的想要repar字符串 你可以直接调用repar 这样就能得到了 子表达式的求知规则和Python其他地方是一样的 它们可能会产生副作用 假如我们有个列表987 然后构建字符串时 用到spop spop s 这些表达式会按顺序执行 这个得到7 这个得到8 现在列表s里就只剩下数字9了 