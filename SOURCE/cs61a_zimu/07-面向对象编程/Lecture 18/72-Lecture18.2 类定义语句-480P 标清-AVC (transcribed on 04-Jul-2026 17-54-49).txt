除了内置的类以外 我们也可以定义自己的类 然后通过创建这些类的实例 来构建新的对象类型 一个类描述的是 它实例的行为特征 比如说 我们要表示银行账户 我们想把账户相关的信息 比如余额和持有人 与相关的行为打包在一起 比如存款和取款这些操作 具体来说 它的工作方式是 就是所有银行账户 都需要有余额和持有人 也就是说 这需要由account类来实现 这个类 我们会在下一张幻灯片里定义 负责为每个新账户实例 设置这些属性 这行代码调用了account类 也就是我们马上要定义的那个类 我们先假设已经定义好了 用它来创建一个新账户实例 好 这就是我们的类定义 我像调用函数那样调用它 这就创建了一个新实例A 这个实例A的持有人属性 就是John 所以查询它的holder属性 显示的就是John 我怎么知道它叫holder呢 嗯 这正是我们接下来要看的 类定义的一部分 但抽象的概念就是 我可以访问holder 也可以用点表达式 访问任何账户的余额 这里的holder和balance 就是A对象的属性 也就是account类的一个实例 所有银行账户 都应该有相同的withdraw和deposit方法 也就是他们的行为方式是一样的 虽然他们的余额和持有人可能不同 但存款操作的含义 对任何银行账户来说都是一样的 我们在A账户上调用了存款方法 存入了15元 因为之前余额是0 所以现在余额就变成了15 接着我们取出10元 这样就从余额中扣除了10元 剩下就是5元了 所以存款和取款操作都会改变余额 并且返回操作后的余额结果 现在我们可以看到余额已经更新了 所以无论是取款还是存款 都要用到A账户来调用了 我们可以看到余额通过这个属性 来确保所有操作都能保持同步 这就是一个局部状态的例子 我说的局部是指什么呢 我是说这个余额是A对象独有的 如果不通过点表达式 我就没法直接访问他 方法可以实现任意复杂的行为逻辑 你可以根据需求写任何函数 比如说当我们执行取款操作时 他会先检查余额是否足够 如果不够就会返回insufficient funds 这个字符创造的 同时也不会更新余额信息 存款和取款都是方法 而这些方法是在对象的类里定义的 比如这里的账户类 要实现这种行为 我们需要写不少代码 所以我现在就带大家过一遍代码 让大家看清楚 然后再详细讨论 为什么这样设计 方法就是在类声明中定义的函数 所以类声明要以class这个关键词开头 然后要给类声明 当我们创建这个类的新实力时 就是这里的第一行代码 我们使用特殊的init方法来实现 这个方法总是这样写 包括这些下滑线 因为这个函数会自动被调用 当我们创建新账户时 这里我们把新账户称作self 并给它设置属性 balance和hold 其中balance总是设为0 而holder则接收传入的任何值 这个名称是从函数定义中得来的 大家注意 属性名称可以和init函数 内部使用的名称不一样 现在我们给对象设置了属性 但我们还需要给它提供行为 所以我们要定义方法 方法看起来像函数 但它们是在内部定义的 这里的函数是在内部定义的 这里的函数是在内部定义的 这里的函数是在内部定义的 这里的函数是在内部定义的 这里的函数是在内部定义的 这里的deposit方法 接受两个参数 第一个是self 而self就代表着账户类的实例对象 当我用点表达式调用deposit时 实际调用的是哪个deposit方法 大家注意 我调用deposit时只传了一个参数 但我在定义时却用了两个参数 这样就能访问这个账户的所有属性 这样就能访问这个账户的所有属性 就像是a被复制给了self 现在我们来看看 现在我们来看看 deposit方法内部的代码 它会查找balance属性 加上金额来修改余额 加上金额来修改余额 最后返回balance属性 最后返回balance属性 正是因为return语句 我们在调用adeposit时才会看到输出 我们在调用adeposit时才会看到输出 如果我们没有return语句 也是没问题的 它只会返回一个none 但余额仍然会被改变 取款方法会稍微复杂一些 它接受self这个账户实例 它接受self这个账户实例 和amount这个结果 它会检查取款金额是否大于当前余额 大家注意 每次要访问balance属性时 都需要用点表达式来访问 因为它是个属性 我可以返回任意值 也可以更新实例的属性 或者直接返回这些属性 通过这样的账户定义 我们就能实现刚才演示的用法 这样我们就可以创建一种新类型的对象了 