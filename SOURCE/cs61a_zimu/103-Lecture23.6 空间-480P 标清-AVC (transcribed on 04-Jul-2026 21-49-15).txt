空间或者说内存 是程序运行时消耗的又一种资源 所以这也是我们需要关注的一个方面 空间消耗主要来自各种值 所以长列表比短列表占用更多空间 但框架也会占用空间 所以我们需要知道 同时存在多少个框架 这是因为程序中同时有多个函数在执行 这里会涉及到一些新概念 我们需要关注的是 在计算过程中 哪些环境框架需要保留 以及哪些框架可以直接释放 简单来说呢 在任何时刻都有一组 我们称为活动环境的东西 这些活动环境里的值和框架 都会占用内存空间 而那些不在活动环境里的值和框架 它们占用的内存就可以回收利用了 Python解释器会自动回收这些空间 当它判断这些信息不再需要的时候 那到底哪些算是活动环境呢 其实很简单 活动环境就是那些当前正在执行的函数调用 也就是说 我们调用的那些函数 但它还没有执行完返回结果 还有一种情况不太常见 就是当一个函数的副环境 位于活动环境中时 当我们在一个函数内部 定义另一个函数时 所以它的副框架就不是全局框架 而是另一个框架 只要我们需要用到这个函数 就得保留它的副框架 还有副环境里的所有其他函数 还有所有其他框架 幸运的是 Python Tutor能帮我们可视化 显示哪些框架处于活动状态 这是我们定义的ifib函数 用来计算斐波那契数列的第六项 有个设置功能 我来给大家看看 选项是不显示已退出的函数 这样一来 它就会把已经返回的函数框架 都删掉 所以当我们调用fib60 显然它还没有返回结果 因为还有计算任务没完成 这就需要调用fib4 必须在fib6完成之前 先算完fib4 fib4需要调用fib2 所以这些框架在同一个时间点 都是活动的 fib2调用了fib0 fib0返回后就不再是活跃状态了 所以它就会返回了fib2 所以它就会返回了fib2 所以它就会消失 取而代之的是fib1的框架 注意 我们之前已经有了f1 还有f2和f3 然后我们创建了f4 但随后又把它清理掉了 因为我们不再需要它了 现在我们到了f5 如果继续这个过程 大家会看到 当fib4 算完fib2后 fib2框架就被移除了 然后现在我们 有fib3框架 这是计算fib4的另一个部分 会继续进行一段时间 最终fib4框架也完全消失了 这是 只剩下最初的f1框架 因为我们还没算完fib6 还有 为计算f5比5创建的 f11框架 完成工作后也会被删除 fib6 最终返回了结果 8 当代码执行完毕后 我们还可以查看使用了多少个框架 现在让我们定义一个高阶函数 countFrames 它接收一个函数f 然后定义一个带计数功能的版本 这个版本 接收参数后 会递增 开放框架的计数 同时它还会跟踪任意时刻的最大框架数量 如果当前计数 超过了f1框架的最大框架数量 超过了最大值 我们就更新最大值 然后调用f 获取返回值 最后关闭这个框架 也就是把活动框架数减1 然后返回结果 活动基数从0开始 最大基数也从0开始 我们返回这个基数版的函数 如果我们快速定义一下f1b函数 我们都知道 它会进行 大量的地归调用 但我们不清楚 在任意时刻有多少个活动框架 以及最多同时有多少个框架 我们可以通过countFrames 包装fib函数来搞清楚 然后调用fib 这样就能计算出第20个 斐波那契数 这时候fib open count 应该是0 因为 所有fib函数的调用都已经返回完毕了 但max count会告诉我们 同一时间打开的最大框架数量 答案是20 这样 就可以看出这个函数占用了多少内存空间 它不需要一次性打开6000个框架 只需要几个就够了 那么 怎么理解为什么这个数字是20呢 大家看 这就是我们计算fib的数型过程 假设这部分的工作都已经完成了 我们还需要完成剩下的工作 并且即将从这次fib的调用中返回 好 我们可以看看哪些调用还处于活动状态 就是这一个 还有这个 这些调用 还没有返回 左边的那些调用 已经执行完毕返回了 所以这些框架占用的内存 都可以回收利用 然后看这边 这些 还没有被调用 所以 我们 不用担心这一点 所以同时打开的最大框架数 就是这棵树里 最长的调用链 比如这里从5B5到4到3B5 到2到1的链 长度就是5 