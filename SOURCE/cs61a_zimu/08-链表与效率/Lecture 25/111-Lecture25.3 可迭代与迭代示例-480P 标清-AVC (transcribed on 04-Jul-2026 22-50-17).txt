我们先来看几个操作可迭代对象和迭代器的例子 我们就专注于使用内置函数和列表推导式 以及字典推导式来处理一些比较中等难度的数据处理 这些操作用一行代码最多两行就能搞定 只要你用对方法 这类操作往往是一些更大问题的核心部分 所以我就直接给大家准备了一些例子 然后我建议大家可以先暂停一下 自己尝试解决看看 然后我们再一起来逐个讲解分析 我们来看个例子 请问列表S中绝对值最小的所有元素 它们的索引是什么 假设我有这么一个列表S 绝对值最小的元素是负2和2 但我们需要的是它们的索引位置 列表索引是从0开始记的 0 1 2 3 4 5 这些就是绝对值最小的元素 对应的索引就是2和4 那么我们要怎么把这个列表作为输入 然后得到这个索引列表作为输出呢 或者以这个列表为例 按绝对值来看最小的元素在索引1的位置 我们要找出所有绝对值最小元素的索引 这样就得到一个包含0的列表 好 第一个练习 我会把这些练习都过一遍 列表中两个相邻元素的最大和是多少 这里假设列表长度大于1 来看这个列表 如果我把这两个数相加 算出来是负5 这个值不算大 把这两个加起来得到1 如果我算这两个数 结果是6 所以答案是6 这是相邻两个元素的最大和 这里有个类似的列表 但我稍微改动了负号的位置 这次呢就不是6了 最后算出来是负2 这不对 把3和负2相加 能得到正数1 所以这就是列表中相邻两个元素相加的最大值 好的 下一个 创建一个字典 把每个数字D 映射到列表S中所有以D结尾的元素 这里的D是个位数 如果我有一个整数列表 我需要建一个字典 这里的D是个位数 记录所有以E结尾的元素 这是以3结尾的 这是以4结尾的 以5结尾的元素 有这些 可以看到有两个 5和55 以8结尾的只有一个 9也只有一个 最后的问题是S中每个元素是否都有另一个相等的元素 那么给一个列表 每个元素是否都有对应的等值元素 嗯 四只出现了一次 所以这个结果是false 但这里呢 4有两次 3也有两次 而2也出现了两次 所以每个元素都至少和另一个元素相等 这样结果就会是true 如果你想暂停视频 自己尝试做做这个题目 我建议大家试试 现在我们就来一起看看 怎么解这些题 好 第一个问题 列表S中绝对值最小的那些元素 它们的索引是什么 我们就从这个例子开始 试着把它解出来 好 这是我们列表S 最小值并不是这个元素 它是这个列表的最小值 实际上 用key等于abs参数求得的S最小值也不是 那样会得到 具有最小绝对值的元素 但这不会给出最小的 绝对值数值本身 所以 如果我的目标是获取最小的绝对值数值 我需要先对每个元素调用abs函数 然后取最小值 这可以通过对S进行abs映射 来实现 这样 我就得到了 最小绝对值 我能用它 做什么呢 比如 我可以找到所有索引 当索引对应元素的绝对值等于最小绝对值时 这个索引值 就是2 这和直接找出所有绝对值符合条件的元素不一样 用列表推导式就能做到 如果我想找出S中所有满足条件的元素x 只要x的绝对值 等于最小绝对值 这样 就能同时得到 2 和-2单折 并不是我的目标 我要找的是索引值 所以 我要便利的 不是S本身 而是range 这个索引范围 这样 我们就获得了S的所有索引 然后我们相应的调整这个表达式 其实我们只需要索引 这个表达式 也需要相应调整 我们要判断s的绝对值是否等于最小绝对值 minibs 这样就能得到 2和-2单折并没有绝对值的绝对值 所以这个问题 用两行代码 就能解决 当我们使用列表推导式的时候 我也可以使用map 和filter这两个内置函数 所以我可以先定义一个过滤函数 它接收索引作为参数 然后重写这个表达式 然后告诉我 这个索引是不是需要保留的 接着 如果我们用同样的索引范围 然后用f函数来过滤 这样就差不多能得到我想要的结果了 但这 只是个filter对象 要是我真正需要的 是个列表 那就可以给它调用list函数 这样就能得到 同样的 正确结果 所以用这个表达式替换那个表达式 结果是一样的 好 我们接着看下一个问题 如何求出列表s中相邻两个元素的 最大和值 这里我们用的还是之前那个 s列表 最终结果应该是 6 解决这个问题 有两种常用的方法 第一种思路 是从 索引入手 用range 可以得到所有索引 而range1 则给出几乎所有索引 这 就是得到的范围 如果遍离这些索引 排除最后一个就行了 然后我们计算si加上si正1 这样就能算出 每对相邻元素的和 我会跳过最后一个元素 因为最后一个元素 后面没有 没有i正1了 所以这里要用缩减后的范围 好 这样我们就得到了所有相邻元素的和 如果想知道最大的和是多少 直接调用max函数就行 这又是聚合函数的一个应用场景 好 这个问题 就讲到这里 我们把这个代码复制进去 这样就完成了 不过呢 还有一种方法可以用zip函数来解决同样的问题 所以如果我用zip把列表s和它自身 组合起来 我会得到一个zip对象 那么这个zip迭代器 里包含哪些元素呢 好 我们把这些元素列出来看看 它返回的是s中的元素 和另一个s中的元素配对 但这些不是相邻元素 实际上 就是同一个元素 但如果我改用s中 除了最后一个元素之外的所有元素 来和s中的所有元素 进行zip操作 不过去掉s的 第一个元素 现在 我这有五个元素 这里也有五个 然后用zip 把它们组合起来 就能看到 所有相邻元素 对 这样我就构建了两个序列 4 3 2 3 2以及3 2 3 2 4 用zip合并后 就得到了一组配对 如果不只是列出内容 而是把每对元素相加 会怎样 对zip中的每个对计算a加b 也能得到同样的求和结果 其中的最大值是6 所以解决这个问题呢 还有另外一种方法 好 我们再来试一个例子 将每个数字d映射到s中 以d结尾的元素列表 这样得到的结果是一个字典 所以我们要使用字典推导式 这个表达式会稍微长一点 所以我特意多留了点空间 这个字典的件 是数字 值 是一个列表 这个列表 包含了s中 所有 以该数字结尾的元素 我们来看一下x 找出它的最后一位数字 然后看看 它是否等于d 这就是字典中的一个 键值对 我们希望为每个数字 都创建一个这样的键值对 用for d in range会得到所有数字 但大家注意 这个结果里没有数字2 因为这里没有以数字2结尾的元素 所以 我们需要过滤掉 一些d 如果s中 有元素 以d结尾 我们就保留这个d 这时 可以用内置的any函数 判断s中是否有元素 以d结尾 我们可以用 any函数 对s中的所有元素检查 是否存在以d结尾的 所以这里就是我们的过滤条件 我们要便利的是数字0到9 就是0 1 2 9这些 然后对于每个符合条件的数字 我们就构建对应的字典条目 现在我们来验证一下这个方法 是否可行 我们需要把s 设为刚才那个示例列表 然后 运行 我们这个表达式 大家看 运行结果 就是这样 所以 最简单的实现 就是直接返回这个结果 虽然我们解决了这个问题 但 这个表达式 看起来相当冗长 所以也许我们可以想办法 把它拆分一下 有个办法可以把整个过程分步骤来 先获取末位数字列表 那么s中 所有数字的 末位数字 是什么呢 其实 就是对s中 每个x取模式 然后 我们可以重写为 检查这个数字 是否在末位数字 列表中 这样得到的结果是一样的 只是稍微简洁了一点 不过好的只有一个复杂表达式 而不是两个了 好的 下一个问题 集合s中每个元素 是否都等于另一个不同的元素 这里有个势力集合 s 解决这个问题的一个思路是 从特定索引入手 比如 我们看索引1 1的值 是3 就在这里 我们可以构建一个新列表 排除当前元素 然后检查3 是否还出现在其他地方 s i 获取索引 i之前的 所有元素 i正1 则给出 3之后的 所有元素 如果我把这两部分 合并起来 这样我得到的 就是列表中 除了那个3之外的所有元素 我们并没有删除 所有的3 我们只是删除了 索引i处的元素 然后我可以检查 si是否在这个新列表里 这个列表不包含元素i 结果是存在的 这意味着 3在列表的其他地方出现了 好 既然我们现在有了一个表达式 可以检查索引i处的元素 是否出现在列表的其他位置 我们就可以对 所有i 进行这个检查 是不是 所有以下条件 都成立 对于每个在range中的元素 i和i的元素 都存在于 s的剩余部分里 我把这行代码完整写出来 确保没有漏掉最后的括号 这样大家就能看明白了 好 这是解决这个问题的一种方法 不过呢 我们还可以用其他思路 来看待这个问题 比如说 如果我们不想用 索引的方式来做 我们想直接关注数值本身 一个有用的表达式是 y for y in s if y等于等于3 这样就能告诉我们 列表中 有多少个3 我们可以通过计算这个列表的长度 就能知道有多少个3 或者换个思路 我们也可以统计1的个数 而不是3 然后求和 这样就能算出有多少个3 但我们并不关心具体有多少个3 我们关心的是 每个数值出现的次数 所以我们可以把这个表达式 写成 列表推导式 我觉得这个推导式 可能会挺长的 这里我们不用固定的3 而是对s中的 每个x都进行统计 这些2 代表什么意思 这说明 有两个4 还有两个3 有两个2 这样看就有点重复了 有两个3呢 有两个2 还有两个4 如果我们想确保每个数字的出现次数 都超过2次 那么一种方法 就是 检查这些值 是否都 大于1 另一种方法是 算出 所有值 找出 最小值 然后确认这个最小值 大于1 这样大家就知道 他们都大于1了 所以这其实就是表达相同想法的两种 不同写法 其实我们这里做的很简单 就是统计 x 在s中出现的次数 结果 我们发现这个功能 Python已经内置好了 我直接用scount 就能知道有多少个3 它会直接给出结果 所以我可以简化一下 对每个x都用scount 来统计 这样 就能得到 其中最小的计数 是2 既然 都大于1 我们就知道 每个元素在s中 都有对应的相同元素 所以这大概就是解决这个问题 最简单的方法了 

好 如果我们的操作正确 那所有的测试用力都应该通过 确实都通过了 看来 我们把这四个问题都解决了 