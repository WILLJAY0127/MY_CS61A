我们来做一些链表处理的练习 我来给大家演示几个练习题 大家可以先自己尝试解决 然后我们一起讨论如何解决 怎么编写一个函数来判断链表S是否从小到大排序 这个链表的元素是 而这个就不是 如何判断链表是否按绝对值从小到大排序 比如说这个链表1 3 4表示的是绝对值 但这组就不是 为什么只考虑绝对值呢 我们可以用任意件函数 对每个元素调用后判断排序 这是个经典练习 创建有序链表包含S和T的所有元素 所以如果我有有序链表S和T 怎么合并成新的有序链表 这样就基于两个链表创建了新链表 但如果不调用link 构造函数该怎么实现呢 我们可以直接操作原始的S 然后对T进行修改来创建这个链表结构 我们要怎么做呢 不是让S的剩余部分指向5 S的剩余部分应该指向包含1次5的链表 大家可以暂停一下 自己先尝试解决 接下来我们一起看看怎么解决 好 下面这个是 我们的链表类 它接受一个first值和一个rest指针 rest默认是空的 那我们怎么检查一个链表是否有序呢 比如1 3 4是有序的 但1 4 3就不是 这里我们需要用递归方法 先检查前两个元素是否有序 然后再确认剩下的所有元素也都是有序的 如果链表S是空的 或者S rest是空的 那这个链表肯定是有序的 因为它只有0个 或一个元素 这么少的元素 根本不会出现乱序 如果S的第一个元素大于第二个元素 如果索引0的元素比索引1的元素大 那它们就不是有序的 如果你不确定整个链表是否有序 也不清楚它是否无序 但你能确定前两个元素是有序的 那你只需要检查剩下的部分就行了 Fanway ordered 现在如果引入键函数 事情就更有趣了 键函数能指定我们真正要排序的值 那我们是在对原始值排序吗 还是在检查对它们取绝对之后的顺序 那么abs的值就是1 abs是3 abs是4 1 1 3 4 这三个数是有序排列的 所以如果我们使用abs作为键函数 结果就是成立的 我们这里的用户是3 我们的用法和sorted meanmax 这些函数里key参数的用法很相似 这里我们需要添加第二个参数 默认情况下 它使用的是横等函数 但如果你传入的是abs函数 那么我们就要检查这个条件 我们需要判断的是abs是否大于abs 嗯 这样就能处理绝对值的情况了 但我们希望这个函数足够通用 因为它可以运用各种减函数 所以我们只需要把abs换成key 也就是说默认情况下它会使用横等函数 但如果你想也可以换成abs函数来使用 好的我们来看下一个例子 合并操作反回将有序链表s和t合并后的有序链表 比如我合并a和b两个链表 a是1 5 b是1 合并结果就是1 1 4 5 如果s是0 是空链表 那么合并结果就是t里的所有元素 如果t是空的 但s不是 那么我觉得合并结果就应该是s里的所有元素 现在我们知道了s和t里都有元素 所以我们可以比较s first和t first 如果它们相等 它们怎么排列其实无所谓 所以我们就用小于等于 但用小于等于是什么呢 但用小于条件也是一样正确的 这里我们要新建一个链表 从最小的元素s first开始 后面跟着s剩余部分和t中所有元素的合并结果 这样结果就包含了s的全部内容 包括首元素和剩余部分 同时也包含了t的所有元素 不过如果t的首元素是最小的 那么它就应该作为结果了 结果的第一个元素 我们还需要包含s的所有元素 以及t的剩余部分也要包含进来 那么原地合并又是怎么回事呢 我们的思路是从a和b两个链表开始 我们调用了原地合并函数 这样我们就得到了a和b中所有按生序排列的元素 但我们从未调用link 实际上实现方法基本上和这个是一样的 基准情况是一样的 不过我们不再用link了 我们再调用link 我们需要修改s保持s first不变 但改变s rest的内容具体会是什么样子的呢 也就是说我们不再返回新的链表 而是直接返回s本身 但我们会修改s rest来包含原本s rest的所有内容 还要包含t中的所有元素同理 如果t的开头元素更小 那我们就返回这个元素 但会修改它的rest的部分 确保包含s的所有元素以及t剩余部分的元素 我们来看看效果 文档测试通过了 这样就初步探索了链表的一些操作 