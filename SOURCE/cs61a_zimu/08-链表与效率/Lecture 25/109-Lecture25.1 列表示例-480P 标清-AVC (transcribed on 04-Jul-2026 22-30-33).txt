好,我们来复习一下列表可变性和环境图 变量名可以指向列表 复制语句则可以改变 变量名与列表之间的指向关系 另外,列表的内容本身也是可以改变的 列表的长度可以变长或变短 即使指向这些列表的变量名没有变化 环境图的作用就是帮助我们在程序执行过程中 追踪所有这些变化 那我们现在来看一个具体例子 这里变量S指向列表2,3,T 绑定到列表5 我们会演示几种可变性操作 通过一些示例,然后展示结果 我们先从Append操作开始看 它会在列表末尾添加一个元素 那么,如果我向列表S添加T 结果会是这样的 我们从一个全局针开始 其中,S绑定到列表2,3 而T绑定到列表5 SAppendT会在S末尾添加一个指向列表5,6的元素 关键是,这个箭头指向的不是T这个变量名 这里必须包含一个值 这个值就是当我们对变量T求值时得到的结果 也就是列表5,6 现在,如果我把T重新复制为0 这意味着,T现在指向0 不再指向原来的列表5,6 但这不会影响S 现在是2,3,5,6 这是一个包含三个元素的列表 两个数字和一个子列表 而T现在指向0 好,我们重新来看一下Extend的方法 Extend的作用 是把一个列表的所有元素加到另一个列表里 所以,SExtendT会把5和6这两个元素加到S的末尾 现在,如果我把T1设为0 我会把这个特定的元素改成0 但这不会影响S里面的内容 因为S里存的只是一些数值 我们再来一遍 看看列表的加法和切片操作 列表相加会创建新的列表 而切片操作会创建一个新的列表 这些列表包含了参与相加或切片操作的原始元素 接下来,我们来看一个更详细的例子 我们将要复制为S加上包含T的列表 然后把B复制为A的一个切片 接着对A和B做一些修改 看看S和T会有什么变化 从最开始说起 S和T直接相加 会得到一个列表2、3、5、6 但这次操作有所不同 这里的意思是创建一个包含T的单元素列表 那我们就来操作一下 虽然我们不会给它起名字 而是要把这两个列表相加 这样就会创建一个新列表 包含元素2、3以及列表5、6 所以表达是S加list的结果 是一个三元素列表包含2、3和一个列表 我们把这个列表命名为A 在继续之前 我们需要考虑一下怎么处理这个无名单元素列表 我们可以不管它 或者直接删掉也行 反正后续也访问不到了 它是在计算过程中生成的 但没有变量名 好 接着看第二行 B是A的切片 从第一个元素开始到末尾 这样就生成了新列表 包含3和5、6这个子列表 现在我们可以做一些改动 A的第一个元素设成9 这是A 这个就是第一个元素 这里我们把它改成9 B的第一个元素中的 第一个元素是0 这是B列表 这是B的第一个元素 这是B 第一个元素中的 第一个元素 我们把它改成0 这实际上会影响到T 现在的结果是 S是列表23 T是列表50 A是列表29 里面包含50 而B是一个包含3和5 0的列表 由于之前的加法 和切片操作 都引用了T 所以 修改T 会影响这三个地方 于是我们看到 三个不同的0 都指向列表里的 同一个元素 好 我们重新开始 List函数会创建一个 包含现有元素的新列表 所以如果我写T等于List 我就会得到一个包含2和3的新列表 这样 就把T原来指向的内容 替换掉了 如果我把S的第一个元素 改成0 这样 就是在修改S 但不会影响T 因为我们对它 进行了复制 所以最终结果是 S 变成了 20T 还是保持 23 那 切片复制呢 我们来看看例子 假设 S是23 T是 56 当我执行S0比0 等于T这个复制操作 意思是在这个位置要插入T的所有元素 因为这个切片长度是0 而T这个列表长度是2 所以其他元素 都要向后移动 从索引3开始的切片复制 意思是 这个位置的值会发生变化 不过 因为我是用长度2的列表 替换长度1的切片 就把5和6 这两个元素 放了进去 现在T等于0这个操作 改变了这个值 但不会影响这个值 因为 S的最后一个元素就是个 数字 这些就是扩展 或修改列表的方法 列表 也可以缩小 这些操作我就不画环境图了 因为我觉得这些操作 都比较直观 但我们 还是来过一遍这些操作 怕操作会移除并返回最后一个元素 假设S是23 T是56 然后把T复制为SPOP 这样 就会移除3T 就指向这个3了 所以结果是S现在只包含2 因为我把3弹出了 而3现在成了变量T的值 这样列表 5 6就失去引用了 现在我们重新开始 假设S是2 和3 而T是5 6 那如果用remove操作呢 remove操作呢 会移除第一个匹配参数值的元素 所以如果我写T extend T 就意味着T 现在变成了 5 6 56 如果我移除 5 剩下的就是6 5 6 S列表 则完全没有变化 