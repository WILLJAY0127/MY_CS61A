我们来看一个对象系统的使用事例 这里有个要点 需要记住 在实例上查找属性时 会先找到实例属性 再找到类属性 如果实例上找不到这个属性 就到类里去找 如果类里也找不到 就到基类里去找 这样类属性就被继承了 我们来看个例子 假设我们定一个worker类 这个worker类会用sir来称呼他人 他有一个构造函数 会为实例设置一个elf属性 还有一个work方法 这个方法会返回self的问候语 加上i work 最后还有一个特殊方法 叫wrapper 这个wrapper方法 用来显示 worker实例在交互时回话中的显示效果 这个wrapper字符串的设计有点奇怪 它完全不对 self做任何操作 而是接收bourgeoisie参数 并返回他的greeting 资产阶级就是土地所有者 也就是拥有生产资料的人 所以他们不会亲自劳动 而是通过雇佣工人来积累财富 他们会说 你去工作 而我来积累财富 Jack是工人 张是资产阶级 这里我们把Jack的greeting属性设置成了mem 如果你想自己尝试解决2014年秋季期中考试2的这道题 这里给出一些提示 我们来仔细看看 具体会发生什么 当我们执行所有的考试时 有这些代码时 首先要注意的是 当我们定义类的时候 类的主体会立即执行 这样就会创建一个叫worker的新类 其中有一个类属性greeting 只为sir 这不是正式的环境图 这是个简单的示意草图 bourgeoisie类继承worker类 有自己的greeting属性pion 这里我们没有改变worker的greeting属性 这里我们没有改变worker的greeting属性 只是说 如果我们查找bourgeoisie实例的greeting 系统会优先找到这个 而不是另一个 这两行代码分别创建了worker和bourgeoisie实例 先创建worker实例 然后立即传给构造函数 从而获得elf属性 它的值取决于worker这个名称指向什么 worker就是worker类的名称 所以elf的值 就是整个worker类本身 这样能被创建出来是因为bourgeoisie类没有除方法 它从worker类继承了ignity方法 于是我们就会去查找 bourgeoisie类里有输ignity方法吗 没有确实没有 于是我们就去g类里查找 然后应用找到的dignity方法 这样就能达到同样的创建效果 所以Jack和John的elf属性是同一个 最后我们执行了jack greeting等于mem这行的创建效果 如果我们直接查找jack greeting 会在这里查找 但找不到greeting属性 所以我们会去类中查找 得到的结果就是sir 不过属性复制有个规则 它总是立即复制给点号左边的那个对象 所以jack greeting的效果就是 现在Jack就有了自己的greeting实例属性 值为mem Jek用mem来打招呼 其他worker则用sir 来打招呼 现在我们来看这些例子 worker后面加括号 会创建一个新的worker实例 这里我们调用它的work方法 它会获取新创建worker的self greeting属性 这个worker没有greeting实例属性 所以我们在类中查找 找到了sir 把sir和iwork拼接起来翻译 我们再来看看whysir i work 好 Jek是一个worker 当我直接输入jack 并计算这个表达式时 结果就是这个worker实例 但我们需要以文本行的形式显示出来 交互式绘画就是这样工作的 具体来说它会计算表达式的值 也就是这个对象的rapper字符串 那么jack的rapper字符串是什么呢 嗯 jack是worker类的一个实例 worker类中有一个特殊方法 叫rapper 每次显示对象时调用的就是这个方法 那这个方法会返回什么呢 嗯 它返回的是bourgeoisie greeting 这就是资产阶级类 它的greeting属性是pn 所以jack这里显示的就是pn 

那jack work呢jack是worker类的实例 work方法 返回self greeting加上iwork 这里的self指向jack jack的self greeting是特别设置的 值是ma'am 这里我们看到self greeting的值是ma'am 然后拼上iwork就得到了ma'ammywork 那这样work又是怎样 我们在站中查找work方法 这个实例本身没有这个方法 但bourgeoisie类本身有work方法 它的功能是打印调用worker的work方法的结果 这里的self就是指John 好的worker类的work方法就是这个 所以我们准备打印出John的greeting 加上iwork 那John的greeting呢 John的greeting是什么呢 嗯 John本身没有greeting这个实例属性 但是他的类有 那个类有个greeting属性是pn 所以打印出来就是pn iwork 等这个打印完成后 这个方法会返回i gather wealth 所以我们看到的是p 然后是iwork 接着是i gather wealth 现在显示的这个有引号 因为它是个返回值 所以我们要写一个 repeat而表达式 让它计算出相同的字符串 pn iwork没有引号 因为它是打印出来的 打印字符串时不会显示两边的引号 它只打印字符串的内容 

最后我们来看Johnelfwork 这是怎么回事呢 我们来看John的elf属性 它就是worker类 worker类里面定义了一个work函数字 所以现在调用的就是这个函数 我们在John这个实例上调用它 所以要查找John的greeting属性 结果是pn 这个我们前面已经看过 把它和iwork拼接起来 就得到了peonywork这个字符串 返回字符串的时候 会看到它带有引号 这就是能得出该字符串的表示形式 