计划是个非常实用的技术 能有效提升程序运行速度 基本原理其实很简单 就是把之前算过的结果记下来 这样就不用重复计算 当你再次需要用到这些值的时候 直接用之前的结果就行 我们再来看一个装饰器 它叫Memo 它接受一个函数 这个函数计算起来可能比较耗时 但我们也会把函数之前计算的结果 都存到缓存里 在F的记忆化版本里 我们会先看看 传入的参数是不是已经在缓存里了 如果不在缓存里 我们就把它加到缓存里去 也就是说 我们把键N和返回值F对应起来 这时候我们就需要 调用F函数 也就是最初传入的那个函数 来计算这个返回值 但一旦结果进了缓存 我们就再也不用重新计算它了 所以呢 我们直接返回缓存里已有的结果 等我切换到这个函数的记忆化版本后 就会自带自动缓存功能了 我们会维护一个字典 用来映射键和值的关系 键是函数的参数 值就是F的返回值 这个记忆化函数的行为和原函数F是一样的 只要F是纯函数 但如果是非纯函数 每次调用记忆化函数时 就不会每次都实际调F 这样的话 它的行为可能就不一样了 所以这点大家要特别注意 我们只能对纯函数做记忆化处理 才能保证行为一致 我们来看看具体效果 用Memo来优化 F的记忆化版本 会维护缓存 接收参数N 如果N不在缓存里 就把F的结果存进缓存 之后都直接从缓存返回值 最后返回这个记忆化函数 之后就会有一个循环 我们可以看到 F的记忆化函数是一样的 我们可以看到 F的记忆化函数是一样的 如果我们之前计算Fib30 需要花点时间 但如果把Fib换成它的记忆化版本 会怎样 那我们就能很快算出Fib30 实际上连F550甚至Fib300 都能很快算出来 虽然这个数字非常大 但计算起来并不难 我们只需要算出Fib298和Fib299 用记忆化版本的Fib 几乎瞬间就能维护缓存 完成 好 我们来看看这背后的原理 我重启了 Fythor 这样一来 Fib又变慢了 我们的做法是把Fib换成记数版 这样就能统计实际调用次数 我们会给这个记数版Fib 起个新名字 方便访问它的属性 现在我要把Fib改成记忆化记数版 连这个也能统计 然后调用Fib 计算瞬间就完成了 计划 Fib总共被调用了59次 其中有些命中了缓存 有些则调用了底层的原始Fib 这个例子中我们叫它记数Fib 它的调用次数是31次 那为什么是31次呢 因为我们总共传入了31个不同的参数 从30 29 28一直递减到0 所以这就是原始函数被调用的确切次数 那59次是怎么回事呢 好 我们来看看这具体是怎么回事 这就是我们的竖形递归过程 我们要记录的是对Fib的实际调用 以及调用记忆化版本Fib的次数 直接从缓存中获取结果 以及完全跳过的调用 肯定有很多情况能够完全跳过计算 因为调用次数大幅减少了 

比如调用Fib5 接着调用Fib3 再调用Fib1返回1 这算是真正的Fib函数调用 那这算是调用了Fib0吗 但下次再调用Fib一时 就直接从缓存里取之前算好的值 所以这次不算调用底层Fib函数 但会记作记忆化版本的调用 Fib2先返回 接着Fib3也返回了 接下来我们调用Fib4 它会接着调用Fib2 而Fib2会调用Fib2 Fib2已经在缓存里了 这样就能完全跳过Fib0和Fib1的调用了 这些调用压根就没发生 因为这个结构我们之前已经计算过了 所以我们直接就能找到Fib2的结果 调用Fib3的时候 也能马上得到结果 省去了地归调用的步骤 所以有了Fib2和Fib3的结果后 我们就能从Fib4返回结果 接着从Fib5返回 大家可以看到 对每个n值来说 原始Fib函数只被调用一次 而对很多n值 记忆化版本会再调用一次 来完成计算 但Fib4从不会被二次调用 Fib5也一样 