地归在链表处理中特别常见 在构建新链表的时候 或者对现有链表进行操作的时候 我们来看几个例子 范围生成是内置的 映射函数也是内置的 过滤函数也是Python内置的 但这些函数不适用于链表 因为链表是用户自定义的 所以如果我们想要相同的功能 我们就需要自己实现范围生成 映射和过滤功能 我们先回顾一下这些功能的作用 假设我有两个函数 比如square 这个函数接受x并计算平方 还有odd函数 这个函数接受x并判断是否为奇数 然后我可以用内置的map filter和range函数写个表达式 先取数字1到5 然后筛选出奇数 这样就剩下1 3 5 再对这些数应用平方函数 所以1平方得1 3平方得9 5平方得25 这些结果会被放入一个列表 因为我们刚才定义的链表类 也能实现类似的操作 但语法上会稍有不同 我们需要设计一个maplink函数 我们稍后就会实现它 这个函数会接受square函数 然后对函数的函数 经过out函数过滤的列表结果中的每个元素进行平方 这里我们从表示1到5范围的列表开始 过滤掉2和4 只留下奇数1 3和5 对这些数都进行平方 就能得到包含1 9和25的列表 好 接下来我们来实现range link函数 它需要一个起始整数 以及一个结束整数 返回包含从start 到end所有连续整数的列表 比如range link的结果就是3 4 5 我们还要写maplink函数 它会返回新列表 包含对原列表s中每个元素x应用f函数的结果 如果我们对3到6范围内的所有数求平方 就会得到3的平方 4的平方和5的平方 还要写filter link函数 它返回一个列表 只包含原列表s中的函数的结果 它会返回一个特定元素 只要对元素调用函数f返回真值 所以如果我们只过滤这个范围内的奇数 那么得到的就是3和5 4就被排除在外了 其实这些函数都是递归实现的 这是我们定义的列表类 这是平方函数 和奇数判断函数 那我们怎么生成这个范围呢 嗯 如果start大于等于end的话 那这个范围就是空的 我们就返回link empty 来表示空列表 否则这个范围里至少有一个元素 也就是其实元素 后面跟着其他元素 返回一个列表 以start作为第一个元素 那么剩下的范围是什么呢 就是从start正一开始到end的这个范围 我们怎么使用一个函数 把它映射到列表s的所有元素上 对每个元素调用函数f 然后构建结果列表 如果列表s是空列表 link empty 那就没什么需要做的了 我们可以返回s或者返回link empty 因为这两种写法其实是一回事 所以写哪个都可以 否则如果至少有一个元素 我们就对它调用f函数 然后返回一个新的列表 新列表的首元素 就是f作用在s首元素上的结果 接着我们需要对剩下的元素调用f 这可以通过递归调用map link来完成 这里我们对s rest中的所有元素 调用相同的函数f 那过滤操作又该怎么做呢 嗯 如果s是link empty 同样就没有要过滤的元素了 那我们就返回s 否则呢 我觉得需要把s剩下的部分都过滤一遍 所以我们要先得到过滤后的 剩余部分 也就是用同样的f对s rest 调用filter link得到的结果 但现在我们需要做个判断 那么s first到底要不要包含在结果里呢 嗯 这取决于我们对s first调用f后能得到什么 

如果返回的是真值 那么s first就需要保留在结果里 那其他部分怎么处理 嗯 就是对列表剩余部分进行过滤 如果返回的是假值 那么s first就不应该包含在结果中 所以直接返回过滤后的剩余部分就可以了 我们来检查一下文档测试是否都能通过 好的 测试确实通过了 所以按照预期 如果我创建一个从1到6的范围 这样我们就得到1 2 3 4 5 我们用odd函数来过滤它 然后对结果用square的函数来过滤 最终得到的结果就是1916 