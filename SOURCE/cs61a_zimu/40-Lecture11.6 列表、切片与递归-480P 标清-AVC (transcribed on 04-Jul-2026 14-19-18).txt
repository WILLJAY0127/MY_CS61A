我们来聊聊列表和地归 这两者是可以结合使用的 在地归实现中 我们经常用到列表的一个特性 叫做切片 切片的详细内容会在后续视频中讲解 但我会先给大家看个例子 使用切片的目的就是把列表分成第一个元素 以及剩下的部分 好 这是大家需要了解的语法 对于存储在S中的列表 表达式S1就是列表的切片 也就是从索引1到末尾的部分 或者简单说从索引1往后的切片 所谓切片呢 其实就是列表的一部分 所以切片表达式的结果是一个列表 长度比原列表S少1 它包含S中除了第一个元素S0以外的所有元素 而且切片操作不会改变原列表S 比如我设S等于2364 现在S就是个有四个元素的列表 如果我切片一下 得到的就是除了2以外的所有元素 也就是包含364的列表 S还是2364 所以切片就是用来获取列表剩余部分的表达方式 对于列表S 第一个元素是S0 这大家都知道 其元素是S0 其元素是S0 其元素就是从索引1往后的部分 这是理解列表内容很重要的一个角度 因为递归函数经常会对列表的剩余部分进行递归调用 因为它本身就是一个列表 所以可以用类似的方式处理 但因为它更短 所以更简单 先来看个最简单的例子 如何计算列表中各元素的和 其实Python已经内置了这个功能 正常情况下 我们不需要自己实现这个函数 不过作为练习还是很有价值的 那么我们的任务是实现一个Sumlist函数 这个函数接收一个数字列表S 然后返回这些数字的总和 如果列表是空的 那么元素的和就是0 好 我们来看具体实现 我们先来看如何对列表S中的元素求和 比如有个列表包含2 4 1 这三个数加起来是10 这里有个基准情形 就是当S的长度等于0 或者说S是空列表的时候 这种情况下 我们可以用这个函数来做练习 我们的函数的和就是0 否则我们就返回一个加法表达式的结果 这个表达式包含了对列表的递归调用 大家先花点时间 想想这个表达式应该怎么写 然后我会先讲解递归思路 再给大家看代码 好 现在揭晓递归思路 3 2 1 列表所有元素的和 其实就是把第一个元素 递归思路 和剩下元素的和相加 这个递归思路之所以有用 是因为求剩余元素的和本身 就需要递归调用 明白了这个思路 大家试试看能不能写出代码 我来倒数三秒给大家展示 3 2 1 第一个元素S0是个数字 因为我们处理的是数字列表 剩下的元素之和呢 我们可以对S从1开始的切片 调用sumlist函数 这个切片就是列表中剩下的元素之和 剩下的所有元素 这样得到的是一个数字 而这个切片本身是个列表 调用sumlist函数处理列表 会得到一个数值 这样我们就把两个数字相加 比如这里 我们先把4加1加3加起来是8 然后加上2 2加8就是10 好 现在我们用数型递归 来解个更有挑战的问题 所谓子列表就是从原列表S中 按顺序选取部分元素组成的新列表 可能一个元素都不选 也可能包含所有元素 我们的任务是实现Large函数 它接受一个正数列表S和一个非负数n 这个函数返回S中求和最大的子列表 按理说应该是整个列表 但有个限制条件 这个最大和得小于等于n 所以我们可能只需要挑选S中的部分元素 让它们的总和不超过 我们可以调用Sumlist函数 它接收列表并返回所有元素的和 我已经写好了大部分代码 只留了两个递归情况 那我们来看看这个代码 这个实现不一定是最简洁或最快速的 但这个例子正好展示了 如何用列表来解决数型递归问题 那么在这种情况下 我们有一个列表42567 我们可以用不同的n值来测试 当n等于30总和不超过3的最大子列表就是2 这个子列表里只有一个元素 要达到总和8我们就选2和6 要达到19选4267 要达到20选2567 这里的递归思路是 第一个元素要么包含在最大子列表里 要么不包含 是否要包含这个元素之后 还要决定剩余部分要包含哪些元素 具体细节就留给大家自己思考 不过我先带大家看一下代码 如果s是空列表 那么它唯一的子列表就是空列表 我们这里假设n是非负数 所以它至少为0 如果s0大于n 也就是s的第一个元素 如果它大于n 比如这个例子 4比3大 那它肯定进不了总和不超过3的子列表 所以我们就跳过它 跳过它具体指什么 其实就是对剩下的列表调用large函数 保持同样的上限值 由于这个表达式里根本没用 s0它就不会出现在最终结果里 接下来既然首元素小于或等于n 这时候我们可能用它 或者我们不选它 如果用它能得到比不用时更大的和 我们就返回这个结果 反之如果不用它反而能得到更大的和 那我们就选择跳过它 这可能吗 当然 可能 我们来看第二个例子 4是第一个元素 结果发现跳过4反而能得到和为8 而如果使用4的话 最多只能得到总和为6 那么该如何确定这一点呢 让递归来解决 两种方法都试试 看哪种方法得到的总和更大 就返回那个结果 好大家思考一下 我建议暂停视频 看看你能否算出这些表达式 它们生成的都是s的子列表 并且总和是n的子列表 不超过n 当你完成这一步后 对照一下我的答案 我数3 2 1就揭晓答案 如果要包含第一个元素s 就需要构建一个包含剩余元素子列表的长列表 这个子列表的总和是n-第一个数 最后再把第一个数加进去 为什么要从n里减去第一个数呢 道理是这样的 一旦你用了4 剩下的数字只需要凑出84等于4的总和 最后再把4加回来 但如果你不包含第一个元素 那么结果就和我们上面写的表达式一样 这是列表s剩余部分的子列表 不包含第一个元素s0 这种情况下 由于我们没有包含任何额外数字 我们仍然可以求和到n 所以这个例子应该能让大家理解 如何将地归和列表结合使用 分解长列表最常用的方法 是把它看作由首元素和剩余部分组成的 然后在地归调用中使用剩余部分 