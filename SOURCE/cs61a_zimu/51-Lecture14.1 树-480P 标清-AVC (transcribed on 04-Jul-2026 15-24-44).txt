树这种数据结构非常重要 它能帮我们表示层次关系 之前我们画过树的结构图 也讨论过在计算机科学里 树是倒过来生长的 现在我们来聊聊描述树时常用的一些术语 其实呢 我们通常会用两种不同的比喻方式 第一种是从地规的角度来描述树 这种描述下 树就像真实的树木 比如苹果树 按照这种描述方式 树包含一个根标签和一组分支 这里就是根标签 这里是一个分支 这棵树只有两个分支 另一个分支在右边这里 顺便说 每个分支本身就是一棵树 也就是说 分支也有自己的根标签 没有分支的树就叫做叶子节点 所以这里就有一个叶子节点 没有任何分支 但它也有自己的根标签 当你地规构建好一棵树之后 你可能想要描述树中的特定位置 所以我们会用加谱的相对描述方法 树结构中的每个位置都叫做节点 这些就是所有的节点 我都用圆圈 标出来了 这个是根节点 或者说顶部的根节点 每个节点都有一个标签 可以是任意值 所以树里存储的数据都在这些标签上 我之所以叫它足谱关系描述法 是因为一个节点 可以是另一个节点的负节点或子节点 比如说这个标着2的节点 就是那个标着3的根节点的子节点 你还能描述其他家庭关系 比如祖先节点 后代节点和兄弟节点 顺便提一下 人们经常通过节点位置来引用对应的标签值 比如说每个负节点的值 等于其所有子节点值的总和 这就是斐波那契数的特点 既然我们现在已经理解了数的基本概念 接下来就可以讨论 如何解决这些问题 我们就来看看如何具体实现数的抽象结构了 一棵树包含一个根标签和一个分支列表 构造函数需要接收这两个参数 每个分支本身也必须是一棵树 那么如果我要为这棵小树构建一个数据结构表示 我就会写出下面这样的代码 这棵树的根标签是3 分支包含一棵值为1的树 以及一棵包含2 1 1的树 这里我就按照刚才描述的方式使用构造函数 这样我就能根据需要随意调整表示方式 现在你可能会问 我是不是违反了抽象屏障 因为我用了列表 不会的 数本来就包含分支列表 这是抽象定义的一部分 所以这样是完全没问题的 我这里用了列表 因为这是抽象层的一部分 而不属于表示法的范畴 那我们来设计一个表示法 我可以设计任意形式的表示法 这会决定 如果我用了这个标签 我可以把这个标签的内容 和这个实际输出的内容 当我运算这个构造函数表达事实 我们得到的结果是这样的 就是列表 嵌套列表 标签值包含在其中 要构建这个结构 我们需要定义一个tree构造函数 这个函数接收一个标签和一个分支列表 默认情况下 分支列表是空的 所以默认我们会得到一个 可以直接用tree来构造这个叶子节点 我把标签放进列表里 分支已经是列表了 然后我把它们拼接起来 标签选择器会返回数 在列表表示中的第0个元素 而分支就是列表中剩下的其他元素 那么为了确保我们在使用数构造函数时 不会构建出不符合数结构的内容 我打算添加一些结构来做这个结构 我打算添加一些结构来做这个结构 第一个 检查是关于分支和分支列表的 断言这个分支必须是一棵树 这是抽象层的一部分 我的代码会确保 在构建数的过程中 遵守这个抽象规范 我还会对分支列表调用list函数 而不是简单的将标签放入列表再加分支 这样能确保 即使 传入其他类型的序列 在合并到另一个列表之前 它会被转换为列表形式 所以这两行代码 主要就是用来验证 我写的任何程序里是否都遵循了 数的定义规范 那我们怎么判断一个分支是不是数呢 要想成为一棵树 它必须是一个列表 至少要有一个元素 作为标签 如果不满足这个条件 我们就返回false值 而且还要求所有的分支 本身也都是数 所以实际上 Eastree本身 就是一个数定规函数 它会检查数中的每一个分支 如果某个分支不是数 那么整个 就不可能是数了 如果这两个检查都通过 那么它一定是数 因为它有标签 而且 这些分支本身也都是数 最后 我还要为数抽象添加一个函数 用来检查一棵树 是否只是叶子节点 它会检查分支是否为空 如果我们调用数的分支函数 得到的是一个空列表 这就表示假值 所以如果数没有分支 Not branches的结果就是真值 现在我们已经有了数据抽象的代码 那就可以开始使用了 我们可以创建只有一个 叶子节点的数 这是叶子节点吗 对 确实是 如果 我想创建 有分支的数 然后我会把这些分支 列在一个列表里 因为每棵树 都应该有一个分支列表 默认情况下 是空的 如果 我在这里只放一个单独的值 就会报错 说分支 必须是数 构建时必须确保 放进分支列表的 每个元素 本身 都得是数 如果我想让一棵 有分支的树 成为我的分支 那就给那棵树 加上分支 像这种情况 那我们就直接放一个叶子节点 这样呢我就完成了 以5为根节点的树 我还想要一个分支 它就是一个 以6为根 节点的叶子 现在我已经列出了这棵树的 所有分支 这样 整棵树就构建好了 大家注意 当我执行这个构造函数表达式时 没有任何报错 因为我构建的是一个 格式正确的树结构 不过呢 它正好是 一个具有复杂嵌套结构的 表现形式 不过 我不会这样去访问它 我会直接问 T的标签 是什么 是1 T的分支 是什么呢 就是 一组子树 如果我想获取 索引为0的那个分支 直接 这样写就可以了 这就是一棵树 我只需要问 索引0分支的标签 就能得到它的根标签 通过把分支看作列表 我并没有破坏抽象屏障 因为我在抽象定义里说过 树是有一个分支列表的 