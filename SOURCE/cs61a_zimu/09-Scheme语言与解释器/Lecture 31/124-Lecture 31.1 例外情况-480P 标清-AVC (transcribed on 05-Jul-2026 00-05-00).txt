编程语言通常都有一种机制来表明 当错误或异常事件发生时 以及处理这些错误的机制 并说明接下来该怎么做 在Python和很多其他编程语言里 这种机制就叫做异常 我们可以通过抛出异常来表示 有特殊情况发生 比如出现了错误 Python中的异常是通过race语句来触发的 就是写上race 后面跟一个会产生异常对象的表达式 这个表达式的结果 必须是一个类或者一个实例 这个类得是机异常的子类 这是Python内置的 实例必须是某个异常类的实例 异常的创建方式和其他对象 一样 所以有不同的异常类 很多都是内置的 比如类型错误和名称错误 创建特定类型错误时 只需传入描述信息的字符串 这里显示的是错误类型 这里则是具体细节 还有些类型错误不常见到 当函数接收到数量或类型错误的参数时 当未找到名称时会引发名称错误 而键盘的错误会被创建成 是在字典中找不到对应键时引发的 甚至还有递归错误 当递归调用次数过多时就会出现 这些错误类型都是内置的 这些异常会在特定情境下自动触发 但我们也可以手动触发这些异常 我们来具体看看 在Python中 如果我计算3的绝对值 这样不会出错 但如果我对Hello字符串求绝对值 就会引发类型错误 系统会提示类型错误 ABS函数的操作数类型不对 其实我们也可以主动引发类型错误 现在这个类型错误没有具体描述 但如果我加上错误信息 再引发类型错误 比如 这主意很糟糕 错误信息就会显示这句话 所以 我们可以自己写个函数 double 比如 当X字数是0 当X是字符串时 我就可以抛出一个类型错误 错误信息 可以随便怎么写 比如 只接受数字参数之类的 然后返回2乘以X 那我为什么要这样写呢 你看 2Hello在Python里是可行的 但如果我出于某种原因 想让我的double函数不接受字符串 那我对数字参数是不可行的 如果我用Double函数 使用Double函数 它能正常工作 但处理Hello时 这个就不行了 提示 Double函数只能接受数字 自动触发的其他错误类型 还有名称错误 键错误和地归错误 当我调用F 它会调用F 然后F又调用F 这样一直调用下去 最终就会触发地归错误 因为地归调用次数 它会出现地归错误 太多了 异常之所以有类型 一方面 是为了帮助描述发生了什么错误 让你看到错误信息时 能明白发生了什么 另一方面 也是为了让语言具备处理异常 并继续运行程序的能力 通常情况下 一旦出现异常 程序就会停止运行 显示错误信息和调用站回速 然后由程序员来排查问题 修改代码 再重新运行程序 程序,但有时候 你可能希望程序即使 遇到错误也能继续运行 这时候 就可以用try语句 来处理异常 try语句的基本结构 是try关键字,后面 跟着一个代码块,里面放你要执行的代码 然后配合try的是accept语句 你甚至可以设置多个accept语句 来处理不同类型的异常 而accept 后面描述的是 这个try语句要处理的 特定异常类别 你还可以给异常本身 起个名字,紧接着 是一个代码组 try语句的 工作原理是这样的,首先 会执行try代码组里的内容 如果没出错,那就正常 结束了,但如果 在执行try代码组的时候 出现了异常,而且 没有被其他方式处理掉 如果这个异常的类 继承自我们这里描述的 异常类 就会执行accept代码块 程序就不一定会停止运行 如果accept代码块 只是,你看啊 打印个错误信息,然后继续运行 那结果就正好是这样 这个名称的作用 就是给异常对象命个名 这样有时挺有用的 因为你可以把它打印出来看看 异常处理可以防止 终止,比如这么说吧 你试着去计算 1除以0 但如果你希望 在除0错误发生时 不希望程序直接停止 只是想要打印 正在处理除0错误 然后把x 复制为0 那么x就会被 绑定为0 尽管这行代码的位置在程序里 比较靠后 是在引发除0错误的 类型之后 所以这里显示的内容 就是打印调用的结果 而这个复制是因为x被设为0了 因为异常被处理了 我们根本没看到多行报错信息 那我们再来看个例子 这个invert函数 接收x 就是算1除以x 如果x是0 那这行就不会执行 对吧 因为根本执行不到那里 然后我们让它返回结果 这个程序本身没问题 我可以对2取倒数得到0.5 这里之所以会打印出来 是因为x的值是2而不是0 但如果我对0取倒数 那么就会引发异常 由于异常没有被处理 这行信息也就不会打印出来了 不过我可以写另一个函数 叫invert save invert save函数的作用 是接收x参数 然后尝试对其取倒数 它会尝试直接返回x的倒数结果 但如果出现除0错误 我们把这个错误命名为e 那我们返回e的自复串形式怎么样 在invert函数中 如果我说a等于invert2 那么a等于invert2 那么a就被定义了 但如果我说b等于invert0 那么b就根本不会被复制 但invert save就不同了 如果我说a等于invert save2 那么a就会被复制为0.5 如果我说b等于invert save2 抱歉应该是0 这样就不会报错了 b被复制为自复串 division by 0 为什么会这样呢 看代码就知道 就是这么设计的 代码的逻辑是尝试计算x的倒数 如果计算失败 因为出现除0错误 invert save就不会让错误层层上抛 导致程序崩溃 相反 它应该在这里处理错误 返回一个自复串 而如果不返回那个自复串 如果让它返回数字0 那么行为就会完全不同了 a就会被复制为1 2 b则会被复制为0 因为这就是invert save 在这种情况下返回的值 当求倒数失败时 异常机制 就是这样工作的 异常处理特别有用 当你的程序处理用户输入时 或者网上的数据格式不对 或者属性不对时 就可能出错 但你可能不想让程序处理用户输入 去直接停掉 而是希望它打印出错误 然后继续运行 或者像这里使用默认值 