Python内置了多种数据结构 比如列表、元祖、字典、集合等等 而SKIM语言主要用列表 SKIM里的列表就像我们在Python里创建的链表类 Megaskim 列表其实都是链表 Lisp语言诞生于1950年代末 那时候的计算机科学家 特别喜欢用些令人困惑的名称和缩写 所以他们发明了一个叫Cons的过程 这是个双参数过程 专门用来创建链表 光创建链表还不够 我们还得能访问第一个元素 以及链表的其余部分 当然也有对应的过程来实现这些操作 Cars过程就是用来返回SKIM链表的第一个元素 而Cutter过程 则返回了SKIM 而链表的剩余部分 还有一个内置的Nail符号 它求职后得到的就是空链表 如果要创建一个只包含数值2的长度为1的链表 你只需要写Cons2 new 这样就会构建一个包含2的Cons 其余部分就是Nail 我们通常会用这种方式来表示 斜杠就表示列表剩下的部分是空的 SKIM语言里 列表会显示成括号里用空格隔开的元素 实际上底层是链表结构 但标准的显示方式不会展示这个结构 它只会显示其中的元素 所以如果我Cons2和Nail 我就建立这个Cons单元 如果我在Cons一到这个结构上 这样就得到了这个Cons结构 而链表就会显示成这个Cons结构 其余部分则包含数字2 但在SKIM中执行这个表达式时 不会显示这种结构 而是直接显示包含1和2的列表 如果我定义X为包含1和2的列表 我查看X时就能看到整个链表 X的Card就是数字 E X的CDR则是包含2的链表 如果我使用Cons1 Cons2 我可以看到这个Cons2 Cons3 Cons4和Nail 我构建出来的结构就是这样的 但在SKIM中显示出来 就像包含1 2 3 4的列表 所有的SKIM列表都是链表结构 我们可以用Card和CDR来访问链表中的元素 但显示出来就是普通的元素列表形式 我们来看几个具体的例子 现在我来加在一边 我们可以看到一个网页版解释器 这是我们课程专用的 因为它能直观展示我创建的任何列表的链表结构 比如我定义S为Cons1 Cons2 Nail S就是一个显示为这样的列表 但如果我们用Draw功能 就能看到实际的链表结构了 Draw功能并不是所有SKIM版本都内置的 但这个版本有内置这个功能 我们就能看清楚内部结构 如果我把3Cons到S上会怎么样 这个操作是允许的 当我调用Cons时 第一个参数可以是任意值 但第二个参数必须是另一个Cons 或者Nail Cons3秒就会得到列表3 1 而把4Cons到3Cons到S上 就会得到列表4 3 1 就会得到列表4 3 1 如果我们用图形来表示 就能看到它的列表结构 那么如果我改成用4Cons3 Nail来构造S呢 大家想一想 这样会显示什么结果 建议大家暂停一下视频 先自己思考一下答案 我来倒数三个数 3 2 1 然后揭晓答案 Cons操作会创造S Cons操作会创建一个列表 把第一个参数作为首元素 第二个参数作为列表的剩余部分 因为S的长度是2 经过这个Cons操作后 列表长度就变成了3 这三个元素 依次是包含4 3的列表 接着是数字1 最后是数字2 如果我画出这个结构 显示出来是三个元素 第一个是列表 第二个是数字 第三个也是数字 如果我构建这个结构 然后查看它的Cars 我会得到列表4 3 这个列表本身也有Cars 是数字4 希望大家已经逐渐掌握要领了 我们来试一个更复杂的例子 记住S是列表1 2 如果我执行Cons会怎样 大家想一下 可以暂停视频想一想 我数3 2 1后给大家看答案 这是个包含两个1 2列表 作为元素的列表 画出来就能看到 其实是同一个元素 出现了两次 一个包含1 2和1 2两个列表的列表 接下来我们看看几个列表 相关的内置函数 所以如果S是1 2 我们就要把S的内置函数 放在Cons里面 我们可以看到 S是1 2 我可以验证它是不是一个列表 是不是一个列表 确实是列表 大家要记住 在Skin语言中 t代表帧值 3是列表吗 不是s的Cars s是列表吗 不是 因为s的Cars就是数字1 nil是列表吗 是的 它表示空列表 那我们怎么解决呢 那我们怎么区分空列表和非空列表呢? 我们可以用no这个位置来判断 我们也可以使用list函数来构建列表 list1,2,3,4就构建了一个包含1,2,3,4的列表 不过它底层其实还是列表结构 比如说我可以获取列表1,2,3,4的CDR 这样就得到了列表2,3,4 如果我有这个CDR 然后用cons把0加到前面 这样就会得到0,2,3,4 