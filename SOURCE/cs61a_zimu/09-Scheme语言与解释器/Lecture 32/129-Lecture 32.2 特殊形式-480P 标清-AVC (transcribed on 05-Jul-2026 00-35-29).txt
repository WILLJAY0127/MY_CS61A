今天我们来聊聊特殊形式 在对SKIM表达式求职的时候 我们需要先判断它是哪种表达式 之前的计算器只区分数字和调用表达式 但SKIM里还有更多类型 所以SKIM求职器会根据表达式类型 采用不同的处理方式 具体取决于遇到什么表达式符号 会绑定到当前环境里的值 自求职表达式会直接返回自身 这些包括像数字空列表这样的东西 剩下的都属于SKIM列表 我们称之为组合式 我们叫它组合式 就是说它是个格式正确的SKIM列表 是原代码的一部分 具体的列表 例子有F表达式 Lambda表达式 Define表达式 还有我们平常用的那种调用表达式 所以比如说你看到一个组合式是以F开头的 那你就知道它就是一个F表达式 简单说特殊形式就看列表的第一个元素是F Lambda还是Define 如果它不属于我们刚刚说的这几种 如果遇到Lambda或Define 我们就知道这是个调用表达式 接着 我们就对操作符进行求职 可能是内置函数或我们自定义的名称 这样就得到了应用于参数的过程 这些参数就是操作数字表达式的求职结果 

好 现在我们可以做计算器做不到的事情了 我们可以定义新过程并调用它 比如这个叫做demo的实例 它接收参数S 如果S是空列表 它就返回包含3的列表 否则就用cons构建一个新的数 对 它的第一个元素就是S的第一个元素 剩余部分就是对列表剩下的元素 递归调用demo的结果 比如我们传入1和2来调用它 这到底会做什么呢 大家先想一下 demo函数 会在这里调用Demo函数 会在这里调用Demo函数 会在这里调用Demo函数 在传入列表的最后 加上一个3 实际上它是构建了一个全新的列表 包含原来S的所有元素 也就是1和2 再加上数字3 因为基线情况会返回3 然后我们开始逐个元素进行cons操作 来构建一个完整的列表 这个列表以传入的元素开头 以3结尾 整个过程都是递归完成的 好 按计划我们要做个演示 我们来演示一下 大家右边看到的是项目4的主文件 SkinPack 它有什么功能呢 它包含了eval和apply函数 而SkinEval负责对Skin表达式进行求职 表达式可能属于不同的类型 可能是一个数字 也可能是一个组合式 我们来跟踪一下这个过程 然后启动Skin解释器 数字2会调用SkinEval函数进行求职 在全局框架中进行求职 结果就是2 如果我对2取负值 那就是对整个表达式求职 我加上兼顾号是想说明 这不是Python表达式 而是Skin表达式 所以要对2求职 就需要先找到取负操作服务 然后对数字2进行求职 接着把取负操作应用于2 就得到了2 我们还可以看个更复杂的例子 接下来我们定义demo过程 它接受参数S 第一步是检查S是否为空列表 如果是空列表 就直接返回只包含3的列表 如果不是空列表 我们先把这个窗口放大一点 我们会用S的第一个元素构建一个cons 对 然后对S的剩余部分调用demo 最后返回得到的结果 结束cons 结束demo 结束cons 结束if 结束define 这样就算完成了 注意Skin求职器实际上还没对这个过程体进行求职 它只是创建了demo这个过程而已 那么现在来看看会发生什么 当我对列表1 2调用demo时 这里面会进行很多计算工作 我们来试着追踪一下具体的执行过程 那么在最开始的时候 我调用了demo 参数是list 12 也就是说 我在对这个表达式求职 这是一个调用表达式 我们先弄清楚demo是什么意思 demo是我们之前定义的那个函数 再弄清楚list 12是什么 经过一些计算后 发现它表示列表1 2 现在我们要把这个函数 应用到参数上 这需要在新环境中求职 我们要把这个函数的形式参数s 从求职函数体其中s 我们函数的形式参数s 被绑定到了参数1和2上 在这个scheme求职期调用的环境中 这个框架之后是全局框架 好接下来会进行一些处理 所以我们要查找这些诸如此类 处理完成 每次进行递归调用时 我们再次调用了同一个函数 只不过这次的环境不同 大家注意 此时s绑定的是列表1和2 但在下一次递归调用时 s就绑定到了2 再下一次 递归调用时 s绑定到了空列表 这就是我们的 基准情形 我们在这里判断出 这是基准情形 然后 返回基准情形的结果 我们再来看 也就是 只包含 3的列表 好 这个值已经返回了 现在我们构建更大的列表 把数字2 放在最前面 记得吗 这是s绑定到2的情况 所以s的第一个元素 是2 把它和3组合 就得到2 3 接着是更后面的情况 原始递归调用里 s绑定到了1 2 这样就构建出了列表1 2 3 最终我们就得到了想要的结果 