为了实现编程语言的抽象机制 我们需要能够将符号与数值关联起来 这就要用到define定义表达式 那么我们来具体看看 在你将要构建的解释器里 这个机制是如何运作的 define的作用是将符号绑定到当前环境的第一层框架里 刚才我们说过 框架是一种类似字典的对象 但它们可以形成链条 因为每个框架都有副框架 所以当你要查找某个符号时 可能就在它的副框架里找到 define表达式就是给一个名字和一个表达式 我们会先对这个表达式求值 再把名字绑定到这个值上 放在当前框架里 比如我定义x等于plus1 2 注意这里我们会先计算plus1 2 得到结果就是3 然后x就指向了这个数字3 系统会忘掉产生3的那个表达式 它真正知道的只是这个值是3 过程定义其实是带lambda表达式的 define的简写 所以当我说要定义一个新过程时 它包含一个名称 一些形式参数和一个函数体 我的意思其实就是 我要定义一个新名称 它指向一个包含形式参数和函数体的过程 

所以在SI语言中 这两个表达式是完全等价的 这意味着在我们构建的解释器中 它们应该用同样的方式来处理 所以当我们遇到这样的定义表达式时 它就会被转换成类似这样的形式 然后我们就按照常规方法 来对lambda表达式进行求值 再把这个过程值绑定到某个名称上 

好 那么我们可以执行用户定义的过程调用 我们在本课题上 我们在本课题上 在第一个视频里已经看过演示 当我们调用用户定义过程时 会创建一个新框架 将形式参数绑定到实参值 这个框架的附集就是该过程的环境 这里的nv是过程对象的一个属性 它记录了该过程最初被定义时所在的环境 然后我们就在这个新框架开始的环境中 执行过程体 好 让我们回到本讲最开头那个演示的例子 这个demo过程会返回一个新列表 它接受一个旧列表 然后返回一个包含相同元素 并在末尾加上数字3的新列表 现在来看看创建的环境框架结构 当我定义这个函数时 我就把demo这个名字绑进来 绑定到了一个lambda过程 实例 这个实例的副环境就是全局框架 然后当我调用它的时候 这就会创建一个列表1 2 我们可以用欠套的配对结构来表示 在这个环境中创建一个新框架 形式参数会绑定到实参值上 具体来说形式参数s会绑定到这个实参值 也就是我们看到的 这个列表 而它的副环境是g 也就是刚才调用的lambda过程所在的副环境 好现在我们来查看这个过程的主体部分 我们对过程体进行求值 然后发现s不是空值 所以我们需要对s的cdr调用demo 这意味着我们又要引入一个新框架 这里s绑定到了s的cdr 也就是s去掉第一个元素后的剩余部分 再次进行递归调用 我们发现此时s绑定到了当前s的cdr上 也就是空列表0 当s绑定到列表时 我们来求值这个过程体 那么就返回3 这次调用返回2 3 而上一次返回1 2 3 不过在这个环境里 我们不记录返回值 这是程序其他地方处理的 那么这种可递归求值 并递归应用的思路其实很早就有了 这就是apply和evo 在lisp1.5文档里的原始定义 lisp1.5是sci的前身 所以这些定义很古老 但基本结构 和今天看到的差不多 只是语法不同 它负责处理像car cdr和cons 这样的基本操作 这些都是内置功能 还有一个检查数据 是否为原子性的功能 原子性数据其实就是数字 还内置了相等性判断的功能 除此之外就只有内置的引用和条件判断功能 其他功能都要基于这些来构建 除了我们熟悉的lambda 所以大家可以看到 evo会根据类型进行分发 并调用的数据来构建这些功能 然后apply又会调用evo 很有趣吧 