说到局部名称 函数的形式参数都有局部作用语 我给大家演示一下具体的含义 假设我们不用嵌套定义这种方式 而是分别写两个独立的函数 定义函数f 它会调用函数g 并传入x g 接收一个参数 然后返回这个参数a 加上 现在我来调用f 这样就会出现错误 错误提示 说全局名称y为定义 实际上这个错误想说的是 在函数g内部 也就是在计算这个 反回表达式时 找不到这个名称对应的值 我们来看这个例子 我们定义了函数f和函数g 调用f后 x被绑定为1 y被绑定为2 由于正在执行f的函数体 所以调用了g 现在进入g内部 绑定为1 所以不同的名称 对应不同的字绑定为1 然后我们看到了这个错误 为什么这会是错误呢 虽然y明明在这里 是因为这个框架不在当前环境中 这是代码 这里就是错误出现的地方 这些是我刚才展示的框架 当前环境包含这个局部框架 后面跟着全局框架 所以当我们查找名字y的时候 先在这里找 没找到 再去找 再去那里找 还是没找到 所以就会报错 说y不在全局框架里 报错了 环境就是一系列框架组成的 调用顶层函数创建的环境 就是局部框架加上全局框架 这里说的顶层函数就是没有嵌套定义的函数 你就不能引用这个y 因为这个y是那个函数的局部变量 在这个函数里是访问不到的 现在我们对比一下 之前看到的Machete例子 而other函数体却可以引用n 因为它采用了嵌套定义 