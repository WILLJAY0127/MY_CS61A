我们之前花了这么多功夫 才搞出这个通用的求和函数 我们也用它计算了自然数的和还有立方和 那收敛到尺底的那些项求和呢 好 我们也来试试 从operator模块导入明文函数 第二步 我们定义一个函数来计算收敛到百的序列的每一项 我们就叫它比项函数吧 它以k为参数 返回8 除以乘以 然后调用我们的求和函数 我们来求一下 这100万个 这项函数 我们来对付他 现在我们的总和 得到的结果是什么呢 显示3.141592 后面就不对了 这是个近似值 但已经很接近了 好了 今天再讲最后一个例子 这部分就到这里 我们把这些清掉 我来演示如何定义一个返回函数的函数 这个函数叫做 接收一个参数 这个参数是个数字 我们就叫它n 然后它会返回另一个函数 这个新函数也接受一个参数 我们叫它k 然后返回k加n 那这什么意思呢 这意味着我们可以随时创建新函数 比如我们可以创建一个add三函数 只要用数字3调用make adder 就能得到它 当我们有了add3这个函数之后 我就可以用其他参数来调用它 比如4它就会把3和4相加得到7 这个函数的特点在于 我们创建的add3函数 会记住3是它需要加上的数值 那么 它是怎么定义的呢 好 我们来定义add3 这是一个带有行参ok的函数 它会返回k加上n的结果 就像我在文档注释里说的那样 好 我来给大家讲讲这个结果 

这里有一个def语句 它里面又嵌套了另一个def语句 这个return语句是 f 这是一个add函数体的一部分 而这个return语句大家可以看到 它属于make add函数体 所以make adder返回的是一个函数 而add只是返回一个数字 有意思的是 add这个函数可以使用它自己的行参名 还有外围函数make add的行参 

所以这是一个函数作为返回值的例子 当一个函数在另一个函数内部定义时,并且新创建的函数会绑定到本地框架的一个名字上,这就是makeAdder的代码。 就是我们刚才看到的那个makeAdder是一个返回函数的函数。 在我写的文档测试中,s3就绑定到了返回的那个函数上。 这里我们有一个局部的def语句,def adder k,它可以引用外层函数里的变量名,所以n在这里被绑定,k在这里被绑定,k和n在adder的函数体内都可以使用。 如果我输入makeAdder12这样的代码,会发生什么呢? 这些空格只是用来间隔的。 大家现在看到的是一个调用表达式。 它的操作符本身也是另一个调用表达式。 而操作数就是一个数字,所以操作符就是任何一个能求值为函数的表达式。 这正是makeAdder函数返回给我们的结果,操作数是可以计算为任意值的表达式。 那么我们该如何计算这个表达式呢? 我们先来计算操作符部分,这是个调用表达式。 这意味着我们需要先计算它的操作符。 我们先调用makeAdder函数和它的操作数。 结果是1。 接着我们用1来调用makeAdder函数。 返回给我们的就是Adder函数。 这样我们就获得了可用的Adder函数。 它要在什么上面调用呢? 这里是2。 所以这个加法,气函数会把1和2相加,得到3。 我们来看看实际运行效果。 Python3,IX,Py,makeAdder函数。 1,2的结果是3。 makeAdder 2000和13,得到2001,13。 不过我也可以分两步来做。 比如我先输入makeAdder 2000。 现在我们有了一个函数,可以传13进去调用它。 

那么这些操作的意义是什么? 首先我们已经看到, 我们已经看到, 函数是一等公民。 就是说,它们可以当参数传,也能作为返回之返回。 就像编程语言里的其他值一样。 高阶函数就是能做这些事的函数。 要么把函数当参数收,要么返回一个函数。 高阶函数为什么有用呢? 因为它们能够体现通用的计算模式。 也就是说, 我们能够求和而无需关心具体求和的对象。 它们能帮我们消除程序中的重复。 因为求和这个过程,我们只需要定义一次。 最后,我们能在函数之间实现关注点分离。 我们希望每个函数都只负责一项任务。 有了高阶函数,这些任务就能变成非常通用的计算方法。 或者作为参数传入的函数,而返回的函数, 则可以更加具体化。 