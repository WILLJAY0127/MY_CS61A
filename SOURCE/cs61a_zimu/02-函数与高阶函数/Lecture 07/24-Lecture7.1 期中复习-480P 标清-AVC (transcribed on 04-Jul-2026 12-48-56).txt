我们来看几个复习用的例子 这些例子选自往年的七种考试 我最喜欢的一类问题是 Python会输出什么 也就是让你告诉我 在交互式解释器里输入代码后会看到什么结果 所以最关键的一点是要记住 print 阐述总是返回 none 同时它还会显示传入的参数 并且各参数之间用空格分开 我经常会在题目里加入一些简单的函数定义 比如平方函数 可能会从 operator 模块导入 eddy 和 mu 函数 然后我会提一些问题 比如给定一个表达式 它的求知结果是什么 或者在交互式解释器里 输入这个表达式会显示什么 所以如果你输入 5 求知 结果就是 5 交互式输出也会显示 5 如果表达式是 print 5 它的求知结果是 none 但交互式输出确实会显示数字 5 那么关于 none 这个值呢 这个值是在执行 print 5 时计算得到的 但要注意 如果 none 只是表达式的结果值 交互式输出是不会显示的 要想在这里看到 none 你需要显示的打印它 现在如果看到嵌套表达式 print print 5 调用表达式的求知过程是 先求知操作符 再求知操作数 这里求知操作数会打印出数字 5 然后返回 none 然后我们对 none 这个值 应用 print 函数 这样就会在交互输出中显示出 none 那么整个表达式最终求知结果也是 none 不过呢 这里打印 5 之后 屏幕上会显示 5 然后打印 none 就会显示 5 就是 none 所以完整输出就是 5 none 整个表达式的结果还是 none 好 那么我经常会定义一些简单的函数 然后给出包含函数调用的表达式 问你们这个函数是做什么的 那我们就来看这个例子 def delay arg print delayed def g return arg return g 这是一种非常常见的模式 就是在 def 语句里 我设定了一个 def 语句 我定义了一个函数 g 然后返回它 当我调用 delay 时 会创建一个新函数 函数 g 的副框架是 delay 框架 也就是定义 g 时当前环境的第一个框架 也就是 g 被创建或定义时的框架 这意味着每当我调用 g 时 g 的函数体就会被执行 它可以运用到函数体的定义时 可以访问其封闭作用语中的名称 也就是说在定义 g 时 绑定在 delay 帧中的那些名称 所以这里的 arg 指的就是定义 g 时 delay 函数内部的那个 arg 的值 所以如果我用 delay 作为参数来调用 delay 函数 然后调用它返回的函数 再将这个结果函数作用在 6 上 最后再调用一次 这是 怎么一回事呢 大家要记住 这整个是一个大的调用表达式 所以我们必须先对所有部分进行求值 然后才能对整个表达式求值 我们之前讲过 嵌套 def 与距离的变量 可以访问其封闭作用语 所以这个 arg 会有具体的值 它的值取决于我们调用 delay 时 传入的参数 所以说 delay 这个函数接受任意参数 然后返回一个新函数 这个新函数会返回刚才传入的参数 好 这样我们对 delay 函数的参数会反回刚才传入的参数 好 我们来看看这个 delay 的功能 有了基本概念 我们来仔细过一遍 弄清楚调用这个表达式 到底是什么意思 这个调用表达式有个复合操作符 就是从头开始直到 但不包括最后那堆括号的所有内容 所以我们需要先把这里所有带下滑线的内容求值 然后在最后把这个函数应用到无参数上 那这是什么意思呢 其实这是一个复合操作符的 这个函数的内容是什么意思呢 调用表达式 所以我们现在要处理从开始到这个括号之前的所有内容 但不包括括号本身 然后再把这个应用到数字6上 好的 那这又是什么呢 这其实也是一个调用表达式 现在来看这个复合操作符 delay delay 它将不接收任何参数 好 那么 delay delay 是什么呢 这其实是一个调用表达式 就是我在用 delay 调用它自己 那么用 delay 调用它自己会发生什么 我会得到一个无参函数 它返回的是 delay 被调用时接收的参数 而这次 delay 调用的又是 delay 所以它会返回 delay 函数本身这样 我们就得到了一个返回 delay 的函数 顺便说一下 当我在这里调用 delay 的时候 它就会打印出 delayed 所以这就是为什么会显示一个 delayed 接着 我要调用 g 它会返回 delay 的参数 它会返回 delay 的参数 它会返回 delay 的参数 它会返回 delay 的参数 然后用数字6调用这个函数 然后用数字6调用这个函数 然后用数字6调用 delay 会再次打印出 delayed 同时返回一个函数 等我调用它时就会返回6 等我调用它时就会返回6 现在我来调用它 所以整个表达式的结果是6 因为这个值不是 none 就会在交互式输出中显示出来 所以整个表达式的值是6 而我们看到的是 delayed delayed 6 而我们看到的是delayed delayed 6 那么 print delay print 4这个例子呢 我们需要走一个类似的流程 这里调用 print 函数 参数是 delay print 的返回值 参数是 delay print 的返回值 然后再用 4作为参数调用它 所以在确定要打印什么之前 我们需要先弄清楚这个调用表达式 会得出什么值 所以接下来我们会用 delay 返回 6 所以接下来我们会用 delay 6 来调用 print 函数 这就会打印出 delayed 同时返回一个能返回 print 函数的新函数 我来调用这个函数 没问题 这样我们就得到了 print 函数 然后我用 print 来输出数字 4 就能看到数字 4 显示出来 最终整个表达式的结果就是 print 的返回值也就是 none 这就是打印出来的结果 整个表达式的结果是 none 我们再来看一个例子 练习越多掌握越好 开始同样的格式 从 operator 导入 ad 和 mo 定义 square 函数 不过这次有个新函数 定义 pirate 函数 参数是 arch 它会打印 mat 然后在内部定义 plunder 函数 并返回它 然后 pirate 函数 返回 plunder 函数 这里的基本结构是一样的 但这个函数的行为是完全不同的 那么我们来看一个例子 关键不在于名字不同 而是内部 def 语句定义的行为 这个内部 def 语句的意思是 只要我调用 plunder 函数 就会直接返回传入的参数 这没有任何延迟执行的过程 简单来说 当我用某个值调用 plunder 就会立刻返回那个值本身 plunder 的结果就是 2 实际上 由于 plunder 函数使用了 arg 这个参数名 我们永远无法在外部作用域中 访问到这个 arg 参数 因为变量查找 总是先从当前环境的最内层作用域开始 所以当我们查找 arg 这个变量时 就会直接在当前作用域找到 完全不用去外层作用域查找 好 那如果我们计算 p3 正1 会得到什么结果呢 我们需要把这个表达式的计算结果 加上 1 我们来看一下 这里我们有 p3 这个表达式 也就是要对数字 4 应用某个函数 为了弄清楚这个函数是什么 我们要对 square 调用某个函数 而这个函数 就是调用 pirate 返回的结果 所以首先要搞清楚 pirate 返回什么 就像我刚才说的 名称的值 就是当前环境里最早能找到这个名称的 真绑定的那个值 具体来说 arg 这个名称 永远指向 plunder 函数真理 绑定的值 所以每次 调用 plunder 时 返回的 就是我们传进去的那个值 我们永远不会用到外层 这个 arg 所以 pirate 函数 本质上就是一直返回那个恒等函数 也就是返回任何传入参数的函数 所以如果我调用 pirate 3 我会得到一个函数 它的返回值就是我传入的值 所以 如果我传入 square 那么整个表达式 就会得到 square 函数 那么用 square 处理 4 会得到什么 我得到 16 我把它加上 1 就得到了 17 所以这就是恒等函数 这整个表达式的结果是 square 函数 而 square 等于 16 每次调用 pirate 函数时 我都会打印出 mati 所以这里会先显示 mati 然后得到 17 不过整个表达式的最终结果 还是 17 好 那 pirate pirate pirate 57 会怎样呢 我们来看一下 首先 我们要对 7 应用某个函数 不管这个函数是什么 我们需要先对 5 应用 另一个函数来得到它 那我们要应用的到底是什么呢 首先我们需要 我们接下来要调用 pirate 函数 但我们要把它应用到什么上呢 就是我们调用 pirate 得到的那个结果 所以这是我首先要计算的 最内层表达式 调用 pirate 会返回恒等函数 当然也会打印出 mati 因为我每次调用 pirate 交互式输出里都会显示 mits 所以这里我们就得到了一个恒等函数 我再调用一次 pirate 这样就又得到一个恒等函数 同时再输出一次 mpt 然后用 5 作为参数调用它 这样就返回了 5 所以目前我们看到两次 mits 输出 然后这个表达式 求知得到数字 5 我尝试用 7 作为参数调用 5 但 5 不是可以调用的对象 我们只能调用函数 不能调用整数 所以才会出现错误 所以整个表达式没有值 因为它求值得到的是 5 好 最后一个例子 就是我个人特别喜欢的一个例子 就是关于 horse mask 的例子 这里定义 horse mask 为一个函数 它先将 horse 设为 mask 然后定义 mask of horse 函数来返回 horse 最后返回的是用 mask 作为参数调用 horse 的结果 那 mask 到底是什么呢 mask 是个函数 接收 horse 作为参数 然后用 2 来调用这个 horse 函数 然后我们来求知 horse 这个表达式 所以这里的重点在于 我们要处理 horse 这个词 在不同环境中的含义 mask 在不同的环境里 意思不一样 我们得记住当前处在哪个位置 每次返回之后要回到哪里 确保每一步调用的都是正确的函数 现在假设 horse 绑定到了一个也叫 horse 的函数 这个函数接受 mask 作为参数 同时 mask 也绑定到这里 创建了 lambda 函数 它接受 horse 作为参数 这两个函数的附集都是全矩整 现在我们用 mask 来调用 horse 也就是说我们用这个参数来调用这个函数 所以调用 horse 函数时 我们会创建一个新针 这里我们要复制函数的名称 同时还要给这个新针加上标签 然后把函数的形参和传入的实参进行绑定 这样我们就把 mask 这个名字 也就是从这里来的 赋予给传入的参数 我们传入的是全矩针里的 mask 值 就是这个 lambda 函数 所以这个值被绑定到了 mask 上 现在 mask 在当前环境的第一针里 就代表这个 lambda 函数 接下来我们执行函数体 也就是要确定 mask 的含义 然后把 horse 这个名字绑定给它 复制语句总是在当前针中修改某些内容 也就是我们当前所在的这个针 所以要把 horse 绑定到 mask 当前的值上 

接着我们要定义 mask 函数 记住使用 def 定义 mask 函数包含两个不同的步骤 它会创建一个新函数 其复集就是当前这个针 然后第二步是 它会把 mask 这个名字重新绑定到这个函数上 而且这样的绑定也总是在当前针里完成 这样我们就创建了一个叫做 mask 的新函数 它的参数是 horse 复集设为 f1 然后我们就创建了一个叫做 mask 的新函数 它就会绑定到这个函数上 因为我们现在就在 f1 针里 然后我们在当前针里 把 mask 这个名字绑定到这个函数 所以 mask 现在指向的就是这个 mask 函数 就是我们刚才在这里定义的那个函数 同一个名称 不能在同一个框架里代表两个不同的东西 所以我们需要清除旧的绑定关系 现在它已经不存在了 到目前为止 除了 return 语句外 我们已经完成了所有操作 这个语句是我们用的语句 就是 return horse 为了执行这一步 我们需要搞清楚 horse 快计算出什么结果 所以我们需要弄清楚 当我们用 mask 参数调用 horse 函数时 会发生什么 那么到底是哪个 horse 函数呢 那么具体是哪个 mask 参数呢 大家要记得 我们现在处于这个框架里 就是 horse 框架 我们正在对 horse 函数进行求知 所以就在 horse 框架里 那我们就来分析一下 现在 horse 指的就是这个 lambda 函数 mask 则是指这个 mask 函数 这样我们就调用这个 lambda 函数 但这里传入的是个参数 调用 lambda 函数的意思就是 这时我们会引入一个新的针 这个针的名字是 lambda 它的负针是全局针 因为这个 lambda 函数的负针是全局针 我们把形式参数 horse 绑定到传入的实参上 所以在这个 lambda 函数体里 用到 horse 时 指的就是这个绑定值 这就是我们传入的 lambda 函数 那么执行它的函数体会发生什么呢 我们就直接返回 horse 的结果 现在该用哪个 horse 函数呢 好 现在我们在这个当前针里 这里说的 horse 指的是这个函数 所以我们要用参数2来调用这个函数 这就意味着需要创建一个新的针 mask 针的负极是 f1 因为每次调用 mask 函数的时候 f1就是它的负极 它的参数 horse 被绑定为2 就像我刚才说的 那么这个 mask 函数是干什么的呢 mask 函数就是返回 horse 这个值 horse 的值是2 所以返回2 现在我们要返回到哪里去呢 我们刚才是从这个 lambda 函数调用的 它当时正想要算出 horse 的结果并返回 我们刚算出来在那个上下文里 horse 的结果就是数字2 所以现在我们需要返回这个值 我们要把这个数字2返回到哪里呢 我们正在求值 horse mask 的函数体 然后我们想要返回得到的结果 就是调用 horse 函数传入 mask 时得到的值 现在我们知道这个值就是数字2 所以我们可以返回这个值 这样求值过程就完成了 我们发现这样调用 horse mask 不会出错 它会生成这个环境 最终求值结果就是数字2 