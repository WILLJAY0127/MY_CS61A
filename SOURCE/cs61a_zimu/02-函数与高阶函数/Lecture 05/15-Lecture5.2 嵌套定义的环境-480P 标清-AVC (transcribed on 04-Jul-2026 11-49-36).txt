今天我们来聊聊嵌套定义的环境模型 上次课我给大家看了一个例子 我可以创建一个make adder函数 它接受一个行参 它内部又定义了一个make it dark函数 返回k加n的结果 然后make adder函数返回这个add函数 

也就是它内部定义的那个函数 所以make adder的作用就是创建并返回一个新函数 它本身并不直接做加法运算 那如果我把这个函数加载到python里 会发生什么呢 好 我可以动态创建新函数 我可以调用make adder来创建一个add三函数 现在我就有了一个能给任何数加三的函数 我可以给4加3 我可以给5加3 我可以给6加3 有意思的是 这个add三函数 内部似乎包含了要加的那个数字3 那函数里面怎么会包含数据呢 好 我们来看看环境图 这样我们就能完全理解其中的原理了 好 我们来看这个例子 我定义了一个make adder函数 它不会马上执行函数题 只是定义了一个新函数 然后我用参数3来调用make adder 这会创建一个新框架 

我定义了一个新函数 然后我把这个函数定义到参数3 接下来我定义add函数 这是make adder函数体内的第一步操作 当我定义add函数时 我会创建一个新的函数值 然后把函数名add绑定到当前框架中的这个值 注意 这里说的当前框架不是全局框架 所以make adder框架及其环境都能访问这个add函数 在这个情况下 我们还能够把这个函数的值值 从全局框架引用到我们的调用框架 但是当我们执行返回时 情况就完全不同了 返回语句的作用就是把信息从局部框架带回来 返回到我们调用时的那个框架 也就是我们最开始调用这个函数时的框架 这样我们就回到了一个特定的执行点 我们刚才正在执行这个调用表达式 所以当我们返回时 我们就要把这个框架的位置 也就是要把结果绑定到add3这个名称的时刻 那么我们要绑定给add3的返回值是什么呢 大家看 就是这个 这个返回值是怎么来的呢 就是我们在当前环境中找到add的含义后返回的结果 而这个值其实就是add函数本身 所以在下一步 我们就直接返回到之前的位置 也就是回到全局框架中 当我们执行完makeadd这个调用后 它返回的值就是那个add函数 我通过复值把这个函数绑定到add3这个名字 大家记住 同一个函数可以有多个名字 好 现在我们有了add3 这个函数的作用是给数值加3 它是怎么做到的呢 它的函数体就是k加n 好 我们用4来调用add3 看看结果 这样我们就创建了一个新框架 OK被绑定为4 它的负框架其实就是这里的f1 所以虽然我们已经从makeadd返回 我们依然能访问到它的内容 因为我们创建的这些框架 负极都是makeadd框架 这个框架里就包含了变量n 稍后我会详细讲解这个问题 但这里的框架是n的框架 这里的情况是 我们在一个环境中计算k加n 这个环境从f2开始 然后是f1 最后是全局框架 我们在f2里找到了k等于4 在f1里找到了n等于3 这样我们就得到了数字7 这个值被绑定到了result变量上 这样我们就完成了 那么这里展示的 就是我刚才例子的环境图 我们来稍微讨论一下这个图 这里我们刚才的环境图是这个图 这里我们看到有一个嵌套的df语句 还有一个我们之前没见过的新情况 一个函数的负框架不是全局框架 实际情况是 函数的负框架会被设置为它创建时的当前框架 那么这个add函数到底是怎么创建出来的呢 这其实是makeadd函数体的一部分 所以当add函数被创建时 makeadd就是当前环境的框架 所以它的负框架才是f1 那么当负框架是f1时 我们调用add函数 会把函数名复制到新框架里 再把行参也复制下来 和食餐值进行绑定 同时还会把函数的负框架设为新框架的负框架 那么我们为什么要给函数设置负框架呢 其实是为了当我们调用这些函数时 能够为框架设置正确的负框架 那框架为什么需要负框架呢 这样就能告诉我们如何查找当前环境 所以当我们实际计算k加n时 当前环境首先从adder框架开始 然后是其负框架f1 接着是f1的负框架 也就是全局框架 我把它简称为g 这样就能把所有内容都显示在屏幕上 把它当成全局框架g来查找也是可以的 好 现在我们有了一个三层框架的环境 如果我们要在这个环境中查找变量k 先在这里找k是4就找到了 如果要查找n的话 先看当前环境的第一个框架 但那里没有n 所以接下来我们要看第二个框架 也就是第一个框架的第一个框架 就是n的第二个框架的负框架 这样n就被复制为3了 这样我们就能得到n k加n就是4加3等于7 这样就完成了 好 那么关键要点是什么呢 每个用户定义的函数都有负框架 在我们之前看过的例子里 负框架都是全局框架 但遇到嵌套的df语句时 情况就不一样了 内部的def语句会继承外层函数调用的框架 一个函数的负框架就是它 被定义或创建时所在的框架 每个局部框架都有一个负框架 就像我们之前看到的 而框架的负极就是被调用函数的负框架 这样就能解释我刚才的做法了 当我创建这个addr框架时 我复制了它的名称 也复制了它的负框架标记 并把它的行参绑定到对应的食餐上 所以大家看 正确创建这个addr函数框架 所需的所有信息都保存在这个函数值里面 所以呢 只要你知道调用的是哪个函数 就知道该怎么正确画出它的框架 

这里给大家一个小指南 教大家怎么画环境图 这样不用python tutor也能自己画出来 这个方法真的很实用 因为这样哪怕是复杂的例子 也能搞懂了 当我们定义一个函数的时候 总会创建出一个类似这样的函数值 它的负框架就是当前这个框架 所以这就是为什么我们在创建addr函数时 我们正好处在make adder框架里 于是我们就记下负框架是f1 f1就是这个框架的标签 现在我们不会直接写make adder 因为可能会有多个不同的框架都叫make adder 但每个框架都会有一个唯一的标签 所以我们要用标签来区分 我们会把名称绑定到当前框架的函数值上 这就是当addr这个名称指向该函数的情况 

当函数被调用时 我们会创建一个新的框架 所以我们会添加一个以被调函数名命名的局部框架 我们将函数的负框架复制到局部框架中 然后在该局部框架中将行参与实参绑定 最后我们就可以在以该局部框架为起点的环境中执行函数体 如果我们需要在该环境中查找名称 我们会沿着负框架一直向上查找 直到到达全局框架 无论我们首先在哪个层级找到该名称 这就是我们要用的那个值 