我们来看一个包含λ表达式和λ函数的环境图例子。 

λ函数的负帧就是对应λ表达式被求值时的当前帧。 好,我们来看个例子,我们先将a设为1,然后定义一个新函数f, 它接受一个函数g,并将a负值为2,这是f帧内的局部负值,然后它返回一个函数。 这里我们通过λ表达式,λyag创建了一个函数,这里我们可以看到g的具体用法,就在λ函数体内部被调用。 现在我们调用f,它接受一个函数参数,我们这里创建了一个这个函数。 会将a与y的值相加,调用f实际上会返回另一个函数,这意味着我们可以用a作为参数来调用它。 这里的关键问题是,每种情况下使用的到底是哪个a? 这个λyag表达式是在全局帧中求值的,我知道是因为它不在任何函数体内。 所有完全不缩进的代码都在全局帧里求值。 所以我知道,这会创建一个负帧是全局帧的函数。 所以变量a,指向的就是全局的a等于1,这个表达式的后面部分,也就是我们在a上调用返回值的部分,也属于全局表达式,不在函数体内。 所以这里的a也是全局帧。 所以我们来看全局变量a。 相比之下,f函数体内的λ表达式,负帧就是f函数的帧。 在那个帧里,变量a会被负值为2。 这个λ表达式会在那个帧里求值,所以它的负帧就是f1。 所以,当它被调用的时候,a的值就是2。 现在我们来看环境图。 被负值为1。 f绑定到了f函数上。 然后我们来对这个表达式求值。 要这么做的话,我们需要先求它的操作符,看看要调用哪个函数。 这个操作符本身是个调用表达式,我们需要对f求值。 好,所以我们要调用这个函数。 然后,对操作数字表达式进行求值。 这会创建一个新函数。 现在看第五行,这里有个λ函数,注意。 它的负帧是全局帧,因为这个λ是在全局表达式中定义的,不在任何函数体内。 虽然这个函数的用法是它会被立刻作为参数传给f,所以只能在f一帧内部访问它。 这样,f一帧就有了g这个行参,这里的g是行参,但它关联的λ函数。 是在全局环境中创造的。 是在全局环境中创造的。 这意味着调用它的时候,它将使用全局a,而不是局部变量a。 

被负值为2后,我们返回一个函数。 虽然这个函数稍后才会被调用,但它所属的负环境是f一,因为函数在被创建时就确定了负作用欲。 先创建,再返回。 那么这个函数最终返回到哪里了呢? 嗯,这就是这个表达式的值。 所以现在我们知道要对a调用哪个函数了? 接着要查一下a的值。 结果是1,现在就可以用1来调用这个lambda函数了。 具体操作就是,为第四行的lambda创建一个lambda针。 负针设为f一,行参y绑定时参一,这个函数会做什么呢? 嗯,我们需要看一下函数体里的内容,表达式是a。 反过来,这个函数是a乘以g。 那a的值是多少? 在当前针里找a没找到,所以往上找负针,发现a是2。 那么g乘以y的结果是多少呢? 嗯,我们需要先搞清楚g是什么。 这里没有g,所以我们在负针中查找。 这里有g,我们要用数字1来调用g函数。 这就导致产生了这个额外的针。 这个针用于计算a加y。 当我查找a时,发现它不在这里。 所以我把这个针调成g。 我往负极针里找 也就是全局针 这里有个1 Y是1 我把额和Y相加 得到2 这个2就是G的值 A我们已经查过是2 所以最终返回值是4 