好 那么可变性这个概念 我们来谈谈什么是相同的含义 以及什么是改变的含义 在早期的编程概念里 我们是不修改对象的 那个时候符合对象 就是它各个部分的总和 有理数除了分子是多少 分母是多少之外 就没有其他含义了 但这种整体就是各部分加总的观点 但是各部分加起来 在有变化时就不成立了 像列表这样的复合数据对象 除了由元素组成外 还有自己的身份标识 所以一个列表可能还是原来那个列表 即便我们改了里面的内容 如果我让A指向10 然后让B也指向 同样的内容 我们就会看到 A是同一个列表A 那会发生什么呢 A会变成一个包含10的列表 然后B也指向了这个列表 A等于B 如果我对A追加20 A仍然等于B 因为A是10 20 B也是10 20 B和A绑定到了同一个列表 尽管我们修改了它的内容 也可能有两个列表 内容碰巧相同 但它们的对象标识不同 它们不是同一个对象 只是值可能相等 所以如果我说A是列表10 而B是另一个列表也包含10 A等于B这个判断是成立的 到目前为止还没什么太大区别 但如果我向B追加20 现在还是10这个列表 B变成了10 20这样的列表 这两个列表就不再相等了 之前它们确实是相等的 我们从未对A或B进行过重新复制 所以并没有A等于这个 B等于那样的复制语句 但这只是因为它们之前的内容正好一样 所以才会相等 但它们从来就不是同一个东西 所以当其中一个改变 另一个不会跟着变 那么怎么判断两个东西是否相同呢 有一种称为标识运算符的工具 看起来就像一样 看起来就像英文里的is这个词 而is的反义词就是is not 所以如果两个表达式求值得出同一个对象 那它们就是相同的 如果两个表达式求值后结果相同 就认为它们是相等的 所以任何一个包含10的列表 都等于其他包含10的列表 但它们可能是 也可能不是同一个列表 完全相同的对象值肯定相等 但反过来就不一定了 

所以10永远等于10 这个没问题 

如果A是10 而B是一个包含10的列表 那么A和B是相等的 但它们不是同一个列表 当两个对象不是同一个东西时 要改动 一个不会影响到另一个 

但C绑定到B 意味着C其实就是B 只是同一个东西的两个不同称呼 所以如果从C中弹出某个元素 其实也就从B中弹出了这个元素 但A没有任何变化 这里有个重点需要注意 要特别注意的是 可变默认参数存在很大风险 所以当你们学到可变性这个概念时 就一定要了解它带来的风险 默认参数值 在我们定义一个函数时 函数值中的某些部分 不会在每次调用函数时 都重新生成 那么假设我定义了一个函数F 它接受一个序列S作为参数 默认情况下接受空序列 它的作用是向这个序列追加5 然后返回追加5 后序列S的长度 当你调用F时 它会使用默认参数值 也就是一个空列表 向列表里加上5 然后返回这个包含5的列表长度 这时候得到1 再调用一次就得到2 再调用一次得到3 

这是因为每次调用 S都会指向同一个值 默认参数就是这样工作的 要是这个默认参数是可变的 然后在函数里修改了它 这个更改会一直保留到 下次调用函数时 而且你得到的 还是那个相同的默认参数值 所以看似简单的东西 实际上会有很奇怪的行为 每次调用函数 都会得到不一样的结果 这绝对是个需要注意的地方 第一次调用时 S绑定的是一个空列表 然后这个列表被修改 添加了一个3 下次我再调用F时 S绑定的还是那个相同的列表 然后列表又被修改 加上了第三个3 第三次调用的时候 得到的就是第三个3 看结果就是这样 所以每次调用都会返回不同的值 第一次是1接着是2然后是3 