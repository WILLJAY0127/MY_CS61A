那么可变值可以用来定义可变函数 我们来看一个行为会随时间变化的函数 这正是它被称为可变函数的原因 每次调用这个函数 即使输入相同的参数 得到的结果也会不同 假如我们要创建一个代表银行账户的函数 初始余额设为100美元 然后我希望能够从这个账户取款 只需传入取款金额来调用这个函数 那么这里的银行账户具体指的是什么呢 实际上代表银行账户的就是这个取款函数本身 我们来取出25美元 还剩多少钱 75美元 这里的参数就是取款金额 返回值就是剩余额 要是我再取25美元 那余额就应该是50美元 同样金额的第二次取款 返回值却不同了 这就是可变函数 而不是纯函数 因为调用函数时的副作用 会让某些东西发生改变 withdraw函数的行为特征 就在不断的改变 这个withdraw函数在某种程度上代表了一种信息 也就是我们所说的状态 就是我们取款账户里的余额 我们把这种状态成为函数的局部状态 因为我并没有把它形成为函数的局部状态 我并没有把它形成为函数的局部状态 我并没有把它形成为函数的局部状态 它其实就是函数的一部分 所以也就定义了函数的行为 如果我要取60元 但账户里只剩50元 我可能会得到一个返回值 比如你的银行账户里没有这么多钱可以取 但如果取15美元就没问题 就会从50美元的余额里扣除 那这个余额存在哪里呢 它其实就在withdraw函数里 这个函数是用高阶函数 make withdraw list创建的 它需要创建的 它需要传入初始余额作为参数 余额实际上存储在withdraw函数 副框架引用的一个列表里 也就是make withdraw list frame这个框架 由于这是个可变列表 每次调用withdraw函数时 我们都能修改它里面存储的值 这个例子想要说明的是 我们创建了一个函数 它把一些行为和数据绑定在了一起 但和之前的例子不一样 每次调用withdraw时 数据都会发生变化 这其实就是编程语言支持可变值 带来的必然结果 也就是说行为也会随着时间的推移而改变 那我们怎么实现持久性的本地状态呢 就是说有一个与withdraw函数关联的余额 这个余额会在多次调用中 一直保持在最后的时间中 那么如果我多次调用这个函数 我实际上是在累积信息 我是通过可变值来实现的 我们来看看具体实现 我定义makewithdraw的函数 它接受一个初始余额 创建一个列表 里面存放着这个余额 因为b是一个列表 所以它的内容是可以改变的 实际上 即使在withdraw函数内部 我们也能改变b的内容 只要一个函数能够引用到这个列表 它就能修改这个列表 所以withdraw这个函数会接收取款金额 然后检查是否有足够的钱 它检查的是哪里呢 实际上它不会检查余额本身 那个余额是保持不变的 也就是我们最初设定的余额 它会去检查可变列表b中 所引领位置的元素 如果金额太大 那就结束操作 但如果金额没超现 我就会给这个值 负一个新的值 来减少它 这样就能返回新的余额了 makeWithdrawList会返回这个withdraw函数 它的负框架就是这个 makeWithdrawListFrame 框架里有个b 指向包含更新后余额的可变列表 这个余额最初就是传入的初始值 但之后会根据取款金额来减少 如果我连续两次调用withdraw 我用的都是b0更新后的新值 也就是前一次调用更新后的值 所以这个名称是在withdraw定义之外绑定的 但通过元素复值 我们可以修改这个列表 我们就能得到这样一个环境图 调用makeWithdrawList传入初始余额100 它定义了一个变量b 这个b是一个列表 初始值存放的是100 然后定义了一个withdraw函数 这个函数的负极是f1 也就是makeWithdrawList的真 这样withdraw函数是f1的负极 然后用这个b的值 把这个b的值移到函数体 就能引用这个b 也能修改b的值 withdraw函数被返回后 绑定到了withdraw这个名称 然后用金额25来调用它 它会检查确认25不大于列表原来的数值 也就是100 然后将列表内容重新复制为100-25 得到75 withdraw不会重新分配 复集框架中的任何名称 像b复制这样的语句总是分配给本地框架 这其实并不完全正确 在Python中可以通过non-local和global语句 来分配到非本地或全局框架 如果你想了解更多 可以自己查阅一下 不过 我们这里不会用到那些方法 实际上 我们是通过引用b来实现的 这个b始终是同一个变量 它始终指向同一个列表 只是列表里面的内容会随着时间变了 所以 withdraw函数并不会改变这些绑定关系 它改变的是b本身的值 这样一来 我们就利用了可变值的特性 这其实是Python中自带的一种功能 用来实现可变函数 