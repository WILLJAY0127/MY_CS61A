今天我们来聊聊字典的迭代操作 我们可以从多个角度来查看字典的内容 它们虽然是不同的对象 但都支持迭代操作 所谓可迭代值 就是能传给内置iter函数 来生成迭代器的任何值 当我们调用iter函数时 它会返回一个迭代器 然后这个迭代器又可以传给内置next函数来使用 所有的迭代器都是可变对象 当我们对其调用next的方法时 它们的状态就会改变 指向下一个元素 字典本身以及它的keys,values和items方法返回的视图 都是可迭代对象 这些都可以传给iter函数 生成的迭代器再传给next函数 字典中条目的顺序就是它们被添加的顺序 从Python 3.6开始 新版本都是这样的 但在老版本Python里 字典条目的顺序是随机的 所以字典以前被称为建制队的无序集合 但现在它们是有序的了 不过写代码的时候 大家要记住一点 根据大家用的Python版本来运行程序 字典的顺序可能不可靠 不过在示例中 我会假设 我们用的字典是一种 我用的是新版本Python 所以字典最终会按照构建顺序来排序 如果我有三个建制队的字典 一对应一 两对应二 三对应三 然后我再添加第四个建制队 给建Zero复值为0 那么便利件的时候应该会依次得到 1 2 3 最后是Zero 因为Zero是最后一个添加的件 那我们来看看具体怎么便利这些建制队的顺序集合 我们来看看具体的建制队的顺序集合 通过调用iter函数来获取件的迭代器 如果不写D keys 而是直接用D 效果是一样的 便利的仍然是件 这个K就是件的迭代器 也就是说 如果我要获取下一个值 我会依次得到1 2 3 最后是0 如果我想便利字典的值 我可以调用values方法 这个可迭代对之后 这个对象会按字典构造顺序返回所有值 所以先返回1 然后是2 3 最后是0 最后我可以获取字典的items 这些是以元组形式存储的件值 对 第一个件值对中件是1 我们可以同时获取件和值 以及由件值队组成的二元组 接下来是23 最后是0 所以便利字典内容 其实有很多种方法 Python的设计思路是 我们只有一个字典对象 D通过keys values或items方法 我们可以查看它的不同视角 并且每种视角都能获得对应的迭代器 这里有个有趣的情况需要注意 假如我们有个字典 并且已经为件创建了一个迭代器 正在便利这些件的时候 这时候我去修复它 我们可以看到它是在修改字典 现在的情况是迭代器已经失效了 所以我会看到这个错误 字典在迭代过程中大小发生了变化 意思是说我不能再使用K这个迭代器了 因为我在使用K期间字典被修改了 现在字典变成这样了 如果我只是在迭代件的时候修改某个件的值 比如现在是K 1 2 然后我把0改成5 我可以继续迭代下去 因为我没有改变字典的形状 也就是说字典结构本身没有改变 我只是修改了某个件的值而已 但如果字典的大小发生了变化 那我就不能再使用之前创建的任何迭代器了 而是需要重新创建新的迭代器 这个规则对keys,values和items都同样适用 