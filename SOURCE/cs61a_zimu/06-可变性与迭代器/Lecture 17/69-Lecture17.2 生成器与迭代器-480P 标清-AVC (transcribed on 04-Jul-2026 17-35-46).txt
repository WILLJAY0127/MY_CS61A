生成器函数会返回生成器对象 但在执行时经常要处理各种迭代器 这种情况非常普遍 所以在Python 3.3版本 也就是2012年发布的 就新增了一个yield from uj 它能让生成器直接输出 另一个迭代器的所有元素 也就是说yield from uj 能返回迭代器或可迭代对象的全部内容 举个例子 假设我要定义一个a then b函数 这个函数接受两个可迭代对象或者迭代器 然后按顺序返回第一个对象的所有元素 接着是第二个的 一种写法是用for x对a执行yield x 然后对b也用for x和yield x 但有个更简单的方法 我可以直接写yield from a 然后yield from b 这两种写法完全等价 所以你可以把yield from 看作for g的简写形式 也就是便利a中的所有元素并yield 但它能让你避免为序列中的每个元素命名 这样代码就更简洁了 再来看个例子 假设我们要定义一个生成器函数 从5开始倒计时 那么其中一种方法就是用递归的方式来实现 如果k大于0 我就先yield k 然后通过yield from countdown k-1 来生成剩下的所有值 所以如果我要从k开始倒计时 如果k大于0 我肯定要先yield k 如果我写错了代码 只写了yield countdown k-1 我返回的迭代器里 第二个元素其实还是个迭代器 

所以从3开始倒计时 一开始没什么问题 但下一个拿到的却是个生成器对象 这和我预想的不一样 我本来想要的是数字2 要想得到2和1这两个数字 我就得写个for循环 然后在循环里逐个yield的元素 这样倒计时的时候就能得到3 然后是2最后是1 那么更简洁的写法就是 不用写for循环 直接写yield from countdown就行 这样就能输出 321发射 最后加个发射 是不是更酷一点 我们可以改一下 直接加个else yield blast off就行 好 现在我们运行4k countdown 3print k 这样就能得到完整的倒计时序列 最后还有一个惊喜 最后我们来看一个稍微复杂点的例子 我们可以生成s的所有前缀 或者我们假设s是一个字符串 但实际上它可以是任何可迭代对象 如果它非空的话 那么我会yield from s的所有前缀 直到最后一个元素之前 然后我再yield s本身 所以both的前缀 是一个生成器对象 如果我把它们列出来 就会得到b ball bot和both 而且就是这个顺序 因为我在生成s本身之前 先通过yield from处理了前缀 既然我们能够生成前缀 我们也就能生成子字符串了 如果s不为空 那么有些子字符串 其实就是前缀 所以我会先产生所有这些前缀 然后我再用yield from s 生成子符串 剩余部分的子字符串 比如说tops的子字符串 就是一个生成器 如果我把其中的内容列出来 就会看到ttotop和tops 这些就是tops的所有前缀 然后我得到ops的所有前缀 op ops 接着是p ps和s 把这些合起来 就是tops的所有前缀 所有子字符串了 