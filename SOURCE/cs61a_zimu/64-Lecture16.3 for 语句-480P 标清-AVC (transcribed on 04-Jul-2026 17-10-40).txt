前面讲到的集中语句都可以便利可迭代值 这些语句同样可以直接操作迭代器 比如我有个范围对象 从3到6 它包含的数字就是3 4和5 如果我们便利这个范围 就能得到所有元素 要是再便利一次 依然能得到全部元素 因为这个范围对象本身没变 但迭代器就不太一样了 所以如果我们从范围对象获取一个迭代器 它会记录当前在序列中的位置 每次调用next的方法时 这个位置都会更新 这样我就可以逐个获取迭代器剩余的元素 但它不会打印3 因为3已经被跳过了 而是会接着打印4和5 所以迭代器也会迭代器内部的位置 一直移动到序列的末尾 现在我们还有原来的范围对象 我要重新创建一个从起点开始的新迭代器 对于 Ri中的每个元素 我会打印出i的值 所以我把这个范围迭代器传给了for语句来处理 现在迭代器的迭代器会迭代为1 迭代器会迭代为2 迭代器里已经空无一物了 如果我再遍历它一次 什么都看不到 因为已经到Ri的终点了 这就是在for循环里用迭代器的另一个特点 总结一下 如果我在for语句里用迭代器 我还是能遍历所有元素直到最后 但这会改变迭代器的位置 所以就不能再用了 反过来 如果我再迭代器里用迭代器 我操作的是可迭代对象 比如范围对象本身 每次在for循环中使用它时 我都能从头到尾完整遍历 完全不用担心内容会发生变化 