链表是计算机科学里一个很基础的数据结构 它们应用非常广泛 现在我们就来学习怎么用Python的面向对象系统来实现链表 链表要么是空的 因为里面什么都没有 要么包含序列的第一个值 然后跟着链表中其他的值 那我们来看看怎么表示序列345 我们会用一个叫link的类的实例 这个类我还没定义 稍后就会讲到 这个类包含两个属性 我们看到first属性是3 然后rest属性指向某个对象 这个对象就包含了剩下的所有值 而且它本身也是个链表 也就是一个link实例 所以每个链表都包含一个更小的链表 作为后续部分 那么这个link实例的first值是4 它的rest部分则是first为5的link实例 到这里我们要表示的所有数据都用完了 但我们还是需要一个rest属性 这时候我们就用link empty这个特殊值 它是我刚才说的那个规则的例外 每个链表都有一个剩余部分link empty没有首元素 也没有剩余部分 它只是表示列表已经到末尾了 所以正确理解链表的方式 不是关注具体的类和属性名 而是看作一对值 它是一个序列的首元素 接下来是这个序列的剩余部分 这里我们把第一个元素 实际上是第零个元素 保存为属性值 我把它叫做第零个元素 因为等我们实现了列表缩影方法后 它会对应缩影零 

链表的剩余部分 本身也是一个链表或link empty这个值 会存储在另一个属性中 所以属性值本身也可以是对象 这里我们利用组合的概念 就能构建出很有意思的结构 也就是说 链表的属性值本身又是一个链表 最后呢 我们用一个类属性来表示所有的空链表 我们把它叫做link empty 那么构建链表的时候 我们不能直接写3 4 5 那样就变成元组了 而是要用构造方法 比如创建一个包含3的link 而链表的其余部分是一个包含4的link节点 而这个链表的其余部分是一个以5作为首值的link节点 而这个链表的其余部分就是空链表link empty 所以每次调用link构造函数 都会创建一个新的链表实例 这个调用创建了相应的实例 这个实例必须存在才能进行后续调用 这个调用创建了以4开头 后面跟着5的链表 这里需要注意的是 Python中函数求值的顺序很关键 为了计算link4 link5 linkempty这些表达式 我们需要先计算这个操作数字表达式 从而创建代表从5开始序列的链表实例后面 就没有其他元素了 等这个创建好之后 我们就可以把它作为参数 传给这个link 它从4开始 后面跟着我们刚创建的链表 最后我们就能求值整个表达式 构建出完整的链表结构 这种结构在计算机科学中非常常见 所以有一些约定俗成的画法 通常我们会用箭头画出这样的虚堆虚链 但不会画箭头指向linkempty 而是直接在那边画一条斜杠 这不是什么正式的标准写法 但在实际应用中大家经常会这样写 所以我想现在给大家展示一下 在我们的实现中 我们会把linkempty设为rest属性的默认值 这样就能模拟这种结构 这意味着我们可以省略它 当我们需要构造链表的时候 我们可以用这个表达式去写出一个例子 所以这个表达式求之后 应该得到一个代表序列345的link实例 为此我们需要写一个类声明 在这个链表类里面 我们只需要实现一个init方法 它接收first和rest作为参数 好 这就是我们的类 这就是初始化方法 就像我之前说的 我们为rest参数设置一个默认值 就是linkempty 首先我们要确保 我们构建的链表结构是正确的 它要符合我之前说的那个性质 就是说链表的rest部分 要么是空链表 linkempty 或者说它本身是link类的一个实例 也就是说它本身就是一个链表 所以只有这两种情况 可以作为链表的后继部分 然后我们把第一个元素 以及链表其余表的内容 以及链表其余表的内容 以及链表其余表的内容 以及链表其余表的内容 以及链表其余表的内容 这一部分都存储为self实例的属性 essence是Python的内置函数 传入这两个参数后 它会判断rest是否是一个link 如果你查看Python 关于essence的帮助文档 它会告诉你 这个函数用于判断对象 是否属于某个类或其子类 所以这和直接获取rest的类型 还是有些区别的 然后检查它是否完全等于link类 本身 如果rest是link子类的实例 这个判断同样成立 这个设计思路很不错 因为这样一来 我们的系统就具备了扩展性 如果想通过继承link 来创建特殊类型的链表 我们仍然可以使用同一个构造方法 验证逻辑也能按预期正常工作 那么link empty就能用来创建 具体是什么呢 在具体实现上我们有几种选择 我选择用空元组来表示它 虽然我可以专门为这个空链表 设计一个特殊的类 但我直接用一个零长度的序列来代替 因为空链表本质上就是这样的 就是一个零长度的序列 好 我们来看看具体怎么用 我可以创建一个链表 依次包含三四五 依次包含三四五 这三个数字 如果我把这个链表叫做s 我就可以开始检查它的属性了 s first是3s rest是4和5 也就是说 如果我想获取数字4 我需要写的是s rest first 也就是链表剩余部分的第一个元素 我可以通过访问链表剩余部分的 剩余部分的第一个元素 来获取s rest first 我可以通过访问链表剩余部分的第一个元素 来获取s rest first 这样就能得到5 最后 我可以检查链表往下数三个rest 是否为link empty 结果应该是真 因为那里已经没有元素了 这些就是我操作链表的基本方法 当然 我们也可以修改链表中的值 比如 我可以把s rest first的值改成7 这些就是我操作链表的基本方法 当然 我们也可以修改链表中的值 比如 我可以把s rest first的值改成7 这些就是我操作链表的基本方法 当然 我们也可以修改链表中的值 当然 我们也可以修改链表中的值 比如 我可以把s rest first的值改成7 我可以把s rest first的值改成7 我可以把s rest first的值改成7 我可以把s rest first的值改成7 那么 如果我看看s的内容 会发现 它现在是3775 这样的顺序 不过 链表通常用在这样的场景中 也就是 不需要修改数值的情况 也就是 不需要修改数值的情况 这正是链表专门为此设计的 这正是链表专门为此设计的 所以 所以 你只需要在前面加个不同的值 你只需要在前面加个不同的值 就能创建一个类似的新链表 就能创建一个类似的新链表 所以 如果我想创建一个链表 开头是8 后面是7和5 我只需写成link8 我只需写成link8 然后加上列表的剩余部分 然后加上列表的剩余部分 这样写 就会创建一个新列表 但不会改变原来的列表 但不会改变原来的列表 但不会改变原来的列表 这样写 就会创建一个新列表 但不会改变原来的列表 然后加上列表的剩余部分 然后加上列表的剩余部分 然后加上列表的剩余部分 这样写 就会创建一个新列表 但不会改变原来的列表 但不会改变原来的列表 