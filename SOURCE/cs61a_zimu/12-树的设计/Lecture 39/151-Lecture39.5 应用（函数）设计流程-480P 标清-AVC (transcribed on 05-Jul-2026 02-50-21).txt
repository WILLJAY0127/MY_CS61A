现在我们把这个设计流程 应用到另一个数结构问题 问题是这样 实现Small参数 它接收一个带整数标签的数 实例T 函数返回T中 那些标签小于所有后代节点的非叶子节点 这里有一行行为描述 返回T中标签小于所有后代节点的非叶子节点 还有一个实例 首先看数据定义 这个函数直接操作数结构 这里我们不清楚这些数具体代表什么 所以第一步基本没什么要做的 第二步是写文档说明 有些信息已经给出了 但文档里很重要的一点是理解函数签名 包括它接收的参数类型和返回值类型 Small参数接收一个数结构 这个在文档里明确写出来了 然后我还看到一个例子 例子中 在数上调用了small 它返回什么呢 这个问题很关键 需要我们好好想想 想要正确实现这个函数 就一定要注意 它返回的是数T中的非叶子节点 那么返回的格式是怎样的呢 这里我们看到调用了small函数 A是一个可迭代对象 这个可迭代对象里的每个元素都有一个标签值 这就说明small函数需要返回某种形式的数 结构列表 或可迭代对象 也就是说 我们把非叶子节点表示为输入数 T中对应的数实例本身 第三步 我们通过一个示例来理解 这里我们需要画个示意图 然后逐一检查每个节点 它是不是T中的非叶子节点 并且标签值小于所有后代节点的标签值 根节点怎么样 节点1并不比它所有的后代节点数值小 因为它有一个标签值 它有一个后代节点0 节点2呢 节点2比它所有的后代节点都要小 它比节点4和5都要小 节点4怎么样 这是个叶子节点 我们要找的是非叶子节点 节点5也是叶子节点 3不小于0 而0则小于它所有的后代节点 6是叶子节点 关键点在于 一确实小于2和3 只是不比0小 所以 我在实现这些标签值数据中 这个函数时 需要确保 我能找出以某个节点为根的 整颗子数的最小值 而不仅仅是看直接子节点 但这个例子还没分析完 我得明确记下返回的内容 当我用a调用small函数时 我们先构建一个列表 这个列表里要放什么呢 所以我要建的列表 含以2为根的数 还有以0为根的数 但测试用力 没显示完整的数结构 而是按顺序显示了 每个子数的根节点标签 所以 我看到有个0 还有个2 好 现在我来写个代码模板 我们的代码模板是 创建一个列表 用递归函数process 来填充它 最后 返回这个列表 要注意 我们这里新加了一个函数 所以 我们需要重新梳理一下这个流程 数据定义这块 基本没什么要改的 但这个process函数的签名 应该是什么呢 我们需要明确这个函数 具体要做什么 我觉得 process函数 应该处理 数中的每个节点 然后 找出 该子数中 最小的 标签值 为什么我们要这么做呢 因为我们需要判断节点 1是否符合条件 我需要知道 这个子数的最小标签 以及那个子数的 最小标签 因为1 必须比它所有后代节点的标签值 都要小 所以 process函数的目标 就是 这是我在考虑如何实现 small函数时想到的 就是需要一个能找出数中 最小标签值的函数 所以 process函数的签名 应该是 接收一颗数作为输入 返回一个数字 这个数字 就是最小标签值 然后我还要去看 我还需要对它的行为 做个简洁描述 我们要找出数题中的 最小标签 那么 一个函数 其实是可以同时完成两件事的 在这个例子中 我们在处理这个节点 找最小标签的时候 我们也能顺便决定 要不要把它加到结果里 因为一旦我们找到最小标签 这说明 我们就能找到它 各个分支的 最小标签 这样 就能判断这个节点 是否比所有后代节点的 标签都小 如果满足条件 就把它加入结果 好 按照我之前 给大家介绍的方法 其实我本来想直接写代码片段 不过我们还是按他们的流程 来走一遍 他们的方法是 先分析一个 具体例子 好 我们来看个例子 假设Prosess函数 接收2 4 5 作为输入 我们要计算 每个分支的 最小标签 然后找出这些值的 最小值 就是最小后代标签 判断这个最小值 是否小于 还是 大于等于 根节点标签2 这里2 就是最小值 接着 我们要找出 整个子数的最小值 也就是 比较根节点的标签值 和各分支的最小值 去其中更小的那个 在我继续思考这个问题时 每次用到变量t的时候 我就会回想 刚才分析过的这个例子 现在我觉得 我们可以开始写模板了 模板大概会是这样的 如果这是个叶子节点 那么找整棵树的最小值 就很简单了 因为只有一个标签值 直接返回这个值就行 否则 我们需要比较分支中的 最小标签 和根节点标签 去更小的那个 如果是在考试中 现在是查看题目模板的好时机 模板 就在这里 大家可以随时暂停视频 自己尝试解决一下 好消息是 题目给我们的模板 和我们自己设计的 模板完全一致 所以我们已经知道该怎么办了 如果t是叶子节点 那我们直接返回t label就行了 现在 我们需要马上对照函数签名 检查一下 返回的是个数字 这正是我们需要返回的结果 这是t中最小的标签 但我们有没有考虑到 或许应该把t加到结果里这件事 好 如果t是叶子节点 那它就不可能是非叶子节点了 所以 我们不需要把它 加到结果里 否则的话 t肯定是有分支的 所以我们需要找出 各个分支中的 最小标签值 如果t的根节点标签 比所有后代的最小标签 还要小 那就要把它 加入结果 集中 这里就印证了 我们之前的分析 也就是说 返回值应该是t中的 最小标签这个值 要么来自t节点自身的标签 要么来自它后代节点中的最小标签 那么我觉得可以采取这样的做法 我需要留意的是 在某些条件下要向结果添加内容 那么 具体要添加什么呢 好 这里我们来看一下 small 函数的签名 就知道 结果列表里 需要包含数结构 那么数的变量名 是什么呢 t本身就是要添加的数 接下来 我会记下对smallest的理解 smallest这个变量 将表示t的 所有分支 或后代中的 最小标签值 一旦弄清楚它的含义 就能确定 怎么使用它 即使我还没写下具体的计算方法 将t 加入结果 列表的条件是 t的标签值要小于 所有后代节点中的 最小值 最后呢 我们需要计算 这个最小值 也就是 对t的每个分支 递归掉用process函数 然后 取最小值 这里有个熟悉的模式 就是把递归掉用的结果 汇总起来 这样 我们就能处理 数中的所有节点了 我们先处理根节点 然后处理各个分支 接着 处理分支的分支 这样process 函数就会在数的每个节点上 调用一次 对于每个节点 如果是 叶子节点 我们会找出这个叶子节点中的 最小标签值 其实就是 t label本身 否则的话 我们要找出 整棵树的 最小标签值 也可能把t 添加到结果里 