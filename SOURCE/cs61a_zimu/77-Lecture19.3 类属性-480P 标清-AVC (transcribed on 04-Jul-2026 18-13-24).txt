类属性就是属于类本身的属性 当我们执行类语句的时候 就会创建一个新的类 它会在当前环境的第一帧中获得一个名称 然后类语句代码块中的复制语句和定义语句 就会创建类属性 通常复制和定义语句会在帧里创建变量名 但这里的情况不一样 实际上它们都会成为类的属性 实际上你可以在类语句里写任何代码 不过这段代码本身没什么实际用处 但这只是为了展示一些可能性 你可以写复制语句和定义语句 这会创建类属性 也就是说这些值可以通过类来访问 no是一个类属性 它的值是又大又红 dance是一个类属性 它是一个可以调用的函数 但通常你不会写这样的函数 因为当你实例化这个类时 这时就会遇到问题 不过这个例子的重点是想展示 执行类语句时会发生什么情况 它会在类中创建一堆类属性 那么创建类属性是有充分理由的 类属性会在类的所有实例之间共享 因为它们是属于类本身的而非实例 比如说 如果所有账户的类率都一样 与其用实例属性来表示类率 不如直接使用类属性 这样就不用为每个创建的实例都存储这个值 我们只需在类里存一次 创建了两个实例之后 我们可以通过任意一个实例来访问类率 访问时不太容易看出 所以这种类型的定义是什么 类型的定义是相同的 即使它是类属性 看起来就像是Tomacom的属性 它确实是Tomacom的属性 但它是类属性 也就是说每个实例都共享相同的值 重要的是interests的属性并不属于实例 而是属于类的一部分 这其实就是通过查找过程来访问它 