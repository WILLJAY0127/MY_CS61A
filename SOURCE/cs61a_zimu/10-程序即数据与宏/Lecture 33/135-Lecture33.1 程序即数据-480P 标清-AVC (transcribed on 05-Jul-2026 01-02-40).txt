程序本身就是一种数据 scheme程序都是由表达式组成的 这些表达式可以是基本表达式 像是数字2,3.3,true,还有符号plus,符号quotient 但除此之外的都是组合表达式 每个组合表达式其实就是scheme列表 所以这里有个列表包含了符号quotient 数字10和数字2 这里看到符号not和基本表达式true 我们之前讨论过的内置scheme列表数据结构 它的内部实现是car和cdr构成的链表结构 但可以用来表示scheme程序中的组合表达式 好,如果我们计算下面这个表达式 构建一个包含quotient的列表 然后是10,再接着是2,得到的 就是一小段scheme程序代码 一个表示求10除以2的组合表达式 注意,这样计算并不会真的去算10除以2 它只是构建了一个列表 但有个内置函数叫evil evil可以处理任何用scheme列表表示的表达式 或者是基本数据类型 然后返回运算结果 所以evilquotient 102就会得到数字5 这节课给我们的启示是 如果想写一个生成scheme代码的程序 我只需要构建列表就可以了 如果在某个时候我构建了一段代码 又想知道对它求值会得到什么结果 那我直接调用evil函数就行 Python也有内置的evil函数 输入字符串就能得出它的值 死兵的值值是10 输入字符串就能得出它的值 死兵的值值是10 输入字符串就能得出它的值 输入字符串就能得出它的值 输入字符串就能得出它的值 死兵的特别之处在于 它内置了完整的功能来处理scheme列表 而且所有的代码本质上都是列表 所以像写这样的表达式来构建程序就更容易了 Scheme语言有个很棒的特点 这在Lisp家族语言中都适用 就是编写能生成程序的程序非常直观 我们现在就来试一下 我先启动scheme 如果我写一个能算出值的表达式 这很简单 但如果我用列表形式把各部分列出来 这是个对数字1和2进行相加的过程 那我就会得到这些部分的列表 但这还不是scheme代码的样子 scheme代码其实是这样写的 所以我这里加引号的意思是暂时不对 我这里加引号是为了让scheme代码进行求值 而是让它保持符号的形式 等我让scheme对整个组合求值时 这个符号才会被求值 因为它处在运算符的位置 所以会被求值为加法函数 然后应用到1和2上 现在如果我直接列出plus 比如在这里写个更复杂的表达式 2加3 我就会得到一个表达式 其中这部分已经先被求值了 因为这就是scheme的工作方式 scheme会对运算符和操作数进行求值 然后返回一个已经算出5的表达式 但还没处理到6这部分 好现在我们来加在右边的这个小程序 fact这个过程的逻辑是 如果n等于0 就直接加成0 直接返回1 否则它会返回n乘以的阶乘 这就是阶乘的一个直接递归定义 如果我计算3的阶乘 就会得到3乘以2乘以1 而5的阶乘就是 5乘以4乘以3乘以2乘以1 这只是一个返回数字的scheme程序 但我也可以写个scheme过程 让它返回计算n阶乘的表达式 这个叫做fact xp的变体 它的任务就不是返回数字了 相反 它的任务是返回一个表达式 1本身就是一个表达式 很好地表示了数字1 如果我想用更小的表达式 来构建一个大表达式 我需要一个列表 这个列表包含乘法运算 我要相乘的数字 还有计算剩余阶乘的方法表达式 所以现在当我计算5的阶乘时 结果依然是120 但如果我要获取计算5阶乘的表达式 现在就能看到5的阶乘是如何计算的 1乘2乘3乘4乘5 所以这就是一个返回表达式的函数 如果我对这个表达式进行求值 就能得到 120 我们再用同样的模式 我们再用同样的模式 我们再用同样的模式 看一个例子 首先定义斐波那契数n 如果n是0或1 也就是小于等于1的情况 那么fib n 就是n 否则就需要把前两个斐波那契数加起来 也就是fib 和fib 所以斐波那契数2等于1 斐波那契数6等于1 斐波那契数7等于7 斐波那契数6等于7 × 2 × 2 × 2 × 2 × 2 × 2 是8 它是怎么算出8的呢 用的是数形地规的方法 我也可以写成一个大的欠透表达式 所以我们要把所有fib改为fib exp 表明这个过程的目标 是生成计算斐波那契数n的表达式 这个表达式通过列出要执行的操作来构建 然后将这个操作应用于两个更小表达式的结果 它们分别计算fib n2和fib n1 所以fib6的结果还是8 但计算fib6的表达式是 把fib4和fib5相加 其中fib4是这样的表达式 好 这就是计算fib5的表达式 现在如果我执行 并计算这个0和1的欠透加法 得到的结果就是非b6 但这个程序生成的是表达式 而这个程序直接给出数字结果 