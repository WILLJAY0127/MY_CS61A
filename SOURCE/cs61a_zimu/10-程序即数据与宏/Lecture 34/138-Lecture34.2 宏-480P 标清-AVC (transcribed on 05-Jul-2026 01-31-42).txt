红是SKIM语言的一个重要特性 它可以让我们在语言中定义新的特殊形式 在Python中 我们有一整套表达式和语句类型 SKIM语言也是这样的 我们有define, lambda, if, can, and, or 等等这些特殊形式 其他的就都是过程了 现在红的作用就是让我们能够发明新的特殊形式 来扩充语言的表达能力 这意味着我们可以改变语言的工作机制 创造出新的程序求知流程 听起来很高大上 事实也确实如此 正是这个特性 让很多人爱上了Lisp编程 它在SKIM这类Lisp语言中 表现很好 但是它也不只在Lisp语言中 表现不好的原因 是代码数据化处理起来很方便 程序本质上就是列表 红的作用就是进行代码转换 所以我们创建新特殊形式的方法是 先描述如何分解一个特殊形式的组成 再用这些部分构建出普通的SKIM代码 最后对这段代码进行求知 因此 红就是在原代码执行中 在编程前对其进行处理的一种操作 实际上 红在很多编程语言中都有体现 但在像Lisp这样的语言中 它们无疑是最容易正确定义的 因为代码本身就是数据 SKIM 语言中有个特殊形式叫Define Macro 可以让你为自己想创建的特殊形式 定义原代码转换规则 来看一个名为Twice的红的语言 大家可以看到 它的样子很像过程定义 但Twice表达式的求知方式却截然不同 意思是说 当你对表达式使用Twice时 你会创建一个以begin开头的列表 然后把这个表达式重复两次放进去 定义好这个红之后 就可以对任何表达式使用Twice了 比如这里 我们对Print2表达式使用Twice 背后的运作机制是这样的 因为Twice是一个设定的表达式 是红 所以 它会在表达式求知之前 先对输入的表达式 执行红的处理逻辑 所以实际上构建的是一个新的表达式 begin print2 然后 对这个新表达式进行求知 这样 就会打印两次2 这里面有个很有意思的地方 如果Twice只是一个普通过程 不管它怎么定义 Print2 都会在Twice调用之前 就被求知 这意味着 数字2会显示一次 这个值2就消失了 因为一旦执行了打印 就找不回这个值了 所以Twice 能做到一些普通过程 做不到的事情 就是能够处理这个表达式 在它被求知之前先复制一份 然后对这个表达式 进行 两次求知 所以红调用表达式的求知过程 是有效的 比如 像这个例子 是首先对运算符子表达式 进行求知 如果求知结果是红 也就是通过define the macro 特殊形式定义的 那么处理方式 就和普通过程 不太一样了 我们直接对操作数表达式 调用红过程 而不预先求知 然后对红过程返回的表达式 进行求知 所以红接收的是表达式 返回的也是表达式 而不是像普通过程那样处理具体的值 我们先来聊聊print这个函数 当我运行print两时 屏幕上就会显示2 但如果我把x定义为print2 然后查看x的值 会发现x什么值都没有 一旦打印完成 数字2 就消失了 如果我想打印 两次2 我可以写成print2和print2 但要把这两个操作变成一个表达式 我们需要用到 begin这个特殊形式 begin特殊形式 就是说按顺序执行这些表达式 假设我们只能用普通过程 而我想要定义twice函数 我就按照 幻灯片上的 函数体来写 begin 后面跟一个表达式 然后重复这个表达式 现在 twice print2这样写是行不通的 屏幕上会显示一个2 然后构造出的表达式是begin 后面跟着两个空值 也就是print2的返回值 如果我想构造的是begin print2 print2 我这里就需要加上引用了 所以用过程来实现也不是不行 我只需要考虑使用引用机制来确保 它不会被提前求职 但即便是这个版本 也还没打印出东西 要想打印出来 唯一的办法就是对加了引用的print2调用twice的结果进行求职 

红的核心思想是 引用和求职这些步骤都自动帮你处理好了 所以 如果我们不把它定义成过程 而是定义为红 那就不用再输入twice quote print2这么麻烦了 然后对结果求职 我直接输入twice print2 就能看到2 显示两次 所以这里同时发生了 简单又深刻的事情 简单来说 它只是 确保 print2 不会被提前求职 比如先引用它 传给twice表达式 再对结果求职 深刻的 是我们改变了语言的工作机制 twibe现在变成了特殊形式 不需要 先求职参数 这意味着我们完全掌控了 红内部是如何以及何时进行求职的 我们来看看 这一步 这有什么用 假设我想定义一个过程 来检查某个表达式 是真是假 如果我传入一个值 我可以写 如果这个值为真 那就表示检查通过了 否则 检查就失败了 比如说 如果 我设x 等于2 然后 想检查 x 是否大于0 那个检查 我肯定过不了 所以这是一种在scheme程序上的 添加简单测试的不错方法 不过他不会告诉你 具体是哪个测试失败了 如果他能告诉我们 具体是哪里出错了 就好了但要做到这一点 我们需要获取被求职的表达式本身 而不仅仅是最终结果 所以我们来定义一个 红 首先 我们把它命名为check 来保持一致性 但这个红 接受的 不是具体的值 它接受一个表达式 作为输入 而不仅仅是 写下 要计算的内容 我需要 逐部分的构造 这个表达式 我们通过构建一个包含if 的列表 来检查表达式 然后 放入我们要计算的 表达式 最后显示通过 或失败 这里我用了两个引号来表示 我希望 可以看到这个结果 对了 这个结果是这个 被引起来的past 现在我定义的这个红行为 就和之前的过程 完全一样 那么如果我启动scheme 并夹载这个文件 我把x定义为-2 然后 检查一下 看看 x 是否 大于0 它会 显示 失败 我费了这么多功夫定义红 这个 而且目前还没实现什么新功能, 但我们可以让它在显示失败的同时, 还能告诉我们具体是哪个表达式出错了。 所以当检查失败时, 我来创建一个新列表, 里面还是会显示fail, 但同时还能显示具体的表达式。 

现在我们就按之前的方式运行, 定义x为2, 写一段看起来像过程调用的代码, 但这其实不是过程调用, 因为失败时, 它会告诉你失败了。 这是一个假值, 同时还会显示导致失败的表达式。 这是普通过程无法实现的功能。 结束前, 我们快速看一下这个红构件的表达, 表达式。 

你可以通过移除红定义来实现, 现在就只剩一个常规过程了, 这个常规过程可以处理x等于2的情况, 但调用它的时候, 我们需要显示引用表达式, 就像调用红那样, x0, 这个表达式构建出以下内容, 如果x大于0, 它就只会显示past, 否则它就会构建这样一个列表, 先是failed, 然后是待检查的表达式。 如果我对这个结果进行求职, 那么我就能看到它失败了, 但当然了, 如果x的值是2, 然后我同样进行求职, 那么我就能看到它通过了。 这样我们就从头构建了一个红, 一个名为check的新特殊形式, 它可以检查表达式的求职结果是真还是假。 当结果为假时, 它会把这个表达式展示给你看。 