另一种地归数据结构是数结构。 我们之前在课上也讲过数这个概念,但这个结构特别重要。 值得我们再专门讲一次,数结构跟链表很相似。 数有一个根结点标签,以及一组由子数构成的分支。 链表则包含一个首元素,以及剩余的链表部分。 所以主要区别在于,因为数的分支本身也是数,而链表只包含一个链表,作为剩余部分。 这些术语我们之前提到过,但咱们再快速过一遍。 描述数有两种方式,地归描述可以类比为数目从地面生长, 只不过它们是向下,而不是向上生长。 数包含一个根结点。 数包含一个根标签和一个分支列表。 这里的根标签是3,有一个分支,还有另外一个分支,每一个分支本身也是一棵树。 没有分支的数,我们称之为叶子结点。 这张图里有5个叶子结点,叶子结点也有自己的标签,它们只是没有分支,数是从根结点开始的。 比如,这里是整棵树的根结点,这里是左侧分支的根结点,分支可以没有? 可以有一个。 也可以有多个,而且每一个分支本身又都是一棵树。 也就是说,每个分支都有属于自己的根结点,要描述树中的各种子树。 通常会用到结点这个概念,结点就是树中的一个位置。 

每个结点本身也是一棵树,但当我们说结点时,重点就不在于子树本身, 而是它们有一个位置。 这就是分支本身与其他结点的相对位置。 每个结点都有一个标签,并且每个结点都与其他结点存在相对关系。 3是2的负结点,2是3的子结点。 最顶部的结点就是根结点。 就像我刚才那样,大家通常会用标签来表示位置,与其说标号为2的结点是标号为3的结点。 最常见的路径是根结点到叶子结点的路径。 更常见的是,大家会直接说23的子结点。 但实际上,真正存在家族关系的是结点,标签仅仅是数字而已。 路径就是结点的序列,每个元素要么是负结点,或者作为前一个结点的子结点,这样它们在树状图中就连接起来了。 最常见的路径是从根结点到叶子结点的路径。 但也可能有其他类型的路径。 我们之前用数据抽象来处理树结构,但我们也可以用Python的对象系统。 一棵树有一个标签和一个分支列表,每个分支本身也是一棵树。 我们可以定义树类来实现。 构造函数接受标签和分支列表。 默认是空列表。 我们设置。 我们把分支列表的标签称为标签值。 我们会检查每个分支是否都是树结构,然后将这些分支复给self branches。 这里我们把分支转为列表,这样就能保留一份副本。 同时也能确保分支被转为列表形式。 即使原本是其他类型,这是之前用数据抽象的定义方式。 两者很相似,但有些关键区别。 现在我们改用对象系统。 就来定义分支。 我们可以使用数据抽象的构建方法。 但我们不需要明确定义如何从树中获取属性。 使用数据抽象时,我们要写下构造函数和选择器。 这里我们写下构造函数。 选择器则是隐式的self,label就能获取标签。 branches就能获取分支。 更重要的是,用数据抽象时, 我们需要设计一种将标签和分支组合起来的方法。 比如,我们决定把它们存到一个长列表里。 但我们也可以选择其他做法。 存到一个对里,可以把它们存到字典里。 不管采用什么方法,我们都得设计一种方式。 用选择器函数把这些部分提取出来。 当我们使用Python的对象系统时, 我们就不用操心这些部分怎么组合起来。 我们总是采用相同的方式。 每个部分都是一个属性。 对象的所有属性都可以直接用名称来访问。 所以用对象系统定义会简单一些。 但使用数实例的方式和数据抽象基本一样。 这里有一个创建斐波那契数的函数定义大体上是一样的。 但有些细微差别, 当我们选择一个数据抽象时, 我需要把Tree手字母大写, 然后用点号来访问标签属性。 在数据抽象方法里, 我有个叫Tree的函数, 通过选择器函数来获取标签。 但我们的代码基本不用改。 所以面向对象编程的代码风格和数据抽象的写法差别并不大。 定义好Tree类之后, 我们就可以用Tree来写字。 我们可以用Tree来写字。 我们可以用Tree来写字。 我们可以用Tree来写字。 我们可以用Tree来写字。 定义好Tree类之后, 就可以直接使用了。 所以,只要调用Tree函数, 就能创建一个叶子。 如果我想给这棵树添加分支, 如果我想给这棵树添加分支, 不能只传入标签值, 就完事儿, 必须传入一个树的列表。 必须传入一个树的列表。 这样就创建了一棵树, 我称之为T。 它的reper字符串是一个能构建等效数的表达式。 它的reper字符串是一个能构建等效数的表达式。 打印出来会得到一个T, 一个多行输出。 根标签最先显示, 其他所有标签都是缩进的。 其他所有标签都是缩进的。 不妨看看生成reper字符串的代码, 不妨看看生成reper字符串的代码, 以及STSTR字符串的代码, 来理解它的工作原理。 我们还添加了一个方法, IceLeaf,用来判断节点是否为叶子节点。 用来判断节点是否为叶子节点。 我们也定义了FibTree函数, 我们也定义了FibTree函数, 就像幻灯片上展示的那样。 FibTree4已经是个相当大的树了。 FibTree5的根节点是斐波那契数5, FibTree5的根节点是斐波那契数5, 而FibTree6看起来确实非常庞大。 而FibTree6看起来确实非常庞大。 大家看这些圆括号和方括号, 大家看这些圆括号和方括号, 但如果我打印出来, 结构就会清晰很多。 根节点是8, 分出两个不同的分支, 其中一个分支的根标签是3, 另一个分支的根标签是5, 3加5正好等于8。 那这棵树的叶子节点都有哪些呢? 叶子节点包括0,1,1,0,1,1,1,1。 叶子节点包括0,1,1,0,1,1,1,1。 叶子节点包括0,1,1,0,1,1,1,1。 我们来写个函数, 把树的叶子节点整理成列表。 如果T是叶子节点, 就直接返回一个单元素列表。 就直接返回一个单元素列表。 里面是树的标签, 否则就需要把所有的叶子节点 收集到一个列表里。 我们先创建一个空列表, 然后遍历T, 然后分枝B, 我们就对B,地归,调用leaves函数。 那这个结果要怎么处理呢? 我们要把它的所有元素 都加到all leaves列表里。 这是列表自带的方法。 具体是哪个方法呢? 我们要用extend的方法 把地归调用分枝leaves函数 把地归调用分枝leaves函数 得到的新叶子列表 扩展到all leaves里, 也就是分枝B的叶子。 也就是分枝B的叶子。 然后返回所有的叶子。 如果对fib tree, 6,要用leaves函数。 我们得到了一组叶子节点。 它们的总和是8。 也就是斐波那契数6号。 这个树的高度是多少? 嗯,一个叶子的高度是0。 通常来说, 树的高度 就是最长路径上的边数。 就是最长路径上的边数。 从这里开始数, 一次转换, 两次,三次,四次,五次。 我觉得, 我们得到了一个最大高度, 就是5。 我们该怎么计算这个高度呢? 如果t是叶子节点, 那么高度就是0。 否则, 树的高度就是所有分枝中 最大高度, 再加1。 我们来计算t branches中 每个分枝B的高度, 然后取其中的最大值。 这是fib tree6的例子。 它的高度是多少? 我们掌握的处理树的方法, 其实是一样的。 不管是用数据抽象, 还是用树类, 但使用树类有些额外优势。 比如用这个 简单的递归str函数, 就能美观的显示 树结构。 