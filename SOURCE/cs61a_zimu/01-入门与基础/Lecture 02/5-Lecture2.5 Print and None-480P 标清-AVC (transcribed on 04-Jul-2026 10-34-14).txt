下面讲print函数和none值 大家知道 如果我在Python交互式解释器里 输入表达式 它就会显示这个表达式的值 同样的 如果我用print输出2 也会看到同样的结果 但这两次操作其实是不同的 我们来理解一下它们的区别 好 那么如果我输入go bears 大家看 go bears 正好和我输入的表达式一样 因为它是个字符串 字面量 所以我们看到的就是字符串值 但另一方面 如果我输出go bears 看到的几乎一样 只是没有引号 所以输出和单纯求值 肯定是有区别的 我们再来看一个特殊的值 none 就能更清楚地看出这个区别 none表示空值 如果直接求值 就什么都看不到 如果我打印它 就能看到none 那么这里的情况是 python会对输入的表达式 自动显示其值 比如会自动显示2 或者go bears none比较特殊 它不会自动显示 但如果我用print 就能让它显示出来 好的 print还能做什么 print函数可以打印多个值 用空格隔开 它还能打印none值 所以我可以先打印一个none 再打印另一个none 这里有个很有意思的情况 如果我对print函数 进行嵌套调用 会发生什么 大家先花点时间 想想会发生什么 然后我来演示 3 2 1 1 2 none 我们来仔细理解一下 刚才发生了什么 首先 什么是none 嗯 none表示 某个函数没有返回任何值 所以这个特殊值 none就是用来表示空值的 在python中 我们叫它none 其他编程语言里 它有别的叫法 没有明确返回值的函数 实际上都会返回none 解释器不会自动把none显示为表达式求值的结果 如果我来定义一个函数 doesn't not square 这个函数只计算x乘以x 但不返回计算结果 那我就创建了一个不返回任何值的函数 因为我们没有写return语句 所以当我调用does not square时 我不会得到16这个结果 相反 我得到的是none 然而根据第三条规则 解释器不会把none显示为表达式的结果 所以这里是一个调用表达式 如果我对它进行求值 4确实会乘以4 但结果不会被返回 返回的 是none 这个值 但none根本不会显示出来 好 接下来我可以输入16等于 does not square 这里会发生的是 我们要计算这个调用表达式的值 它会被计算得到呢 然后这个值会被绑定到变量名16上 所以现在变量名16就绑定了none这个值 如果我直接输入16 什么都不会显示出来 如果我输入16加4 结果 不会是20 而是会报类型错误 不支持none type和int类型相加 这是什么意思呢 这里的加号就是之前那个加号 错误信息是说 我试图把none值和整形数int相加 4是整数 所以这种错误通常出现在 当我们试图把某个值与空值相加时 这种做法是行不通的 好了 我们回来看看 print函数 其实呢 函数主要分为两种 一种是值返回值的纯函数 另一种是带有副作用的非纯函数 比如ABS函数就是一个纯函数 它可以计算传入参数的绝对值 -2的绝对值就是2 abps函数唯一的作用就是返回数值2 只要用-2来调用它 这就是传入的参数 这就是函数的返回值 这是一张纯函数的函数 这是一个函数的示意图 它就像个封闭管道 从输入到输出 拋函数是另一个纯函数 我传入两个参数就能得到一个返回值 而非纯函数就完全不同了 它们会接受一些输入值 也会产生一些输出 比如print函数 它的输出或返回值就是none 不过除此之外 它不是一个封闭系统 它还有副作用 这就是它产生的影响 是不是除了返回none print还会显示传入的内容 那么在这个例子中 我们传入了-2作为参数 它显示了-2 返回值是none 而副作用根本就不算一个值 这只是执行时产生的现象 这是一种行为表现 这是调用函数带来的结果 所以print和abps函数的返回值是none 和abps和pubule是有所不同的 因为它有副作用 所以才会出现这种有趣的现象 当我们把print调用嵌套在一起的时候 我们再来看看那个print print的嵌套表达式 之前我们看到输出是12 然后是none 我们来看看这是怎么回事 我们可以用表达式数来分析 要计算这个返回值是none 我们先来看看这个嵌套调用表达式的值 我们先计算运算符 也就是打印函数 第一个 参数本身又是一个调用表达式 我们要分别计算它的运算符和参数 现在我们要用print函数处理数字1 实际执行print时就会产生副作用 显示出数字1 这就是第一个1的出处 就在我们求这个操作数字表达时 表达式的值时 屏幕上显示了1 但它并不是任何表达式的值 它出现是因为print函数的功能 就是让内容显示出来 现在我们得到这个表达式的值 也就是none 因为print函数总是返回这个值 接下来 我们计算这个操作数 它本身是调用表达式 对数字2应用print就会显示2 大家看 这里这个子表达式的值就是none 那么现在就是对none和none应用print函数 当我们对这两个none使用print时 就会产生显示none none的副作用 这就是我们在这行看到的结果 最终整个表达式的值也是none 但这里的输出中那个none并没有显示 因为python交互式解释器不会自动显示none 所以我们得到的仅仅是在提示符下求知得到none时 所以我们得到的仅仅是在提示符下求知得到none时 