Python执行程序时 不同表达室会在不同环境中进行求职 所以在同一张环境图中可以有多个环境 接下来我们要通过图表来详细分析上次讲的例子 让大家清楚了解具体运行过程 但在这之前 我们先通过这张幻灯片来回顾一下用户定义函数的相关内容 我会用总结的形式把重要术语都涵盖进来 首先我们来看def一句 def一句就是用来创建函数的 它的写法是这样的 这整个结构是一个def一句 它跨越了多行代码 它包含了函数名称和一个形式参数 这就是你给函数参数起的名字 也可以有多个形式参数 这种情况需要用逗号分隔 每个参数对应一个位置 def一句的函数体就是第一行之后所有缩进的部分 在这个例子里 它只是一个单独的return一句 当然函数体也可以包含多行代码 这个return一句带有一个返回表达式 作用是将x与x相乘 

当我们执行 def一句时 就会创建一个新函数 这个名称 也就是我们看到的square 会绑定到当前真的函数上 

现在虽然我已经定义了这个平方函数 但我实际上还没进行任何乘法运算 因为我还没调用这个函数 这需要通过调用表达式来完成 所以等程序运行到后面 我可能会写square j 就是一个调用表达式操作符 就是函数名square 它的值就是那个平方函数 也就是我们刚才定义的那个 好 括号里面有一个操作数 操作数是一个表达式 在这里就是2加2 这个表达式会求之为4 也就是函数的参数 所以当我们遇到调用表达式时 我们先计算操作符和操作数的值 然后用这些参数来调用函数 调用用户自定义函数的过程 也需要仔细讲清楚 所以我之前展示过这样一个示意图 就像函数签名封装在一个小管道里 参数从这边输入 返回值从另一边输出 这个过程具体是怎么实现的 首先创建一个新的针 把函数的形式参数和我们传入的参数 值绑定起来 这里x就会绑定到数值4 然后在新的环境里执行被调用函数的函数体 那么在这种情况下 我们会计算x乘以x 此时x绑定为4 4乘4得到16 好的 这就是最简单的例子 稍微复杂些的情况 是导入μl之后 我们像之前一样 定义square函数 但这次我们要算的是3的平方的平方 这个图表显示 我们已经执行了dev语句 它将名称square 绑定到新创建的平方函数上 接下来就要计算3的平方的平方 那我们该怎么求3的平方呢 平方呢 这是一个调用表达式 所以我们直接套用调用表达式的求值规则 也就是说先要计算操作符 也就是平方函数 本身操作数本身也是个调用表达式 所以我们需要再次执行这个求值过程 square就是求平方的函数 3就是数字3 现在我们可以用自定义函数square 来对数字3进行运算 那具体怎么做呢 整个过程分三步 第一步新建一个帧 接着把形式参数x绑定到实参值3 接着把形式参数x绑定到实参值3 接着把形式参数x绑定到实参值3 接着把形式参数x绑定到实参值3 这样绑定关系就建立好了 最后执行函数体 这里函数体是return 所以我要计算x乘以x 得到返回值9 这样我就得到了这个调用表达式的值 现在既然我们求出了这个操作数表达式的值 我就可以用square函数来处理参数 9 9 9 重复刚才的流程 好 也就是说我们要新建一个帧 把形式参数x绑定到参数值9 然后执行函数体计算9乘以9 得到结果81 我们稍微看一下这里的情况 我们只有一个square函数 通过两次调用这个函数 我们创建了两个帧 这两个帧是相互独立的 所以给它们标记为不同的帧标签 f1和f2 不同之处还在于我们传入了不同的参数 所以形式参数与实参之间的绑定关系也不同 从而得到了不同的返回值 环境就是一系列帧组成的 之前我们接触的环境就是只有一个全矩帧 这个在我们还没用代幅语句时就存在了 但当我们开始调用自定义函数时 就开始出现多帧环境了 也就是包含矩队的环境了 我们来看看这张图里都有哪些不同的环境 第一个环境只有全矩帧本身 第二个是f1帧 后面跟着全矩帧 第三个是f2帧 然后也是全矩帧 所以这里一共有三个不同的环境 没有一个环境包含了所有的三个帧 但每个帧都对应着一个环境 如果我们从某个特定的帧去调用 我们就会看到 这个环境是一个三个帧 这个环境是一个三个帧 这个环境是一个三个帧 这个环境是一个三个帧 开始 只要顺着帧的复集帧找下去 就能找到整个环境 比如说我们现在关注以f 两帧开头的这个环境 我们知道下一帧 就是当前帧的副帧 也就是全矩帧 这里写着呢 全矩帧没有副帧 它们总是环境中的最后一帧 很重要的一点是 名称离开这些环境就没有意义 名称离开这些环境就没有意义 正是这些环境 就是moo 还有xsq 这些名称赋予了意义和价值 每个表达式 都是在某个环境中进行求值的 这样我们才能理解 名称的含义 名称会被解析为 在当前环境中 最早找到它的那个真理 所绑定的值 

那么在这个例子中 当我们第二次计算 mooxx 这时 我们处于一个从真f2开始的环境里 这时我们处于一个从真f2开始的环境里 接着是全矩帧 这里我们需要查找两个名称 mooxx 那么当我们查找x的时候 我们首先会在当前环境的第一帧里找 我们首先会在当前环境的第一帧里找 果然就在那里找到了x 所以这就是最早能找到x的那个真 所以x的值就是9 当我们查找moo这个名称 当我们查找moo这个名称 f2帧里找有没有moo 结果没找到 于是我们就在环境中看看下一个帧 你瞧moo就在这儿 绑定的是乘法函数 这又是一个例子 展示了我们在当前环境里 找到名称的最早帧的情况 只不过这次碰巧是在全矩帧里找到的 但我们的确先检查了f2帧 但我们的确先检查了f2帧 

同一个名称 在不同的环境里可能有不同的含义 因为每个帧都可能有不同的含义 所以我们可以对同一个名称有不同的绑定 特别要注意的是 调用表达式和背调函数的函数体 调用表达式和背调函数的函数体 是在不同环境中求值的 是在不同环境中求值的 举个例子 我们用square 同时作为函数名 和形式 参数的名称 定义函数square 反回moo 我不建议大家真的这么写代码 但如果你计算square 这是为什么呢 我们来看这个例子的环境图 这里有一个全局针 这里的square 绑定到平方函数 还有一个局部针 这里的square绑定到4 因为我们用参数4 调用了平方函数 所以区别在于 当我们计算square这个操作符时 当我们计算square这个操作符时 我们是在全局针中计算表达式的 我们是在全局针中计算表达式的 这就是调用表达式 它完全没有缩进 这表明它会在全局针中求值 这就是调用表达式 这就是调用表达式 这一行是缩进的 它属于square函数的函数体 所以它总是在一个 从局部针开始的环境中执行 从局部针开始的环境中执行 因为我们创建了这个针 然后执行函数体 然后执行函数体 所以这个square会在f1开始 全局针后续的环境中求值 但当我们查找square的含义时 我们总是先在f1中查找 大家看在这里我们找到了4 所以我们永远不会找到这个绑定 所以我们永远不会找到这个绑定 因为我们只关注当前环境中的最早针 因为我们只关注当前环境中的最早针 