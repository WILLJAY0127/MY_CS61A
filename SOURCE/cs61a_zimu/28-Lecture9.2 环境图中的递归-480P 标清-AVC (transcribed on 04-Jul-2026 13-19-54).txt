我们来看看环境图中的地归 环境图的一个优点就是 能帮我们理解地归函数 下面我们来看一个新例子 阶乘函数 它计算n的阶乘 也就是n乘n-1 再乘n-2 再乘n-3 这样一直乘到1为止 我们先从Python Tutor开始看起 好 这里是一个实现 我们定义n的阶乘 是这样的 如果n等于0 返回1 这是阶乘定义的一部分 也就是说0的阶乘等于1 否则就返回n乘以n-1的阶乘 看 这里就出现了地归调用 最后 我们用参数3 来调用阶乘函数 结果应该是3乘2乘1 这样计算就完成了 好 首先定义这个函数 然后用参数3来调用 因为3不等于0 所以我要计算n乘以n-1的阶乘 所以我们需要搞清楚 这两个相乘的值分别是多少 这里需要计算n-1的阶乘 此时的n-1就是2 所以我们要计算n-1的阶乘 所以我们需要计算n-1的阶乘 所以我们需要计算n-1的阶乘 所以我们再次调用阶乘函数 这次n的值被设为2 因为这就是我传入的参数值 然后再次执行函数体 这次是在新的环境中 m被设为2 而2不等于0 于是我们再次计算n乘以n-1的阶乘 不过在这个环境中 n-1的结果是1 也就是说 下次调用阶乘时 n-1的阶乘是1 n会变成另一个值 现在我们已经有1、2、3次调用了 会不会还有下一次调用呢 我们来看看 这意味着要再次执行阶乘函数的代码 好 我们执行fact 看看1是不是等于0 显然不等于 所以我们要再执行一次 这个返回操作 也就是计算fact 那现在n-1是多少呢 嗯 结果是0 所以我们就用0来调用fact函数 然后我们再次执行这个函数体 看看0是不是等于0 啊哈 确实相等 所以我们就返回数字1 现在终于得到了一个返回值 那返回的时候会发生什么呢 我们会返回到之前正在执行的内行代码 我们最初调用这个函数体 我们会返回到之前正在执行的内行代码 我们会返回到之前正在执行的内行代码 我们会返回到之前正在执行的内行代码 也就是刚才调用的fact n-1 当时f3是当前环境的第一个针 我们发现在那个针里 fact n-1返回了1 好 现在我们看到这个针也返回了1 因为n现在是1 这个表达式算出来也是1 1×1等于1 于是我们返回到刚才的位置 也就是最初调用函数的地方 当时我们正在执行f两针里的返回语句 这次调用依旧位于同一个函数体内 我们再次计算这个我高量显示的表达式 现在这个表达式的返回值是1 我们用n等于2×fact的返回值 我们用n等于2×fact的返回值 我们用n等于2×fact的返回值 我们用n等于2×fact的返回值 1 2×1等于2 所以我们的返回值就是2 于是我们把2返回到同一个位置 但是在不同的调用框架里 回到最初执行fact函数的return语句时 那时我们正在用3×这里得到的结果 也就是刚才这个返回值 那么3×2等于6 所以fact 3整个反回值是2×2等于6 所以fact 3整个反回值是2×2等于6 所以fact 3整个反回值是2×2等于6 所以fact 3整个反回值是2×2等于6 所以fact 3整个反回值是2×2等于6 所以fact 3整个反回值是2×2等于6 所以fact 3整个反回值是2×2等于6 这个答案确实是对的 因为3×2×1确实等于6 在执行过程中的某个时间点 环境图的状态是这样的 这时候我们正在从最后一次fact调用返回 但之前的几次调用还没有返回 我们来分析地归函数调用时 会出现的一些特性 我们可以看到 同一个函数被多次调用 这里的这次调用来自这个位置 还有几次不同的调用也是从这里发出的 所以这次调用是在这里 而F2 F3 F4这些环境帧 都是通过fact函数内部递归调用产生的 不同的环境帧就是为了记录 每次调用时传入的不同参数 正因如此 我们才能用不同的参数 多次调用同一个函数 甚至实现函数嵌套调用 计算机也不会搞混 因为它采用了这种求值模型的设计 不同的环境帧会存储传入的不同参数值 N的计算结果取决于当前所处的环境帧 所以这些不同的N值 分别对应着fact函数的不同词调用 这意味着每个包含N的表达式 都可能计算出不同的值 具体 看我们当前在哪个环境中 每次fact调用解决的问题 都比前一次更简单 在这个例子里 因为N值在不断递减 一旦N变为0 计算就变得非常简单了 我们直接返回 1需要的工作量就更少 当N等于0的时候 N等于1时 这又比N等于2时的工作量少 这又比N等于3时的工作量少 

现在我们可以比较一下 迭代与递归之间的差异 所以迭代是递归的一种特殊情况 这是我们的阶乘函数 它是这样运作的 我们再来看另一种实现方法 用while循环代替递归 也就是说要计算N的阶乘 我们可以记录累计的阶乘结果 以及接下来要乘进去的数字 只要k还小于等于0之后 等于等于N 就把total更新为 当前值乘以下一个数字 同时把k加1 等while循环执行完毕后 total就是N的阶乘结果了 所以k的值一次是123 然后把这些数都乘起来 而用递归的话 表述就简洁多了 我们说当N等于0时 阶乘结果就是1 否则就是N乘以的阶乘 这样不仅解释起来更快 代码写起来也更简短 而且别人看这个函数时 逻辑也更容易跟上我 所以确实有些情况下 递归能让问题简化 我觉得这就是个很好的例子 这两种实现方法 对应的数学公式呢 稍微有点不一样 迭代版本就是说 我从k等于1开始 然后逐步增加到N 每次都乘以当前的k值 这样就能得到N的阶乘了 这个感叹号表示阶乘 

递归定义对应的数学表达式是这样的 如果N为0 阶乘就是1 否则就是N乘以N1的阶乘 这同样是阶乘的正确定义 只是形式上看起来有点差别 所以这个定义正好对应那个表达式 这个版本同样对应这个公式 但这里需要一些额外的处理机制 来追踪运行状态 确保每一步的乘法运算都正确 最后返回正确结果 现在只要看变量名数量 就能发现 factiter比递归版本要复杂 我们有变量N,total,k还有函数名 factiter 而递归实现中我们只需要两个变量 这就需要靠环境图中的环境针 来追踪计算进行到了什么位置 但在左边这个版本里 我们需要一直追踪总数 total和变量k的值 