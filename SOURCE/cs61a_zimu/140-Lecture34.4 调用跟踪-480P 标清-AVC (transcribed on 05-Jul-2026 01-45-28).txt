我们再来看一个红的例子 这次是关于跟踪地归函数执行过程的 我们之前在这门课里 一直在做地归调用的跟踪 在Python中 我们是通过装饰器来实现的 这里有一个叫做Trace的装饰器 它接收一个需要被跟踪的函数 定义这个函数的一个新版本 返回值相同 但在返回之前 会打印出函数被调用的信息 这个F字符串 在括号前面有一个部分 用来获取被调用函数的名称 如果你之前没见过这种写法 处理函数的方法就是 查找它的内置名称 接着会出现一对括号和另一个表达式 也就是函数调用时的参数值 这里我用地归方式定义了n的阶乘函数 如果n为0 返回1 否则的话 返回n乘以n-1的阶乘 通过在def前面添加Trace装饰器 Fact实际上绑定的不是阶乘函数本身 而是调用Trace的返回值 所以Fact绑定的是这个追踪版本的阶乘函数 比如传入5这个数字 它会显示出函数 我先打印出Fact5 然后调用Fact函数计算5 好 现在我们就处在当它进行地归调用Fact4时 实际上调用的是追踪版函数 所以如果在追踪后调用Fact5 我会看到Fact5调用Fact4 然后是Fact3 Fact2 Fact1 Fact0 这些调用最终都会返回 最终结果还是120 所以所有包含Fact的调用信息都会打印出来 而这个就是返回值 那这个就是Fact5的启动函数 那我们能在Skim里实现类似的效果吗? 不过呢 Skim里并没有装饰器语法 感觉难度还挺大的 那我们具体能怎么做呢? 其实有个方法 不用红也能做到 我们可以定义一个阶乘函数 逻辑跟Python版一样 然后把符号original指向这个Fact函数 再重新定义Fact 然后把符号original指向这个Fact函数 让它先打印信息 再调用原始函数 它会打印出包含Fact符号和数字N的列表 这样就能得到下面的输出结果了 很像 对吧 不同的地方是 Python实现了关注点分离 写Fact函数时 我不需要知道任何跟踪相关的知识 写Trace时 我也不需要了解 阶乘函数的细节 连接两者的就是这个简洁的装饰器语法 但在Skim中 我们写了同样的跟踪函数逻辑 但我们必须显示的把它叫做Fact 才能确保 当原始Fact函数进行递归调用时 最终会返回到打印版本里面 这样不仅会打印Fact5 还会依次打印Fact4 3 2 1 和0 我们能否打印Fact5 能否在Skim中将其通用化 避免为每个要跟踪的函数重复实现打印逻辑 我们能不能设计出类似Trace装饰器的东西 只需编写一次 然后就能反复使用呢 好 我们来探索这个思路 写一个红看看 这里有一个阶乘函数 当我们调用Fact5时 得到的结果是120 但没有任何打印输出显示 通常的做法 是给递归过程分配另一个符号 来获得打印输出 然后重新定义Fact函数 让它先进行打印操作 再返回结果 现在当我们调用Fact5的时候 就能得到带有跟踪功能的版本 在跟踪之前 它原本返回的是120 但当我们重新定义之后 再次调用Fact5时 就能看到完整的跟踪输出信息 我们想要实现的是这样一种情况 在某些情况下调用Fact5时 可以启用跟踪功能 有时则不起用跟踪 要想回到之前的状态 也就是不再进行跟踪的状态 我们需要把Fact重新设回原始函数 这时候如果我们再调用一次Fact5 会看到跟踪前的结果是 120 当我们重新跟踪后 它会输出跟踪信息 并返回120 但当我们切换回原始函数时 它只返回120 不带跟踪信息 为了把这个逻辑封装成通用红 我们需要把它变成一个表达式 而不是四个分开的 也就是说 我们要写一个begin表达式 这个begin 首先会定义原始函数 然后定义新版本 再调用它之后 那么如果我们想要确保 这个begin表达式有一个返回值 我们需要捕获 调用它的结果 然后我们把Fact切换回原来的形式 然后把结果作为begin的返回值 所以运行这个代码 会得到同样的输出结果 现在我们有了一个完整的表达式 它显示了调用Fact50的追踪效果 现在的难点 是要把这个逻辑 写成一个反回值 它是一个通用形式 但因为它里面有好几个define语句 会改变Fact的行为 我觉得不能用标准的procedure来写这个 我们需要用红来实现 用来追踪地归函数的调用过程 我们叫它Expert 就像是执行traceFact 5来显示追踪效果 所以如果你想追踪Fact5 我们需要输出Fact这个运算符 这就是运算符符号 我们可以用这个运算符符号去输出Fact5 我们给它起个名字 运算符 就是表达式的第一个元素 所以就像Fact本身这样 接着我们需要构建这个begin表达式 这就意味着要使用准引用 当我们定义原始版本时 我们不希望它总是固定为Fact 我们希望它能适配任何运算符 所以我们用反引用来确保运算符 能够被正确求知 然后我们会重新定义运算符 比如这里的Fact 把它变成一个会打印调用信息的新Num的表达式 而不是每次都只打印Fact 我们想要打印出具体的运算符 以及参数N 然后我们还需要调用原始函数来处理N 这也是我们生成代码的一部分 在定义结果的时候 而不是每次都固定调用Fact5 我们要调用的应该是表达式本身的值 比如说就是Fact5这个表达式 最后我们把运算符恢复成原来的样子 这样如果有人再次调用 就不会出现不同的结果了 接着我们返回结果 结束Begin表达式 结束红的定义 现在我们可以跟踪Fact5 应该就能看到跟踪效果了 不过这个版本有个问题 我们来看看具体是什么 我们定义Fact函数 调用得到120 我们来看看具体是什么 我们创建了一个红 这就是跟踪红 当我们对Fact5应用跟踪红时 计算120之前会输出很多执行信息 看起来效果不错 但我们没有显示Fact5的调用 相反 我们显示的是Fact过程的内部表示 看起来就是那个样子 但我们真正想要显示的是Fact这个符号 我们可以通过引用这个符号来实现 通常称之为运算符号 所以如果我们重新运行一次 现在就能看到我们想要的效果了 直接调用Fact5会返回120 跟踪Fact5会显示执行过程 同时也会返回120 但如果之后我再调用Fact5 一切就恢复正常了 因为我已经把Fact恢复到了原始定义 这样设计的好处在于 这个Trace红完全不涉及Fact过程本身 它假设 Expert是一个可能递归 且只接受一个参数的过程调用表达式 之所以限定为一个参数 是因为我们定义这个Lambda表达式时 就设定了单参数 不过你也可以尝试写个更通用的版本 现在我们就实现了和Python里一样的 关注点分离 但这个版本其实比Python的还要好 因为在Python里 你必须决定Fact函数 是否要一直处于追踪状态 这取决于它有没有被装饰器修饰 但在Skin版本里 我们可以选择性的以非追踪模式调用Fact5 需要跟踪时再跟踪 甚至可以随时切换 所以关键在于 是否跟踪某个具体的调用表达式 而不是永久性的跟踪整个函数 这样就提供了更大的灵活性 很酷 