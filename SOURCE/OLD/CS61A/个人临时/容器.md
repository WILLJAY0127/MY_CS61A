11.3 for循环的环境图比较模糊

盒子指针标记法 来理清各个元素之间的关系


# python里数组和字符串in逻辑不同的探讨
完全对应上你的理解，Python 的这套设计逻辑和 Java 接口 / 抽象方法、多实现思路高度同源，我分两段对比讲清楚：
一、统一抽象接口：__contains__ 相当于 Java 的接口方法
Python 有一套魔术方法（特殊方法），相当于语言层面内置的统一抽象接口：
运算符 x in obj 底层等价调用：obj.__contains__(x)
只要类型实现了 __contains__，就能支持 in 操作，这就是多态。
对应 Java 类比：
Java 定义接口 Containsable，规定方法 boolean contains(Object o)；
String、ArrayList 分别实现这个接口，各自重写 contains；
调用 obj.contains(target)，不同对象执行完全不同逻辑。
Python 只是把这套接口做成了语言内置魔术方法，不用显式写 implements，隐式实现。



13.4 维护抽象屏障的目的，更好的维护代码，而不用过多的重写代码，也就是实现控制。

数据抽象的核心理念，通过数据抽象的行为特征来判断他的类型，不一定非要看他的构造方式。


对象里的是methods ，之前函数抽象的那些事function


# 面向对象
 我们之前用数据抽象来实现封装 而面向对象编程则更进一步 它不仅能像数据抽象那样整合多个信息片段 还把相关行为也打包在一起 这样一来对象它代表现实世界中的某个事物 比如一个日期

 面向对象是一堆不同的对象相互交互 每个对象各自管理自己的信息 所以每个对象都有自己的局部状态 这些状态会随时间变化 所以面向对象编程经常会涉及可变性 虽然这不是必须的 但发生可变性的时候 它不是发生在追踪所有程序信息的全局状态 而是在各个对象的局部状态里 变化的信息其实属于特定对象 每个对象都知道如何管理自己的局部状态

 那么方法和函数的主要区别在于 函数通常是在全局作用域里定义的 而且可以对任何值进行调用 而方法呢是特定对象专有的 所以我们可以用这个方法来调用这个方法 我们可以把这个方法调用 我们可以用这个方法来调用 我们可以用这个方法来调用 但在这个方法 我们可以把这个方法调用 但是我不能随便在什么上面都调用append 但在s这个列表上 我就可以调用它 因为s对象本身就知道 如何执行append的方法 
